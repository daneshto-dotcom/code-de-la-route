═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-21
Session: S116 — THE SESSION THE GAME BECAME VISIBLE
(5 priorities, all complete)
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ `84fe625` · submodule `master` @ `4c10bba`
- **UNPUSHED: parent 6 ahead of `origin/main`, submodule 8 ahead of `origin/master`.**
  Push is operator-confirmed and was NOT done. Remotes exist:
  `github.com/daneshto-dotcom/legacy-of-the-realm` and `.../founding-realm`.
- Stack: TypeScript + Phaser 3.90 + esbuild; art generated by Python into a 174-tile atlas
- Game: `Game/founding-realm/rebuild` — GBC Crystal-style rebuild, 320×288, 16px metatiles
- Save schema: **v6, unchanged.** Nothing this session needed a bump.

## CURRENT STATE
- Build: **PASSING** — `npm run build` exit 0, 11 PASS lines
- Guards: **7/7 PASS** (boundary, palette, reachability, realm, evidence, staleness, bundle)
- `tsc --noEmit`: clean, `tools/shoot.ts` included
- Tests: evidence-check 12/12 planted violations refused + control;
  save-migration **2958 assertions, 0 failed** (was 2915 — **+43**, three new sections)
- gitleaks: no leaks
- Context at close: **~460K / 1,000,000 (46% GREEN)**
- CHECK: **DEGRADED to 2-way** (Grok timed out twice at 120s; see PROMOTION CANDIDATES)
- MCV `verify-session-claims`: **hard_fail=0 warn=0 -> exit 0**, 27 modified files, **0 UNBOUND**,
  **55 typed assertions** across five priorities. It hard-failed 26 on the first run and every
  one was mine to fix — see MCV RECONCILIATION below.
- ANALYZE: completed, both seats gave calibration retrospectives
- Rule 22 runtime audit: completed, including a **restart-survival check** that passed

---

## THE ONE THING THAT MATTERS: YOU CAN SEE THE GAME NOW

S115 closed saying *"THE DIAGRAMS HAVE NOT BEEN SEEN BY A HUMAN."* That is no longer true,
and the block that caused it was never really a block.

The pane still does not composite — `computer{screenshot}` times out, `loop.frame` stays 0
while `actualFps` cheerfully reports 60. But a frame is still recoverable: **drive the loop
by hand, then read the canvas back in the same task.** The renderer is WebGL with
`preserveDrawingBuffer: false`, which should hand you a blank frame — except nothing ever
swaps the buffer here, so the back buffer still holds the render. The trick is a consequence
of the very breakage it works around.

**Six defects fell out of the first minute of looking.** `tsc`, seven guards and 2915
assertions had passed over every one of them.

Codified as `npm run shoot` (sink + `--client` prints the page-side driver) and
`npm run shot` (rows/cols/diff/zoom/sheet). **Read `rebuild/docs/SEEING-THE-GAME.md` before
you try to look at anything** — it has all the traps.

---

## WHAT A PLAYER CAN DO THAT THEY COULD NOT YESTERDAY
1. **Read the keys page without three labels colliding**, and see the word `FIXED` in full.
2. **Read help card 5** without its last word running off the panel.
3. **Watch the throw actually arrive** in the reticle and stay there long enough to register.
4. **Change their favoured hand** — the JOURNAL now has the control the creation screen has
   been promising since S115.
5. **Put a thing down from the pack**, one at a time, and be told why when they can't.

## THE FIVE PRIORITIES

**P2 — the five layout defects** (`a5275a0`, bound `4d53115`) — ran first, on purpose
| # | Defect | Fix |
|---|--------|-----|
| D1 | hint at y=26 (ink 29..35) collided with FIRST/SECOND at y=32 (35..41) **and** the page tab at y=22 | only the hint moved, shortened to clear the tab |
| D2 | `FIXED` at x=294 + 5 glyphs = 324, past the 320 edge → rendered `FIXE` | right-aligned to `W - 10` |
| D3 | card 5's subtitle clipped to `...to spli` | `fitLines()` — a **measured** wrap |
| D4 | the throw reached the sight only at t=1.0, the loop-wrap instant | lands at 0.92 and holds |
| D5 | pack footer's bottom glyph row under its own border | `H-14` → `H-16`, the JOURNAL's value |

**P1 — the capture harness codified** (`665f1b6`, `b75a02b`) — `tools/shoot.ts`,
`tools/shot-inspect.py`, `docs/SEEING-THE-GAME.md`. Verified end-to-end through the
*committed* tool, not the scratchpad original.

**P3 — FAVOURED HAND** (`5ede670`, `00230a4`) — an 11th row at y=236, ENTER flips it.
No schema bump: `setHandedness()` already existed. **+13 assertions.**

**P4 — drop from the pack** (`f33daf4`, `b884cc6`) — `placeFromPack()` draws exactly one off
a stack and **refuses** an occupied tile. On `aim`, the only action the pack screen did not
already use. **+17 assertions.**

**P5 — the shadow state dir** (`3e2f657`, `7ff90d1`) — removed. See below.

**CHECK** (`4c10bba`, `84fe625`) — test 17, the parcel-id invariant. **+13 assertions.**

---

## THREE FIXES THAT REVEALED A SECOND BUG UNDERNEATH

Worth internalising — it happened three times:

1. **D1's first fix traded one collision for another.** I moved the column headers down 4px.
   Correct about the hint, wrong about the destination: the selected row paints
   `fillRect(6, y-3, …, ROW_H-3)`, so row 0 owns **43..58**, and the headers landed at 39..45.
   I had measured the text bands and not the **chrome** bands. A photograph caught it.
2. **D4's timing fix exposed a geometry bug.** Making the arc land at 0.92 put the projectile
   on screen long enough to see it came down **24px below the reticle** — a pure `sin()` bulge
   returns to its launch height while the sight had risen. The defect had always existed and
   was unobservable because the frame was never displayed.
3. **P3's new row created a spacing defect.** The 11th row put the notice banner's fill at 246
   against row ink ending at 245. An ink profile proved the text was *not* clipped — it only
   read that way — so the fix was spacing, not layout.

## THREE FALSE DEFECTS I ALMOST FILED, AND WHAT KILLED THEM
| Claim | Killed by |
|---|---|
| "help card 3 does not animate" | pixel diff — **identical PNG byte counts in genuinely different states.** Byte length is not pixel identity |
| "help card 4 has no throw arc" | sampling a **full** period — it draws only at `t ≥ 0.78` and I had sampled 0.22 of a 3200ms cycle |
| "row 10 is clipped by the banner" | a row-by-row ink profile — the glyphs were complete |

**An impression of "clipped" was wrong three times out of three.** Measure.

---

## P5: THE CARRY-FORWARD'S DIAGNOSIS WAS WRONG

`CF-S115-SUBMODULE-STATE-SHADOW` said the shadow bites *"whenever cwd drifts into
`Game/founding-realm/**`."* **It does not.** Shell cwd was at the project root all session —
the Bash tool resets it after every call — and the shadow was written twice anyway, at 11:18
and 11:19. It is the **file path handed to the hook** that decides.

`find_claude_dir()` (`json_helpers.sh:88`) walks up from a path it is *given* and stops at the
first ancestor whose `.claude/` holds a `session-state.json`. Run with the real logic:

```
BEFORE                                          AFTER
rebuild/src/core/Journal.ts -> submodule  WRONG   -> project root  ✓
rebuild/tools               -> submodule  WRONG   -> project root  ✓
Game/founding-realm         -> project root  ✓    -> project root  ✓
```

So "don't `cd` in there" would have prevented nothing. The shadow was stamped `session_id:
S105` — **eleven sessions stale** — with all priorities terminal and no `analyze_completed`,
which is exactly the shape GOV-28 complains about. It fired a false AUTO-CLOSE against S114
in S115 and against a fresh session in S116.

Removed: the gitignored `session-state.json`, 14 zombie lockdirs, 45 orphaned tmp files, 2
stale `.bak`. **Rule 22 confirmed it survived a session restart** — no shadow recreated, and
`session-summary.md` still reads `_Last updated: 2026-08-20 19:20:08_`, the restored value,
where before the fix a hook restamped it every few minutes.

## MCV RECONCILIATION — 26 HARD FAILS, THREE CLASSES, ALL MINE

The Stop hook caught this before close and it was right to. None of it was fabrication, but
all three classes were real protocol misses:

1. **5 fails — every S116 priority UNCOVERED, 0 assertions.** I wrote rich `check_method` prose
   and no machine-checkable assertions. **Prose is not verification** — that is the entire point
   of MCV. Now 55 typed assertions across P1-P5. Before writing a single one I grepped every
   candidate needle against disk: 52 passed, **1 failed** (`NEVER NULL` lives in my commit
   message, not in the code — the real string is `Flip the favoured hand, and NEVER to null.`).
   Writing an assertion you have not verified is precisely the sin MCV exists to catch.
2. **2 fails — S115-P1/P3 asserted `function hintLine()` in Subscreen.ts.** True when written;
   P4 replaced it with `hintFragments()`. **Superseded and logged, not deleted**, per the S115
   precedent, with a note recording that the asserted capability (a keymap-derived footer rather
   than a literal string) is intact and strengthened. Logged at `mcv_superseded_s116`.
3. **19 fails — UNBOUND modified files.** 16 were `docs/shots/*.png`, captured frames that are
   gitignored and regenerate in seconds; added `*/rebuild/docs/shots/*` to `ignore_globs` **with
   a written rationale**, matching the precedent set for `dist/` (S104), `docs/renders/` (S106)
   and the S110 film media. The other 3 — `tools/shoot.ts`, `tools/shot-inspect.py`,
   `docs/SEEING-THE-GAME.md` — are real shipped artifacts and now carry 20 assertions between
   them. **The sources stay bound; only the screenshots are ignored.**

## OPEN ISSUES
- **`CF-S116-FIND-CLAUDE-DIR-SUBMODULE` (new, HIGH).** P5 is a **mitigation**. The root cause
  is that `find_claude_dir` has no concept of a submodule boundary, and any future
  `session-state.json` there recreates the whole problem. **There is already a precedent for
  the fix:** S164 added a skip for `~/.claude` because *"stale session-state debris there
  poisoned non-project sessions"* — a submodule is the same bug one directory down. That file
  is shared by all eight Founder DNA projects → SYNC-BRAIN Tier 0/1, own amendment.
- **`CF-S116-STALE-S105-SUMMARY` (new, LOW).** `Game/founding-realm/.claude/session-summary.md`
  is **tracked**, titled "S105", and was hook-written. Now inert, but it is the other half of
  the shadow and wants a decision.
- **`CF-S116-ONE-WAY-RECONCILE` (new, LOW).** `syncDroppedItems()` only adds. Both external
  seats raised it independently. **Unreachable today** — the only deleter of `world.dropped` is
  callable solely from the map scene, never while paused. Recorded, not built: a removal path
  must reason about restoring `walkable`, which is real risk for zero present benefit.
- **`CF-S107-KEY-EXPOSURE` — rotate the `.env` keys. Open since S107. HIGH. Owner-only.**
- The five diagram frames are **mostly empty** — a 240×150 box with content in a small central
  band. Composition, not a bug; the owner's call.
- The `man` glyph **reads as a bottle** when standing still.
- `bottle_empty` / `bottle_water` / `key_iron` are still **provisional art** (offered this
  session, not selected).
- **Sketchbook API is still dead code** (0 callers).
- The hand-key tap still fires on **release**, not press.

## BLOCKED ON DANIEL
- **Push?** 6 + 8 commits sit unpushed. One word.
- **`CF-S107-KEY-EXPOSURE`** — rotate the keys. Open since S107.
- **The gate's lower half: open bars or solid panel?** One photograph settles it.
- **A tape measure for STO-1** — unblocks a real interior behind a door that already opens.
- **Backlog #29, the film** — parked; direction is stills, not video.
- **Dael's tutorial role** — unanswered since S110.

## NEXT STEPS
1. **Look at the pictures.** `docs/shots/` is gitignored but regenerates in seconds:
   `npm run dev`, `npm run shoot -- --port $SESSION_PORT`, paste `--client`, press ESC.
2. **Say whether the diagram frames need filling.** They are sparse and I did not touch
   composition without you.
3. **`CF-S116-FIND-CLAUDE-DIR-SUBMODULE`** — the real fix, own amendment, 8 projects.
4. Redraw the three provisional item sprites.
5. Wake the sketchbook on first pickup, or delete it.

## PROMOTION CANDIDATES (ADVISORY — never auto-codified)
- **GROK — `CAP-THE-SEAT`:** default external prompts to **≤200 words** with an explicit
  measurement mandate. Two 120s timeouts on detailed prompts, then a useful answer in seconds
  under a cap. Throughput fix *and* quality fix — the cap forces the seat to rank findings.
- **GEMINI — `TRACE-NOT-SCORE`:** replace scoring with "trace ONE entity end to end including
  serialization." Proposed S115, applied twice here, produced the **best finding in PLAN** and
  a **shipped test in CHECK**, after two sessions of zero-finding perfect scores. **This one
  has earned codification.** His own word for why: *procedural grounding* — a bottom-up
  directive forces mechanical simulation where "score this" permitted pattern matching.
- **GEMINI — `QUOTE-THE-PRECONDITION`:** if the precondition that makes a failure reachable
  cannot be quoted from the brief or the source, suppress the claim. His proposal; it would
  have suppressed exactly the two claims of his that measurement refuted.
- **CLAUDE — `MEASURE-BEFORE-RECORD` applies to me too.** I wrote it for the external seats
  and needed it more: three near-miss false defects, all caught by measuring, none by thinking
  harder.
- **CLAUDE — `PROBE-BEFORE-ADOPT`,** fourth session running, still uncodified. It decided this
  entire session in two commands.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **5/5 complete** | context ~460K/1M (46% GREEN)
- S116-P2 five layout defects + the arc lands — `a5275a0` / `4d53115`
- S116-P1 capture harness codified — `665f1b6` / `b75a02b`
- S116-P3 FAVOURED HAND — `5ede670` / `00230a4` (+13 assertions)
- S116-P4 drop from the pack — `f33daf4` / `b884cc6` (+17 assertions)
- S116-P5 shadow state dir removed — `3e2f657` / `7ff90d1`
- CHECK test 17 parcel-id invariant — `4c10bba` / `84fe625` (+13 assertions)
- Council: Grok 4 calls (2 timed out), Gemini 3 calls (~$0.45 est, not metered)
- PRIME-AUDIT: **5 of 8 Council claims refuted by measurement**, 3 shipped
- The destructive-command guardrail blocked twice. Both times I complied and reworded; the
  first block genuinely improved the work (per-file deletes + `rmdir` beats recursive force).

═══════════════════════════════════════════════════════════
