═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-12
Session: S104 — FIRST GAME CODE. parcel-001 built from Daniel's photographs,
then reshaped twice by his corrections. 3/3 priorities shipped.
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Git: parent `main` @ **57c7bf1** · submodule `master` @ **cdf2a37** · plan (Founder DNA) `main` @ **d2bf1b1** — **all pushed, 0 unpushed**
- Old build frozen at tag `pre-rebuild-final` = 4dc92e8. New game lives in `Game/founding-realm/rebuild/` (32 files, +4251 lines).
- Stack: TypeScript + Phaser 3.90 + esbuild. Node v24.14.0.

## CURRENT STATE
- Build: **PASSING** — `npm run build` green end-to-end (atlas → player → boundary guard → palette guard → esbuild). `npx tsc --noEmit` exits 0.
- Guards: palette **PASS** (47 tiles, 182 non-empty 8×8 regions, 8/8 background palettes); import boundary **PASS** (5 files).
- Verified in a real browser at `localhost:20694` (Phaser 3.90, WebGL). Dev server stopped; port released.
- MCV: **exit 0** — 63 assertions, 32 files bound, **0 UNBOUND**.
- Gitleaks: 2 findings, both the known `gateway.ts` env-var false positives (March 2026, frozen build). Nothing new.
- Deployment: none. Site intentionally dark.

## SESSION COST
Model routing data unavailable — single-model session (strongest tier pinned per ALWAYS-STRONGEST).

## THIS SESSION'S WORK

**P1 — Zone survey + palette from Daniel's own materials.** He delivered 7 stills
(4096×3072) + a 147 s walkthrough mid-session, clearing the S103 art gate. Read all
7 stills, 7 contact sheets (74 frames @2 s), 10 full-res key frames. Every stone,
render, tile, oak, brick, iron and gravel colour in `palette.ts` was **measured**,
then verified by rendering each crop beside its extracted swatch — **6 of 26 regions
were rejected and re-sampled** after the crop showed sky or shadow instead of material.
Survey → `rebuild/docs/SITE-SURVEY-entrance.md`.

**P2 — `rebuild/` scaffold** (§6): own package/tsconfig, esbuild, `palette.ts` with
full provenance, the build-time **palette guard** (decodes the actual PNG via
`node:zlib`, no deps), the **import-boundary guard**, `ParcelScene` base class, and
the single versioned **`SaveStore`** (mandatory from parcel-001).

**P3 — parcel-001, THE ENTRANCE.** Built, then reshaped twice on Daniel's corrections:
the forecourt is now a **half-circle** (radius 7 tiles = 14 m, ~264 m², generated from
the circle equation), the **road is its edge and is off-limits**, and the player begins
standing in it facing the shut gate. 5 new diagonal arc tiles with a lit dressed kerb
+ stone bollards make the curve read. Movement and collision verified by driving real
KeyboardEvents: from (10,15) the player walks north and **stops dead at the gate**.

**Design work (recorded, NOT built): §11 THE THRESHOLD.** Daniel's isekai design —
forecourt = 2026 with cars; the gate = buying a festival ticket; crossing is one-way;
the world becomes 1601. Corroborated by the archived festival plan, which already seeds
a **"Parking / Modern Zone — modern-world transition zone"** and a **"The Gate" zone
(200 m², cap 30) with a guard post, toll board, brazier and "Captain Dael NPC.
Registration desk"**. The game is mirroring the real attendee experience. Our forecourt
is 264 m² against the festival's 200 m² for the same ground.

## OPEN ISSUES
- **Oak-gate tiles are SUPERSEDED.** Daniel: the iron gate is **1783**, he holds the
  **original plans**, and the estate is unchanged since — so it counts as 1601-era.
  There is no divergence. `gate_l_top/r_top/l_bot/r_bot` need redrawing from the real
  gate. **Do not do this without his go.**
- Building numbers **#13 / #5 / #41 / #38 / #32** are unresolvable — `buildings.geojson`
  carries no numbering at all. Ask Daniel their source.
- Benign: stale `.claude/session-state.json.lockdir.zombie.*` + `.tmp.*` in both repos.
  Gitignored. The destructive-command guardrail correctly blocked automated `rm -rf`
  (not pre-approved); left in place.
- `.gitleaksignore` allowlist for the two `gateway.ts` false positives (cosmetic).

## BLOCKED ON
- **Daniel's 7 full-HD videos of the starting zone** — first thing next session.
- `REAL_FESTIVAL_DATE` (§9.10 RISK-1) and the Cloudflare mail question — both still open.

## NEXT STEPS (priority order)
1. **Receive and mine the 7 videos.** Target what aerials cannot give: **wall heights
   and construction**, the **grass field between house and smithy** (553.09 m²,
   tree-occluded), and **where the old smithy access was**.
2. **Await Daniel's instruction.** He directs every step now (§11.0). His stated
   forecourt sequence: skin as 2026 → guardsman on the RIGHT of the gate → class
   conversation → cinematic → 1601 swap.
3. **Redraw the gate from the 1783 ironwork** on his go; ask for the original plans.
4. Parcel-002 (the bell) is still `← NEXT` in §7, but §11 may re-order it. **His call.**

## CHANGED FILES
`rebuild/` — 32 files, +4251 lines: `src/palette.ts`, `src/core/{ParcelScene,SaveStore}.ts`,
`src/main.ts`, `src/parcels/parcel001/{index.ts,map.json}`, `src/art/{atlas,player}.{png,json}`,
`tools/{build-atlas.py,build-player.py,palette-check.ts,boundary-check.ts,render-map.py}`,
`docs/SITE-SURVEY-entrance.md`, `docs/renders/*` (9 PNGs), `package.json`, `tsconfig.json`,
`esbuild.config.mjs`, `index.html`, `README.md`.
Founder DNA: `BRAIN/architecture/ACTIVE_PLAN_realm_rebuild.md` (§10, §11 new, §7 updated).
Parent: submodule pointer ×5, `.claude/launch.json`, `.claude/verify-watch-roots.json`.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **3/3 complete**
- P1 Zone survey + measured palette — completed — `bb606b2`
- P2 `rebuild/` scaffold + guards — completed — `bb606b2` / `26aca69`
- P3 parcel-001 (built, then reshaped) — completed — `e067dcc` / `cdf2a37`
- MCV **exit 0** (63 assertions, 0 unbound). Review gate **APPROVED** by Daniel.

## REFLEXION ENTRIES (8, appended to `.claude/reflexion_log.md`)
- P1 #pattern-verify-the-crop-not-just-the-cluster
- P1 #pattern-a-clean-test-on-the-wrong-question-refutes-nothing
- P2 #pattern-the-guard-caught-what-discipline-would-not-have
- P3 #pattern-hardware-limits-produce-better-art-than-taste-does
- P3 #pattern-input-nuance-is-a-real-bug-not-a-feel-issue
- P3 #pattern-repeated-tiles-amplify-per-tile-noise
- SESSION #pattern-owner-correction-beat-my-inference-every-time
- SESSION #s104-meta

## CARRY-FORWARD PRIORITIES
None — 3/3 complete. §11 THE THRESHOLD is recorded as a Rule-16 scope amendment
and needs its own PDR when Daniel calls for it.

═══════════════════════════════════════════════════════════
