═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-07-10
Session: S99 — T0 durability off-OneDrive + SQLite adapter + T1 reveal reachability; DEPLOYED LIVE
═══════════════════════════════════════════════════════════

## PROJECT
- Name: Legacy of the Realm — Founding Realm (medieval MMO sim; the hype machine for the real Chateau de Chazeuil festival)
- Working dir: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm
- Git: parent `main` @ 8945a45 (pushed) · submodule `Game/founding-realm` `master` @ 3801748 (pushed) — BOTH CI-clean
- Tech: Node.js + TypeScript + Phaser 3 + WebSocket (ts-node); JSON persistence (SQLite adapter now available, opt-in)
- Codebase: server + sim engine; save now at C:\ProgramData\LegacyOfTheRealm\save (OFF OneDrive)

## CURRENT STATE
- Build: tsc --noEmit clean. Tests: simulation 670/670 + 5 new suites (persistence-mode 11, save-paths 5, sqlite 6, reveal-reachability 9) + persistence-recovery 9 + reveal-reward 12 — all green.
- Deployment: LIVE. legacyoftherealm.com (cloudflared → localhost:3000, scheduled task LegacyOfTheRealm-GameServer). Local + public /health/deep = 200; world 1602/SPRING/day15, 28 chars; crashLoop not degraded.
- Persistence: PERSISTENCE=json (live). SQLite adapter present but opt-in (PERSISTENCE=sqlite). Litestream→R2 staged, awaiting token.

## SESSION COST
- Model: all Opus 4.8 (ALWAYS-STRONGEST — quality over cost, no routing). External API: Grok 1 call, Gemini 1 call (deliberation Council). ~65K/150K budget (GREEN).

## THIS SESSION'S WORK (5/5 code priorities + live deploy)
- P0 (6457e7d) — persistence-mode.ts: PG is now EXPLICIT opt-in (PERSISTENCE=postgres); incidental DATABASE_URL/PGDATABASE/NODE_ENV no longer auto-enable it; explicit-PG-then-unreachable is FATAL not a silent JSON fallback.
- P1/DUR-06 (29e477d) — save-paths.ts single resolver (env-driven SAVE_DIR, one app-root anchor); rewired gateway/routes/3 CLI tools; server.ts write-probe + empty-custom-dir hard-fail guard.
- P2/T0.4 (591be3e) — SqlitePersistence (WAL+synchronous=FULL, single-row blob, lazy require, bootstrap via full JSON recovery chain); staged litestream.yml.
- P3/T0.3 (897674e) — scripts/restore-drill.ts (env-isolated throwaway from latest backup, fingerprint-asserts currentYear/tickCount); added those fields to /health/deep.
- P4/T1.2 (6727066) — reveal reachable (B53 gate 40/50→15/15) + FIXED the pre-existing chainProgress restart-brick (gate derives from persisted completedTemplateIds) + realm_curious signal.
- DEPLOY (3801748) — migrated live save OFF OneDrive → ProgramData + restarted on S99 code; verified local+public /health/deep=200, writes now in ProgramData (fresh heartbeat vs stale OneDrive), stable (uptime>200s, no crash-loop).

## OPEN ISSUES
- Parent-repo `.claude/session-state.json` is a STALE S98 orphan (review gate + budget hook read it and show "S98"). AUTHORITATIVE PDCA state is the SUBMODULE's session-state.json (S99). Known long-standing split; reconcile if it causes confusion.
- Scheduled task LegacyOfTheRealm-GameServer cannot be disabled/changed from a non-elevated shell (schtasks /Change = Access denied; /Run works). A future task-stop needs elevation.
- OneDrive save/ still holds pre-migration originals (stale, no longer written) — intentional rollback safety net; archive/remove later.

## BLOCKED ON (Daniel)
- R2 bucket + S3 API token → activates Litestream off-box replication (then flip PERSISTENCE=sqlite).
- Perk model / redemption / signing-secret (open Q #E) → unblocks T1.4.
- Playtest: Knight → wolf (verify B-01 real VICTORY); __previewRoyalSummons() in console.

## NEXT STEPS (priority order)
Immediate: (1) Daniel playtest + R2 token. Short-term (S100): (2) T1.3 legend sheet, (3) T1.4 real perks (QR), (4) B-04/B-05 quest reachability, (5) D2/D6 admin auth. Carry: wire Litestream when token lands; add sealed-letter reveal beat (voice review).

## CHANGED FILES (submodule bbef374..3801748)
persistence/{persistence-mode,save-paths,sqlite}.ts (new) · persistence/index.ts (exports) · networking/{gateway,routes}.ts · server.ts · main.ts · seed/festival-seed.ts · scripts/{migrate-json-to-pg,restore-drill}.ts · modules/quest-engine/{trigger-engine,emergent-templates,index}.ts · litestream.yml (new) · tests/{persistence-mode,save-paths,sqlite-persistence,reveal-reachability}.test.ts (new) · package.json · .claude/session-state.json

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 5/5 complete + deployed | ~65K/150K (GREEN)
P0 persistence-determinism — completed — ~6K — 6457e7d
P1 DUR-06 env-SAVE_DIR + migration — completed+deployed — ~12K — 29e477d
P2 T0.4 SqlitePersistence+Litestream — completed — ~20K — 591be3e
P3 T0.3 restore drill+fingerprint — completed — ~11K — 897674e
P4 T1.2 reveal reachability+restart-brick — completed — ~16K — 6727066

## REFLEXION ENTRIES (this session)
- P0 #read-the-env-before-naming-the-landmine · P1 #one-anchor-not-per-file-dirname · P2 #lazy-require-native-addons · P3 #restore-drill-must-fingerprint · P4 #derive-gate-from-persisted-state · SESSION #s99-meta (full text in .claude/reflexion_log.md)

## CARRY-FORWARD PRIORITIES
None blocking — S99 complete. S100 = T1.3/T1.4 + B-04/B-05 + D2/D6 (campaign block). Litestream-R2 + sealed-letter beat carried with reasons above.
═══════════════════════════════════════════════════════════
