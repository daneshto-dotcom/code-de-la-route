===============================================================
HANDOFF SUMMARY - Legacy of the Realm
Generated: 2026-08-29 | Session S121
Focus: the server switched on, the world began keeping time, and night arrived
===============================================================

## PROJECT
- Working dir: C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm
- Parent main bbccc76 | submodule master eeb1c1f | both clean, both PUSHED (0 unpushed, remotes alive)
- Stack: TypeScript + Phaser 3.90 + esbuild ESM -> Cloudflare Worker + Neon Postgres 18.6
- Codebase: 37 src .ts files, ~15,360 lines, 11 test suites

## CURRENT STATE
- Build: PASS - 9 guards (12 PASS lines; 3 are prebuild generators, NOT realm:build)
- Tests: 3,966 assertions 0 failed | test:db 99 passed (Docker) | MCV hard_fail=0, 0 UNBOUND
- Deployed: https://legacyoftherealm.com - bundle byte-identical, 21/21 round trip
- Database: Neon PG 18.6, 1 real player, 1 save row, 1 realm (chazeuil-i), realm day 16

## SESSION COST
- Model: opus throughout (ALWAYS-STRONGEST). External: gemini 3 calls, grok 2 (1 timed out).
- Estimated external API cost: ~$0.00-0.05. Cumulative: ~/.claude/usage-log.csv

## THIS SESSION'S WORK - 6 priorities, all completed and verified
P0 THE SERVER IS ON. Schema applied to Neon; DATABASE_URL + a fresh 32-byte SESSION_SECRET piped
   from the vault so no value touched a file, an argv or shell history. /api went 503 -> live.
   The $5/mo Workers-Paid claim is RETIRED, measured: PBKDF2 x100,000 registers in ~310 ms.
P1 realm_id in the save PRIMARY KEY while the table was still empty - the one irreversible-if-
   skipped decision, taken at zero cost. Realm as a PATH segment. Plus autosync + flush-on-hide,
   closing CF-S120-NOTHING-PUSHES-DURING-PLAY.
P3 The world clock as a PURE FUNCTION of (realm.epoch, serverNow). Nothing ticks.
P4 `progress` split out of `worldFlags` (save v8 -> v9), with the v8 branch that stops every
   current player being blanked by the fallthrough to fresh().
P5 Night inks: 15 ramps authored by Gemini, VALIDATED by code, four sent back.
P6 The shader (512x512 LUT, 6 bits/channel, two piped layers) + the two procedural cues.

## FIVE DEFECTS FOUND BY CHECKING RATHER THAN TRUSTING
1. push() never called isOutage() - a transient 503 read as "the server refused your save" and
   would have permanently halted autosync. S120 taught register/signIn this and left push behind.
2. The realm FOREIGN KEY silently did not apply: CREATE TABLE IF NOT EXISTS is a no-op on an
   existing table, while apply-schema still printed "schema: OK". It now verifies the composite
   key and all five named constraints. APPLIED is not MATCHES.
3. The LUT builder and the shader quantised differently (v>>2 vs floor(v/255*63+0.5)). 48 of 103
   colours read an empty texel and passed through UNCHANGED - while the frame still lost 23% of
   its luminance, so every summary number looked healthy. Now 42.4%.
4. `worldFlags` held a DYNAMIC world key my first grep missed (ParcelScene writes setFlag with a
   name from map data), which turned P4 from a rename into a genuine two-way split.
5. An unknown realm surfaced as a raw driver exception -> HTTP 500. Now a clean 400.

## OPEN ISSUES
- CF-S121-TESTS-ARE-NEVER-TYPECHECKED (MEDIUM, PROVEN): tsconfig includes only src/**/*, so ZERO
  test files are typechecked. A deliberate type error in a test gave tsc 0 errors and PASS.
- CF-S119-SAVE-DIVERGENCE (MEDIUM): refused, not resolved. Autosync latches OFF on a diverged
  boot so it cannot clobber; the player still has no way to choose. ~60 lines.
- world.dropped has NO decay and grows forever - fine single-player, unbounded once shared.
- The constitution is STALE: blocks the /handoff CLOSE gate until re-stamped (GOV-28.1).

## BLOCKED ON HIM
- CF-S107-KEY-EXPOSURE - HIGH, open since S107.
- `www.` 404 - one zone Redirect Rule, 301 to apex. NEVER a second Worker route (localStorage is
  per-origin; a route would FORK the save). Neither vault token can do it. He said "late on".
- CF-S115-SUBMODULE-STATE-SHADOW - HIGH, deleting inside a submodule needs his go.

## NEXT STEPS
See boot-snapshot.md - it is the single file the next session should read.
Immediate: typecheck the test suites | the divergence CHOICE ui | ambient day/night music.
Deferred with reasons: shared contended world state, Durable Objects.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 6/6 complete | MCV hard_fail=0
P0 server on eeb1c1f | P1 realm_id+autosync | P3 world clock | P4 v8->v9 split |
P5 night inks | P6 shader + cues. Carry-forwards CLOSED on evidence: CF-S117-DEPLOY-HANDLERS,
CF-S119-AUTHGATE-WIRING, CF-S119-WORKER-SHELL-UNPROVEN, CF-S119-CREDENTIAL-IN-SAVE-BLOB,
CF-S121-NIGHT-SHADER, CF-S121-DUSK-DAWN-CUE.

## REFLEXION (17 entries this session - full text in .claude/reflexion_log.md)
Headline: #six-research-tracks-reasoned-about-a-server-that-was-switched-off -
#it-looked-like-it-worked-and-half-the-palette-was-untouched -
#i-measured-a-fade-and-nearly-reported-it-as-the-feature -
#the-optimiser-satisfied-the-metric-and-produced-green-skin -
#the-blocker-i-carried-for-a-session-did-not-exist

## CARRY-FORWARD PRIORITIES
None incomplete. All 6 priorities closed. Open items are carry-forwards, listed above.
===============================================================
