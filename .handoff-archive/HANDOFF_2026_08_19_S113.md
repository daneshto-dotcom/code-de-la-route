═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-19
Session: S113 — THE PACK, THE VERBS AND THE LOCK (4 priorities)
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ `fdc148b` · submodule `master` @ `1fe6f02` — **both PUSHED, 0 unpushed**
- Stack: TypeScript + Phaser 3.90 + esbuild; art generated by Python into a 174-tile atlas
- Game: `Game/founding-realm/rebuild` — GBC Crystal-style rebuild, 320×288, 16px metatiles

## CURRENT STATE
- Build: **PASSING** — `tsc --noEmit` clean, esbuild builds
- Guards: **6/6 PASS** (boundary, palette, reachability, realm, evidence, bundle)
- evidence-check self-test: **12/12 planted violations refused**
- `verify-session-claims`: **hard_fail=0 warn=0 → exit 0**, 23 modified files, **0 UNBOUND**
- gitleaks: **no leaks** across 553 commits
- Context at close: 645K / 1M (64% YELLOW)

## SESSION COST
- Model routing data unavailable (`~/.claude/session-model-counts.tmp` absent)
- Output tokens this session: ~819K. Cumulative log: `~/.claude/usage-log.csv`

## THIS SESSION'S WORK

**P1 — PHASE B: the pack, two hands, the doll** (`a62f22d`, fix `b60f2d1`)
`I`/`TAB` opens a subscreen over a PAUSED parcel: 3×3 grid, left/right hands, and the traveller drawn
between them holding what you assign. `1`/`2` equip. Save **v3→v4**: `carried: string[]` →
`carry:{hands,pack[9],active}`, with a v3 migration that lands an in-flight bottle in the right hand.
Scene model chosen by MEASUREMENT: pause takes the parcel 13 update-steps/200ms → 0, resume restores
exactly 13, a paused scene still renders and receives no keys. Zero new atlas art (the orphan guard
forbids UI tiles) — chrome is Graphics on the DECLARED UI ramp, doll is one appended `player.png`
frame, items are composited atlas frames. Closed two latent traps: `frameFor()` read a hardcoded
frame order while `player.json` sat dead, and `build-player.py` was missing from prebuild.
Follow-up fix: the pack is now refused while a warp is already in flight (a `scene.start` was pending).

**P2 — BACKLOG #30: the cutscene skips on the first viewing** (`9c8d57c`)
Reverses the logged unanimous S110 gate, stated in `Cinematic.ts` and treated as PERMANENT. The
three-line version was unsafe by two independent routes: a HELD `ENTER` carries through `enterRealm`'s
600ms fade into a scene that suppresses no auto-repeat; and the autoplay-blocked "PRESS ENTER TO BEGIN"
prompt registers its handler a microtask AFTER a `create()`-bound skip, so ENTER would have skipped the
film it offered to start (**live on rewatch today**). Both die to one mechanism: the skip is armed off
the video's `playing` event plus a 2s grace, never by `create()`, with a stall backstop held while the
BEGIN prompt is up.

**P3 — PHASE C: the verb engine** (`bd2834c`)
Properties, not verbs. `Items.ts` carries qualities; verbs read them. THREE LAWS: a vessel takes the
nature of what fills it; what is thrown travels until something stops it (Bresenham, Manhattan ≤4);
what breaks releases what it held. Hold `1`/`2` 220ms → drawn sight, arrows aim, release throws, sight
on your own tile cancels. FILL is at the water basin because **the survey chose it** (MED-S109-WALK
3:52). A sixth `yields` placement route lets the orphan guard own verb-produced tiles without losing
teeth. One new tile: `bottle_water`.

**P4 — PHASE D: the lock turns** (`7621a02`)
**RESCOPED ON EVIDENCE.** The plan said "the key opens the two locked doors"; neither can be. §11.3 is
an owner ruling that BLD-2 stays closed; `FEA-STORAGE-HOUSES` records STO-1's "dimensions still want a
tape measure", so a room behind it would be invention. Delivered: an iron key found LAST behind the
cellar bottles, `Open-Key` computed at look-time from what you carry, the lock turning into a durable
worldFlag that survives a reload, and the door opening onto DARKNESS (the `cel_dark` precedent). The
smith's door stopped saying "Key Needed" — an option that can never succeed is worse than none.
Two new tiles: `key_iron`, `sto_door_open`.

**Close-out work**
- Coherence sweep (`f3e3114`): removed map data contradicting the code (`inert:true` + static
  `disabled` on a door whose lock now turns); dead exports.
- **MCV reconciliation** (`76d6481`): the close-gate caught 27 hard failures. **20 were mine** —
  `verification[]` written as prose, binding nothing, plus 4 modified files bound by no assertion.
  Replaced with **76 typed assertions**, 0 UNBOUND. Of 3 historical failures, one (S108's "Storage
  house locked - need key") I had genuinely broken by rewriting a desc — **fixed by restoring the
  phrase**, not by retiring the assertion.
- **Tier correction** (`50af38a`): FILL and PLACE were both on the WRONG tier vs the recorded input
  model. Moving them killed `placeItem` and `placeAt` outright — they existed to serve the drift.

## OPEN ISSUES
- **`CF-S107-KEY-EXPOSURE` — rotate the `.env` keys.** Open since S107. HIGH.
- **The gate's lower half: open bars or solid panel?** Record never says; the film is internally
  inconsistent. One photograph settles it.
- **Sketchbook API is still dead code** (0 callers) — `SaveStore.addSketch`/`annotate`.
- **No drop-from-pack verb.** With all 11 slots full and a container in front, Take draws dim.
  Recoverable (place from a hand onto the ground) so NOT a soft-lock, but the pack is its obvious home.
- `bottle_empty` / `bottle_water` / `key_iron` are all PROVISIONAL art, first in the queue to redraw.
- Verb-menu ceiling past ~8 entries recorded, not solved (menus currently cap at 4).

## BLOCKED ON
- **Daniel: where the key really lives** — his recorded call ("make it hard to find later or make it
  into a quest"). Currently provisional at the back of the cellar's west ledge.
- **Daniel: a tape measure for STO-1** — unblocks a real interior behind the door that now opens.
- **Daniel: Council on CF-S109-WORLDBUILD-SYSTEM** — he sequenced it after Part 3; Part 3 closed S110.
- **Daniel: backlog #29, the film** — parked on his call; direction is stills, not video.

## NEXT STEPS (priority order)
**Immediate**
1. **CF-S109-WORLDBUILD-SYSTEM** — tile inspector + tile library. Oldest open item, Council-sequenced,
   needs a PDR. `tools/grid.py` is the started piece.
2. Move the key to wherever Daniel wants it (one-line change to `holds` in `build-cellar.py`).

**Short-term**
3. A drop-from-pack verb, closing the last "take can be refused" corner.
4. Wake the sketchbook on first pickup (Gemini's S112 proposal) or delete it.

**Medium-term**
5. STO-1's interior once measured; backlog #29's frame-by-frame film.

## CHANGED FILES
```
 rebuild/.gitignore                       |    5 +
 rebuild/package.json                     |    2 +-
 rebuild/src/art/atlas.json|.png          | (172→174 tiles)
 rebuild/src/art/player.json|.png         | (8→9 frames)
 rebuild/src/art/evidence-usage.json      | regenerated
 rebuild/src/core/Cinematic.ts            |  109 ++++--
 rebuild/src/core/Interact.ts             |   55 +++-
 rebuild/src/core/Items.ts                |  116 ++++++ (new)
 rebuild/src/core/ParcelScene.ts          |  600 ++++++++--
 rebuild/src/core/SaveStore.ts            |  330 ++++--
 rebuild/src/core/Subscreen.ts            |  430 +++++++ (new)
 rebuild/src/main.ts                      |   12 +-
 rebuild/src/parcels/{zone1,bld1cellar}/map.json | regenerated
 rebuild/tools/build-atlas.py             |  180 +++
 rebuild/tools/build-cellar.py            |   18 +-
 rebuild/tools/build-player.py            |   48 +
 rebuild/tools/build-zone1.py             |   75 ++-
 rebuild/tools/evidence-check.ts          |   12 +-
 BACKLOG.md                               |    6 +-
```

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **4/5 complete** | context 645K/1M (YELLOW)
- P1 PHASE B — completed — `a62f22d` (+`b60f2d1`)
- P2 BACKLOG #30 — completed — `9c8d57c`
- P3 PHASE C — completed — `bd2834c`
- P4 PHASE D — completed — `7621a02`
- P5 CF-S109-WORLDBUILD-SYSTEM — **pending** (Council-sequenced design work, deliberately untouched)

## REFLEXION ENTRIES (this session)
10 entries appended to `.claude/reflexion_log.md` (48 total, under the 50 cap). Headline four:
- **#harness-lies-before-code-does** — Phaser's TweenManager ignores `step()`'s delta and
  `sys.sceneUpdate` is cached at boot. A harness that lies is worse than none.
- **#baseline-before-finding** — I reported a result from a counter that had never incremented.
- **#prose-is-not-verification** — 20 accurate sentences that bound to nothing on disk.
- **#implement-the-recorded-model-not-the-convenient-one** — FILL and PLACE drifted onto the wrong
  tier, each by a locally reasonable choice nobody actually decided.

## CARRY-FORWARD PRIORITIES
1. **CF-S109-WORLDBUILD-SYSTEM** — tile inspector + library. Not started; Council-sequenced. PDR needed.
2. **CF-S110-FILM-REROLLS** — four weak shots + the gate question. Parked with backlog #29.
3. **CF-S113-MCV-SUPERSEDED** — two historical assertions superseded tonight, logged not dropped.
4. **CF-S107-KEY-EXPOSURE** — rotate `.env` keys. Open since S107.

═══════════════════════════════════════════════════════════
