═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-12
Session: S105 — the 7 videos mined; ZONE-1 surveyed; the realm database built.
2/2 priorities shipped.
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Git: parent `main` @ **5e5da6a** · submodule `master` @ **f9e2582** · plan (Founder DNA) `main` @ **db4aeec** — all pushed
- ⚠ A close-chain commit is still PENDING (see BLOCKED ON).
- Stack: TypeScript + Phaser 3.90 + esbuild + `node:sqlite`. Node v24.14.0.

## CURRENT STATE
- Build **PASSING**: `npx tsc --noEmit` exit 0; `npm run build` green end-to-end.
- Guards: boundary **PASS** (5 files) · palette **PASS** (47 tiles, 182 regions, 8/8
  background palettes) · **realm PASS** (110 records) — the realm guard is new this session.
- MCV: **exit 0**, hard_fail=0, warn=0, 16 watch-root files modified, **0 UNBOUND**.
- Gitleaks: **282 commits scanned, no leaks.**
- Deployment: none. Site intentionally dark.

## THE WORKING AGREEMENT (constitutional, ACTIVE_PLAN §11.0) — READ FIRST
Daniel directs EVERY step. Do exactly what he says, how he said it, at the highest
quality. You MAY offer archived assets (10 prebuilt NPCs + voices, story chains,
items, systems) when a stage calls for them — **but NEVER add them without his
explicit go.** Offering is not permission.

## THIS SESSION'S WORK

**P1 — ZONE-1 survey from the 7 videos.** 119.4 s of footage → 358 frames at 3 fps,
21 contact sheets, 20 full-res architectural keyframes, all read.
- **The cellar is the find.** Under BLD-1: a rubble-limestone **barrel vault**
  (*voûte en berceau*), springing ~1.1 m, crown ~2.3 m, reached by six worn treads;
  entrance a **segmental brick arch in a diagonal saw-tooth band** under a **massive
  oak lintel**. The only interior in ZONE-1 whose geometry we have, and period fabric
  that barely moves between 1601 and 2026.
- **The gate is documented SHUT and OPEN** → it can be animated from evidence. Its
  ironwork is **pale weathered grey-green `#918C83`**; two earlier readings (black,
  then white) came from the backlit clip and are refuted by the front-lit one.
- **The 553 m² grass field**, previously invisible under tree cover, is **mown and
  open** — usable ground, not scrub.
- **BLD-1's interior yields two pieces of 1601 outright**: a *plafond à la française*
  and a **massive single-slab stone chimneypiece**.
- The whole 2026 dressing list for §11.2 is now **photographed, not imagined**.
- **BLD-2's two high square gable openings are *jours*** — smoke/heat vents — which
  corroborate the smithy identification from the architecture alone.
- Survey → `rebuild/docs/SITE-SURVEY-zone1.md`.

**P2 — the realm database** (`rebuild/realm/`, **110 records, 247 edges**). Five
layers, each citing the one below: media → observation → entity → decision → artifact.
`npm run realm:why <ID>` walks any fact down to a video file and a timestamp. JSON in
git is the source of truth; SQLite (built-in `node:sqlite`, **zero new deps**) is a
derived index. `tools/realm-check.ts` is wired into `npm run check` and is **proven to
block**: 7 deliberate violations → 10 errors, exit 1.

**Owner rulings that changed the plan (ACTIVE_PLAN §12):**
- Cadastral building numbers **DROPPED**. Ours now: `ZONE-1`, `BLD-1` guardian's
  house, `BLD-2` smithy. Append-only, never re-pointed.
- The database + vertical memory are a **first-class deliverable**.
- The **2026 AI-stack verdict** (`DEC-2026-STACK`): NOW the DB, graph, layered memory,
  evidence/inference split, guardrails. NEXT a **stateless MCP server** over the DB.
  LATER agentic NPCs, observability, gateway. **NOT NEEDED: fine-tuning, distillation,
  synthetic data** (hostile to the doctrine). **HELD BACK: RAG/vector** — at 110
  records SQL+FTS wins; trigger is ~1000 records, not a date.

## OPEN ISSUES
- **Gate anachronism, flagged once and CLOSED as owner-directed.** Both Council models
  independently called the 1783 gate a real error for 1601. Daniel's ruling stands; the
  dissent and the defensible 1601 alternative are recorded in `DEC-GATE-1783`.
  **Do not re-litigate.**
- **`BLD-3` (the pavilion) is PROVISIONAL** — needs Daniel's name and number.
- 4 materials + BLD-3 carry `needs_resample: true`.
- `.gitleaksignore` allowlist — no longer needed, gitleaks is clean.
- Stale `.claude/session-state.json.lockdir.zombie.*` dirs in both repos. Gitignored,
  harmless; the destructive-command guardrail correctly blocks automated removal.

## BLOCKED ON
- **A close-chain commit is PENDING operator confirmation** (GOV-28 human-gates it).
  Uncommitted: `.claude/session-state.json` (S105 state, CHECK, ANALYZE),
  `.claude/verify-watch-roots.json` (+1 ignore glob, rationale stated),
  `rebuild/docs/SITE-SURVEY-zone1.md` (needs_resample count fix),
  `rebuild/tools/realm-check.ts` (R3 geometry arity + R14 message, from CHECK).
  Gitleaks already run and clean.
- **The old smithy access** — in NONE of the seven clips. Daniel is sending it.
- **`IMG_8865`** missing from the sequence 8861→8864, gap, 8866→8867.
- `REAL_FESTIVAL_DATE` (§9.10 RISK-1) and the Cloudflare mail question — still open.

## NEXT STEPS (priority order)
1. **Confirm the pending close commit + push.**
2. **Await Daniel's instruction.** His stated forecourt sequence (§11.6): skin as 2026
   → guardsman on the RIGHT of the gate → class conversation → cinematic → 1601 swap.
3. **Ask him:** what BLD-3 is and its number; the **1783 gate plans**; and a tape
   measure for the cellar vault, its oak lintel and the 2.1 m boundary wall.
4. **Redraw the gate** from the real ironwork on his go — `FEA-GATE-MAIN` now holds the
   full form description and both positions.
5. Parcel-002 (the bell) is still `← NEXT` in §7, but §11/§12 may re-order it. **His call.**

## DON'T GET THIS WRONG
- The road is OFF-LIMITS and the forecourt's real edge IS the road.
- The gate + all estate houses are unchanged since 1783 → identical in both states.
  Only the modern world moves.
- Smithy CLOSED (craft by delivery at the wall door). Guardian's house enterable only
  with permission from the guardian AND his wife.
- **`palette.ts` was deliberately NOT touched.** Measured colour has exactly one home
  (the database) — S104 proved duplicated palettes drift within minutes.
- **Write session state to the PROJECT ROOT `.claude/session-state.json`**, not the
  submodule's. S105 got this wrong and only the MCV hook caught it.

## CHANGED FILES
`rebuild/realm/` (8 new: README + 7 record files) · `rebuild/tools/realm-{check,build,why}.ts`
· `rebuild/docs/SITE-SURVEY-zone1.md` · `rebuild/package.json` · `rebuild/.gitignore`
· `.claude/session-state.json` · `.claude/verify-watch-roots.json`
· Founder DNA: `BRAIN/architecture/ACTIVE_PLAN_realm_rebuild.md` (§12 new, §11.5 updated)

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **2/2 complete**
- P1 ZONE-1 survey + 25 measured materials — completed — `71a82cb`
- P2 the realm database + blocking guard — completed — `f9e2582`
- CHECK: Triumvirate. RALPH 6/6 green. GROK-ANALYST: 5 defects → **2 real and fixed**
  (geometry arity; decided_by extensibility), **3 refuted by empirical test**.
- MCV **exit 0** (0 UNBOUND). Gitleaks clean. ANALYZE complete.

## REFLEXION ENTRIES (8, in session-state `reflexion_entries_to_archive`)
- P1 #pattern-draw-the-box-dont-infer-it
- P1 #pattern-dominant-returns-the-background-for-thin-things
- P1 #pattern-backlight-is-not-colour-evidence
- P2 #pattern-a-guard-that-never-failed-is-not-a-guard
- P2 #pattern-my-own-tooling-corrupted-the-data-i-was-protecting
- P2 #pattern-resolve-council-splits-on-a-checkable-fact
- SESSION #pattern-refuse-to-duplicate-even-when-it-is-the-obvious-move
- SESSION #s105-meta

## CARRY-FORWARD
- Hand CHECK reviewers the **source file**, not a prose summary — 3 of 5 reported
  defects this session were artefacts of the summary.
- Always pass `encoding='utf-8'` explicitly to python file IO on this machine.
- Consider a realm-check rule rejecting the cp1252 mojibake signature in record files
  — the one corruption class the guard missed.

═══════════════════════════════════════════════════════════


═══════════════════════════════════════════════════════════
S105 LATE-SESSION UPDATE — read this over the sections above
═══════════════════════════════════════════════════════════

## THE LOVE-IT GATE IS NOW CONSTITUTIONAL (ACTIVE_PLAN §13.1)
**Every session must ship something VISIBLE in the game or the project, and show
it to Daniel for approval BEFORE closing.** S105 failed this at first pass — two
priorities of survey and database with zero art — and was corrected inside the
session on his instruction: *"i want small improvements EVERY session!"*
Plan the visible change from the start. Do not repeat that mistake.

## BLD-3 NEVER EXISTED — ZONE-1 HAS TWO BUILDINGS
I recorded a third building, "the pavilion", from footage of a steeply-hipped
roof with an apex finial. **It is the hipped END of BLD-1 seen nearly end-on** —
the roof is continuous. A hipped end viewed end-on is geometrically
indistinguishable from a pyramidal pavilion in one frame, and I compounded it by
attaching those photos to a real but unphotographed cadastral footprint
(buildings.geojson idx 147, 5.7 × 5.8 m) that S104 had measured 11 m north.
BLD-3 is deleted, its features re-parented onto BLD-1. See `DEC-TWO-BUILDINGS-ONLY`.

Also corrected, all on Daniel's word:
- **BLD-2 the smithy now has a RED CORRUGATED METAL roof.** The red plane behind
  BLD-1 in the footage is the smithy's own modern roof, not a lean-to.
- **The red-roofed house is a NEIGHBOUR, outside the property.** Never estate
  fabric, never enterable.
- **The small iron gate is BLD-1's GARDEN gate, and it opens onto the forecourt** —
  so the guardian's garden is reachable from the player's starting ground.

## P3 — THE CORRECTIONS WENT INTO THE WORLD (approved)
- **Inverted hipped roof FIXED.** Daniel spotted it: *"the triangles of the roof
  are pointing th wrong way."* `draw_roof_hip` had the branches reversed, making
  the hip full-width at the ridge and vanishing at the eaves. A hip widens as it
  descends; the cut-away corner belongs at the top.
- **ROOF re-anchored to the measured `#845F59`**; the stills pass had run salmon.
- **RENDER re-anchored to `#86867A`, as the SHADE step** — a first attempt put it
  as the base and the wall read as grey concrete.
- **Red-brick dentil cornice drawn** as 3 courses with proud alternating dentils,
  in the existing BRICK palette so it costs no palette slot.
- Palette guard still PASS at 8/8. Verified live at localhost:20694, zero console
  errors. **Daniel: "good i see it looks better now."**

## NEXT SESSION — DANIEL'S ORDER (ACTIVE_PLAN §13.2)
1. **Enlarge ZONE-1 and let the player WALK THROUGH THE GATE** and explore the
   whole starting zone with everything in it.
   **EXPLICITLY DEFERRED: no tutorial, no time-travel cinematic, no guardsman, no
   class conversation, no festival ticket.** §11.6's forecourt sequence is
   postponed, not cancelled. **No play mechanics.** Walking and looking only.
2. Then the interiors and gardens: inside the guardian's house, its **attic** and
   its **cellar**; the **garden in front of it**; the **smithy**; everything else
   standing in the zone.
3. Then simple mechanics. Step by step.

## STATE AT CLOSE
- 3/3 priorities complete. MCV **exit 0** (0 UNBOUND, 21 files bound). Gitleaks
  **no leaks**. All guards PASS, tsc 0, realm **114 records / 260 edges**.
- Preview server stopped, port released.
- **A close-chain commit is still PENDING operator confirmation.**
