═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-14
Session: S107 — hygiene settled, provenance made enforceable, four owner layout corrections
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ **9ee7f52** · submodule `master` @ **cff52d0** — both PUSHED, 0 ahead / 0 behind
- Stack: TypeScript + Phaser 3 + esbuild · Python tile/map generators · SQLite via `node:sqlite` (zero deps)
- Game lives in `Game/founding-realm/rebuild/` (submodule, PRIVATE)

## CURRENT STATE
- **Six blocking guards, all PASS**: boundary · palette (8/8) · reach (335/335 walkable, 22 landmarks) · realm (126 records) · **evidence (NEW)** · **bundle (NEW)**
- `tsc --noEmit` **0 errors** · evidence self-test **11/11** planted violations refused
- **MCV: 33 assertions, 0 UNBOUND, hard_fail=0, exit 0**
- Zone: 84 tiles, 132 claims, 1340 m² walkable · preview RUNNING on 20694

## SESSION COST
Grok 3 calls (~$0.12) · Gemini 2 calls (~$0.03). Context at close ~630K/1M (63% YELLOW).

## THIS SESSION'S WORK

**P1 — Hygiene.** Pushed 5+1 commits (submodule first, so the parent pointer never referenced an absent object). Settled `CF-S106-GITLEAKS-GATEWAY` as a **verified false positive**: the flagged "secret" is the model id `claude-haiku-4-5-20251001`; the key comes from `process.env`. The exemption is three constraints ANDed (rule + path + secret-shape), with AND-enforcement proven empirically and three negative controls. Swept 20 dead-session artifacts after a dry run caught three live ones a glob would have destroyed.

**P2 — Provenance enforcement.** `tile()` now takes claims as a **required parameter** — omission is a TypeError, so "forgot to annotate" is not a possible state. It immediately caught five dynamically-named tiles (`arc_*`) a regex sweep missed. Grades measured/observed/subtracted/craft, per claim not per tile, mapped onto the existing `evidence_class`. Fifth guard `evidence-check.ts` ships **proven to block**. Staleness made structural: `realm.db` was silently 12 records behind, now carries a source manifest and consumers refuse to run stale. `cel_void` DELETED. `provenance-report.html` puts every tile beside its evidence.

**P3/P4 — Four owner layout corrections.** house | FIELD | WALL | smithy, with the 553 m² field opened and walkable; the 60 m² garden built inside the little gate; the complex moved 6 m north; the invented garden walls struck; the road frontage redrawn as **railing on a plinth** (new tile, no palette cost). **The database already contained three of the four corrections in plain prose.**

**P5 — Sixth guard.** I shipped a scrambled zone: inserting a tile mid-atlas renumbered everything after it, and the 35-minute-old bundle drew the new sheet with the old index. Five guards were green. `bundle-check.ts` now fails when the bundle is older than its inputs.

## OPEN ISSUES
- **`CF-S107-OBSERVED-GRADE`** — 88 of 132 claims are `observed`, asserting only that a record exists. Both CHECK reviewers called it the path of least resistance. Grok's fix (require the most specific leaf record) is mechanisable and unbuilt.
- **`CF-S107-PALETTE-BACKFILL`** — 9 of 11 palettes have empty `derivedFrom`, so the colour check reaches only ROOF and RENDER. The guard now prints this limit on every run rather than staying quiet about it.
- **`CF-S107-RECORD-GAPS`** — gate postern + smith's door have no record; enumerated in `KNOWN_RECORD_GAPS`.
- The MCP server over the realm DB was **deferred**, not built — it serves our workflow, not the game, and §13.2 says finish the zone first. Its precondition (the stale index) is fixed, so it is now a small safe addition.

## BLOCKED ON (owner decisions — see boot-snapshot.md for full text)
1. **ROTATE** the Anthropic + GCP keys in `Game/founding-realm/.env` (`CF-S107-KEY-EXPOSURE`). Transcript exposure only; **nothing was ever committed**, verified four ways.
2. **`CF-S107-PUBLIC-PAGES`** — the public parent repo serves `.handoff-archive/` and `.claude/` at the Pages URL; the plan records the project as dark and never mentions Pages.
3. **`CF-S107-IRON-PALETTE-REFUTED`** — the gate is drawn from a measurement the database calls refuted.
4. **`CF-S106-OFFERED-NOT-BUILT`** — well · mounting block · wheel-guard stones · bread oven · water tank · chimney smoke · the 1783 gate redraw.
5. Still wanted: **a tape measure** (4 sessions running) · old smithy access · `IMG_8865` · **the 1783 gate plans** · BLD-3's name.

## NEXT STEPS
1. **Ask what to correct next** — four corrections came in two rounds; assume more. Do not guess.
2. **Interiors (§13.2 step 2)** — BLD-1's ground floor is FOUR rooms, one shared chimney, attic, bathroom. Smithy interior stays CLOSED.
3. **Simple mechanics (§13.2 step 3)** — only after the zone is right.
4. Optional, cheap: the deferred MCP server; the `observed`-grade tightening.

## CHANGED FILES
28 files across the session. New: `tools/evidence-check.ts`, `evidence-check.test.ts`, `realm-manifest.ts`, `provenance-report.ts`, `bundle-check.ts`, `.gitleaks.toml` ×2, `evidence-usage.json`, `provenance-report.html`. Modified: `build-atlas.py`, `build-zone1.py`, `build-cellar.py`, `realm-build.ts`, `palette.ts`, `package.json`, both `map.json`, atlas art.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **5/5 complete** | ~630K/1M (YELLOW)
- P1 Hygiene — completed — parent 58f0eb2 / submodule 3f1b17f
- P2 Provenance enforcement — completed — parent a9e421d / submodule ed653f1
- P3 Layout correction — completed — parent fe31887 / submodule e36e02b
- P4 Frontage + garden walls — completed — parent 2d1ef56 / submodule 0a1c7cb
- P5 Bundle guard — completed — parent 9ee7f52 / submodule cff52d0

## REFLEXION ENTRIES (this session)
21 entries appended to `.claude/reflexion_log.md`. The load-bearing ones:
- **The guard caught its own author within minutes** — I invented 11 record ids while writing the anti-citation-theatre guard; it refused all 11 on first run.
- **Measure before choosing a threshold, and be willing to find none** — SSIM and my own hex-in-palette fallback both died on real data (median 27.5, nearest step often an unrelated palette). Shipping no number was the honest result.
- **A null test is not a passing test** — three controls this session could not have failed.
- **I verified the data and showed a render and called it done** — the PNG came from the same sources the guards check, so it could only confirm what they already said.
- **The records held the correction three times** — the clearest structural gap left in the project.

## CARRY-FORWARD PRIORITIES
None incomplete. 8 carry-forwards are all **owner decisions**, not unfinished work.

═══════════════════════════════════════════════════════════
