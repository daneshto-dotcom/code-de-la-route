# HANDOFF — S106, 2026-08-13
## ZONE-1 enlarged and open · the cellar · four owner layout corrections

**Parent** `main` @ `27ed451` · **submodule** `master` @ `4f71a1e` — both committed,
**push NOT yet done** (operator-confirmed).

---

## READ FIRST — THREE CONSTITUTIONAL RULES

1. **THE WORKING AGREEMENT (§11.0).** Daniel directs every step. You may OFFER
   archived assets; you never add them without his explicit go.
2. **THE LOVE-IT GATE (§13.1).** Every session ships something visible and shows
   it to him before closing. **S106 shipped and showed four times — and the gate
   is still NOT satisfied.** His closing words: *"there are a lot of corrections
   and stuff we still need to work on!"*
3. **SUBTRACT, DON'T INVENT (`DEC-1601-METHOD`).** S106 broke this once, in the
   cellar, and he caught it in one look. See "How I got it wrong" below.

---

## WHERE THE WORK IS

**`http://localhost:20694`** — `npm run dev` in `Game/founding-realm/rebuild`
(or `preview_start` with the `rebuild` launch config, port 20694). Arrows or WASD.

Two scenes now: **`zone-1`** (28×32, scrolling) and **`bld1-cellar`** (20×18).
Walk into the brick arch behind the guardian's house to go underground.

---

## WHAT S106 SHIPPED

- **ZONE-1: one 20×18 screen → a 28×32 scrolling map** (56 × 64 m). Walkable
  ground **~240 m² → ~1044 m²**. Camera bounds + follow + roundPixels, viewport
  held at **exactly 320×288** — it is the 20×18 window that makes this a Game Boy.
- **THE GATE IS OPEN**, and so is the little garden gate. That was the brief.
- **A third `canopy` layer above the player** — tree crowns overhang the drive and
  you walk under them. Collision never consults it.
- **The first interior: the cellar.** Data-declared warps with a 180 ms fade,
  validated across maps. 1 m per tile indoors (2 m outdoors) — Pokémon does the
  same and it is invisible in play.
- **`tools/reach-check.ts` — a new blocking guard.** Flood-fills from the start
  tile and asserts landmarks **the generator itself declares**, so intent and test
  move in the same edit. **It caught five real defects no render inspection found.**
- **The map is GENERATED** (`tools/build-zone1.py`) from named regions, not hand-typed
  ASCII. This is the load-bearing decision of the session: it made four owner-driven
  layout rewrites routine instead of terrifying.
- **`tools/render-views.py`** renders the true 320×288 player viewport for the LOVE
  gate. Its camera maths independently reproduced the live engine's scroll (72,224).
- **palette-check no longer hard-codes one parcel** (a silent-guard gap found by A.0).
- **SaveStore v1→v2** with a real migration off the retired `parcel-001` id.
- **P4: the narrated video mined** — 126 realm records; `npm run realm:why` walks any
  fact to a timestamp in his speech.

---

## DANIEL'S FOUR CORRECTIONS — ALL APPLIED

1. **BLD-2 the smithy is WEST and BEHIND BLD-1**, sharing its grass and its wall.
2. **Turned upright (N–S)** it is 6 × 11 metatiles = **12.6 × 22.2 m, its real
   measured footprint** — correct for the first time since S104.
3. **The estate wall wraps BLD-1 in an upside-down L**, with a **passage ringing the
   house**: in at the small gate, along the front, down the west flank, along the
   back past the cellar mouth to the storage houses.
4. **Behind the houses is WALL, not open space.** The 553 m² of grass is beyond it.
   The **shared grassland runs N–S down the EAST**, shaped to his orthophoto mask —
   including the **waist**, which is the best thing in the shape.

`DEC-S106-ZONE1-LAYOUT` records the ruling and *why the traced coordinates lost*.

---

## HOW I GOT IT WRONG (read these before building)

- **I invented architecture out of a fact about the camera.** The cellar's far end
  is never lit in any footage, so I drew a dark tunnel running on into blackness.
  Daniel: *"what is this weird dark zone you added above the cellar. there is
  nothing above, you should only see the cellar itself."* The room is now a true
  half-cylinder. `cel_void` is deleted **and an assertion keeps it deleted**.
- **I trusted numbers over prose.** Two records said the smithy was behind the house
  (`OBS-GRASS-FIELD`, `OBS-BOUNDARY-WALL`) and I read past both, weighting
  hand-traced coordinates whose own note says APPROXIMATE. **Check an evidence
  item's stated confidence before letting it outrank a sentence.**
- **I repeated S105's recorded mistake verbatim.** Its reflexion says *"describe the
  old behaviour, do not paste it"* — then I quoted a removed line inside the comment
  explaining its removal, breaking my own `file_lacks` assertion.
- **I declared a governance gap uncurable without reading the checker.** MCV named a
  cure script that does not exist; the *verifier* was present all along and documents
  its own schema. **When a cure is missing, go read the diagnostic.**

---

## STATE AT CLOSE

- **P1** complete · **P2** complete *as re-scoped* (doorway system + cellar; the rest
  carved out to **P5**) · **P3** NOT STARTED (correctly — mechanics wait until the
  zone is right) · **P4** complete.
- Guards: boundary **PASS** · palette **PASS** (zone1 8/8, cellar 2/8) · reach
  **PASS** (261/261 + 82/82, 18 + 6 landmarks, 4 warps) · realm **PASS** (126
  records) · `tsc` **0** · bundle built.
- **MCV: 62 assertions, 24 modified files, 0 UNBOUND, hard_fail=0, exit 0.**
- Context at close: **~470K / 1M (≈47% GREEN)**.
- **Preview server left RUNNING on 20694** so he can keep testing.

---

## S107 — START HERE

**Keep working this zone until he loves it.** He has said there is a lot still to
correct; ask him what first rather than guessing.

**Put this short list to him at the top of the session** (all offered, none authorised):

| offer | basis |
|---|---|
| the **well** (*puits*), **mounting block** (*montoir*), **wheel-guard stones** (*chasse-roues*), **bread oven** (*four à pain*) | GEMINI-AUDITOR: a 1601 court without them is architecturally incoherent. `DEC-1601-ABSENT-THINGS` rules the well "recorded, not authorised" |
| the **cement water tank** in the shared grass | his own words, t≈62 s — but no position and no dimensions |
| **smoke from BLD-1's chimney** | strongest "someone lives here" signal; needs animation |
| **redraw the main gate from the real 1783 ironwork** | `FEA-GATE-MAIN` has it shut AND open, so it can be animated from evidence. Still awaiting his go since S105 |
| **the frontage wall** as tall rendered with red tile coping | his Street View captures show this; the art draws rubble with stone coping. `CF-S106-FRONTAGE-WALL` |

**P5 is ready to build the moment he wants interiors:** BLD-1's ground floor is
**FOUR rooms** (`OBS-S106-BLD1-ROOMS`) — today's three is *modern* demolition
(*"we took them down cuz we're going to make a gym out of this place"*) — with a
heater and the main fireplace on **one shared chimney**, an attic above, and a small
bathroom. Frames f_036–f_041 give the interior, f_040 the chimneypiece, f_045 the
bathroom. **The smithy interior stays CLOSED** (§11.3).

---

## CARRY-FORWARDS (4, all logged in session-state)

- **`CF-S106-GITLEAKS-GATEWAY`** — 2 history + 1 live finding at
  `src/networking/gateway.ts:1366` in the **frozen old build**, from March 2026.
  **Not S106's**; every file touched is under `rebuild/`. `.env` and `dist/` are
  confirmed gitignored. The line reads its key from `process.env`, so it is
  **likely a false positive** — but "likely" is not "verified". **Settle it and
  allowlist with a rationale, or rotate.** An alarm that is always on is not an alarm.
- **`CF-S106-FRONTAGE-WALL`** — see the table above.
- **`CF-S106-OFFERED-NOT-BUILT`** — see the table above.
- **`CF-S106-MCV-UNBOUND`** — **RESOLVED this session**, and the resolution records
  that my first reasoning about it was wrong.

## STILL OPEN FROM EARLIER SESSIONS

- **A tape measure.** The cellar vault, its oak lintel, the 2.1 m wall and now the
  storage houses are all estimates. `FEA-BLD1-CELLAR` is `observed`, not `measured`.
- **The old smithy access** — he shows where it is (t≈75 s) but it is behind bamboo
  in every frame.
- **`IMG_8865`** still missing from the S105 sequence.
- **The 1783 gate plans** he holds — the best possible reference for the gate redraw.
- He has promised **more videos**: *"the next videos I will start letting you explore
  this and show you how it's going to look to get to the Chateau."*
