═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-26
Session: S119 — the game got an owner. Accounts that follow you, 91 characters
behind a palette seam, and the instrument that finally let a session look at it.
═══════════════════════════════════════════════════════════

## PROJECT
- Working directory: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm
- Branch: main @ aaf96b9 | submodule Game/founding-realm: master @ 0679a52 | both pushed, 0 unpushed
- Stack: TypeScript + Phaser 3 (ESM, esbuild) · Python pixel-art generators · Cloudflare Worker + Postgres
- LIVE: https://legacyoftherealm.com (apex 200, all 15 files). www. still 404.

## CURRENT STATE
- typecheck: clean
- tests: 13 + 25 + 58 + 65 + 65 + 3,155 assertions, 0 failed
- guards: 9/9 PASS · site guard PASS (15 files, nothing outside the allowlist)
- endpoints: 80/80 vs a real Docker Postgres
- CI: green on every push this session · gh issue list: empty
- save schema: v8 (was v7) · sprite sheet: 144x1456, 91 variants, 9,679 bytes

## SESSION COST
Model routing data unavailable for this session (statusline_dead; cost-tracker
reports no telemetry write). Context at close: 652,773 / 1,000,000 (65.3% YELLOW).
No external API calls — Grok 0, Gemini 0. Two research subagents died on an
individual spend limit (see OPEN ISSUES).

## THIS SESSION'S WORK

**P0 — the domain, and a 10-agent audit.** Probed rather than asked: legacyoftherealm.com
answers 200 and serves the same build as the workers.dev URL (the body diff is only
Cloudflare's bot-management injection). All 15 allowlisted files verified live. The audit
then produced, every finding adversarially re-checked: 16 false statements across
deploy-site.yml / GOING-LIVE.md / deploy/README.md (they describe Pages; the live thing is
a Worker); the vault token's `Workers Scripts: Edit` scope is CORRECT and the docs calling
it wrong-scoped are backwards — it is merely dead; `wrangler deploy` with no `routes` key
CANNOT delete the hand-attached apex route (traced through wrangler 4.125.0's own bundle);
and the www fix must be a 301, never a second Worker route, because localStorage is
per-origin and a route would silently FORK the save.

**P1 — the four endpoints** (built by the prior session, verified here): register/login/
save/load against Postgres, one character per account enforced by `save.player_id` PRIMARY KEY.

**P2 — accounts that follow you, the CLIENT half.** AuthGate wired to the endpoints with the
local hash kept as the offline fallback; auto sign-in on a live token; a resume window
offering "continue where you left off" / "start new game"; and `Restart.ts` so erasing a
world reaches the account instead of dying on one device. Proven in a browser: a fresh
browser signs in and INHERITS position, era, the iron key in hand and handedness.

**P3 — characters.** 90 variants + legacy in ONE sheet. Five inks (outline, skin, hair, two
attire colours) do not fit a 3-colour sprite — but a 16x16 frame is four 8x8 regions and the
existing art already kept hair/skin above row 8 and garment below, so no region holds both
bands. That is how GBC OBJ hardware assigned palettes. `palette-check.ts` was made STRICTER
to match (per-region, one bank). The measured "hat on a sack" flaw is fixed: legacy had TWO
silhouette widths with the cap WIDER than the shoulders; all three new builds have THREE with
shoulders strictly wider. Plus: seven stats as a pure module, SaveData v8, a six-page creation
wizard, a top-left name/class/level strip, and the Journal's fourth page as the character sheet.

**P4 — the coherence pass.** Six incoherences and five stale docs (see below).

## BUGS FOUND BY RUNNING IT (none of which reading found)
1. **`hasPosition` was the wrong question** — it means "was the save ever WRITTEN", and
   registering writes, so every brand-new player was asked whether to continue a world they
   had never entered. Added `hasPlayed`. Then I made the SAME mistake one call site over in
   main.ts, so a fresh browser looked "played" and refused to adopt the world it had just
   signed in to collect.
2. **"Start new game" only erased the device.** The account still held revision 1, so the next
   reload pulled the dead world back up. Erasing is not local once there is an account.
3. **`adoptRemote` wiped this device's offline login**, because `account` is a field of the save
   and adoption installs the blob wholesale. Invisible until the player is next offline.
4. **The world sprite did not change until you turned** — the image is created before the
   character exists and only a step or turn calls setFrame.
5. **The v7->v8 migration branch was MISSING.** Every save in existence was v7, so all of them
   would have fallen through to `fresh()` and been BLANKED. TypeScript caught six of seven
   sites; it cannot see a branch that is not there. And the suite would have PASSED, because
   test 10's version list was the literal `[1,2,3,4,5,6,BUILD_VERSION]` — which at v8 skips 7.
6. **The pack showed somebody else** — Subscreen drew frame 8, always variant 0.
7. **The creation preview came back wrong after the review page** — SEAL moves the doll to 3x
   and nothing restored it.
8. **Inserting the CHARACTER journal page before ACCOUNT hid RESTART.** He noticed in minutes.

## OPEN ISSUES
- **NOTHING WAS SEEN BY A HUMAN.** WebGL here has no preserveDrawingBuffer and `document.hidden`
  throttles RAF, so every visual claim in this handoff is a state or frame-index reading, not a
  look at pixels. This is the §13.1 debt and it is next session's first job.
- **Two research subagents died on an individual spend limit** (the data-layer study and the
  final plan synthesis). Session limit resets 02:40 Europe/Paris. Nothing was blocked — the
  migration convention was read directly — but the plan he asked for was written by me, not by
  an agent, and this handoff says so rather than implying otherwise.
- **`www.legacyoftherealm.com` is 404.** Fix is a zone Redirect Rule; a second Worker route
  would fork the save. Most dashboard labels in the click path are marked INFERRED.
- **`CF-S119-CREDENTIAL-IN-SAVE-BLOB`** — `push()` sends the whole save and `account` (username,
  salt, PBKDF2 hash) is a field of it, so the hash lands in `save.data` JSONB as well as
  `player.password_hash`. Not a plaintext leak; a credential in a column with no reason to hold
  one. Needs its own approval.
- **`CF-S119-SAVE-DIVERGENCE`** — when the account's world and a PLAYED local world differ,
  main.ts keeps local and records the revision. Non-destructive, but the player is never asked.
- **`CF-S119-WORKER-SHELL-UNPROVEN`** — `src/server/worker.ts` has still never executed.
- **`CF-S118-DEPLOY-DOC-SAYS-PAGES`** — mapped in full this session, NOT fixed.
- **`CF-S107-KEY-EXPOSURE`** — HIGH, open since S107.
- **CF-S114-MCV-VERSION-ASSERTION-CLASS fired SIX times tonight** — four historical needles and
  two of my own, one of them a `file_lacks` on a literal my own comment quotes. Rebound each
  time rather than reverting working code, but the pattern is the point: I keep binding to the
  shape of today's code instead of to the claim.

## BLOCKED ON
1. **A human looking at the game.** 2. The Neon connection string. 3. The `www.` Redirect Rule.
4. Key rotation. 5. P4 art (his call). 6. A photograph of the gate's lower half; a tape measure
for STO-1; Dael's tutorial role.

## NEXT STEPS (priority order)
**Immediate**
1. SHOW HIM THE GAME. `npm run dev:api` and play it, or open the live URL. Nothing from S119
   has been looked at.
2. Get the Neon connection string, then point the harness at it — `dev-api` already honours
   `LOTR_TEST_DATABASE_URL`, so the same 80 assertions run against the real database with no
   code change.
3. The `www.` Redirect Rule (his click, ~3 min, no DNS write).

**Short-term**
4. `CF-S119-CREDENTIAL-IN-SAVE-BLOB` — strip `account` from the pushed blob (own approval).
5. `CF-S118-DEPLOY-DOC-SAYS-PAGES` — the correct `wrangler deploy` design is written; ship it.
6. The hand-key tap fires on RELEASE — `ParcelScene.ts:1100`, ~8 lines, storm risk ruled out.

**Medium-term**
7. `CF-S117-SKETCHBOOK-VIEWER` — §9.3 calls it the reward layer; it has a save slot and no viewer.
8. `CF-S117-BOX-FIVE-OPTIONS` — `offerHandVerbs` is exactly at the 4-option ceiling, unguarded.
9. The roadmap's Phase 1 onward: https://claude.ai/code/artifact/c46b36d5-2c33-4479-a9c3-6de5f37fbbd4
   P0 stays OPEN — he is extending it part by part, by direction, not from the plan.

## CHANGED FILES (submodule, d7cf610..0679a52)
 21 files changed, ~3,300 insertions
 NEW: src/core/{Stats,Character,Creation,ResumeGate,Restart}.ts · tools/stats.test.ts
      deploy/dev-api.mjs
 MOD: src/core/{SaveStore,SaveVersion,AuthGate,RemoteSave,ParcelScene,Journal,Subscreen}.ts
      src/main.ts · src/palette.ts · index.html · tools/{build-player.py,palette-check.ts}
      tools/save-migration.test.ts · package.json · README.md · realm/README.md
      .github/workflows/rebuild-ci.yml · src/art/player.{png,json}

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 5/5 complete | context 652,773/1,000,000 (65.3% YELLOW)
- P0 domain verified + deploy/input audit — completed — 53d15b7
- P1 four auth endpoints (prior session, verified here) — completed — 30a3745
- P2 accounts that follow you, client half — completed — 30a3745 / 53d15b7
- P3 characters, stats, creation wizard, HUD, sheet — completed — 3a1c8f9 / a80691b
- P4 coherence pass + Rule 22 audit — completed — 0679a52 / aaf96b9
MCV: verify-session-claims.py exit 0, hard_fail=0, 0 UNBOUND (41 + 23 typed assertions on P3/P4)

## REFLEXION ENTRIES (this session — 11, now at the top of .claude/reflexion_log.md)
- #the-same-wrong-question-in-two-places-twice
- #erasing-is-not-a-local-act-once-there-is-an-account
- #the-only-reason-i-found-any-of-this-was-that-he-said-see-it-work-first
- #adopting-a-world-should-not-adopt-a-login
- #the-constraint-was-already-satisfied-before-i-invented-a-way-around-it
- #the-type-system-caught-six-of-seven-and-the-seventh-was-the-dangerous-one
- #the-instrument-was-the-deliverable
- #one-page-further-away-is-the-same-as-absent
- #a-duplicate-that-fails-loudly-is-a-copy-a-duplicate-that-falls-back-is-a-trap
- #i-moved-my-own-assertion-one-priority-later
- #the-count-that-named-no-true-number

## CARRY-FORWARD PRIORITIES
None incomplete — all five S119 priorities closed. The carry-forward LEDGER holds 19 items;
the five opened tonight are CF-S119-{CREDENTIAL-IN-SAVE-BLOB, SAVE-DIVERGENCE,
WORKER-SHELL-UNPROVEN, AUTHGATE-WIRING, CLOSE-GATE-CWD-SENSITIVE}.

═══════════════════════════════════════════════════════════
