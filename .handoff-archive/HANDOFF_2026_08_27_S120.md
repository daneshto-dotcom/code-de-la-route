═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm (Founding Realm rebuild)
Generated: 2026-08-27
Session: S120 — the game got LOOKED AT, deployed, and audited; then researched before multiplayer
═══════════════════════════════════════════════════════════

## PROJECT
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Branch: `main` — parent `052d299+`, submodule `f2231b1+` (both pushed, unpushed count 0/0, both remotes reachable)
- Stack: TypeScript + Phaser 3 + esbuild (ESM) · Cloudflare Worker + static assets · Neon Postgres (not yet wired)
- Live: **https://legacyoftherealm.com** (apex only — `www.` is 404)

## CURRENT STATE
- typecheck 0 · `npm test` **3,155 assertions, 0 failed** · `npm run build` **9 guards PASS** · `test:db` **80 PASS**
- Deployed 6x this session with the vault Cloudflare token; apex bundle byte-identical to the local build each time
- Database: NONE attached. All four endpoints answer `503 not-configured` on the apex; the game plays local-only.
- MCV: `hard_fail=0 warn=0`, 0 UNBOUND. 11/11 priorities completed, all `check_completed=true`.

## SESSION COST
Model routing data unavailable for this session (no `session-model-counts.tmp`). API: Grok 0 calls, Gemini 0 calls — no Council convened; deliberation was a user-path waiver, recorded in `deliberation_method` so `deliberation_completed:true` is never read as three models arguing.

## THIS SESSION'S WORK (11 priorities)
- **P1 LOOK AT S119.** The instrument already existed and had gone unused — 89 frames sat in `docs/shots/` while S119 shipped blind. 16 new frames; 5 real defects found, each confirmed twice (render + measured bounds or traced code path). Corrected boot-snapshot rule 7, which falsely claimed the game could not be photographed.
- **P2** hand tap fires on PRESS (was release). Verified: exactly 1 `useHand` call per press at 60ms/800ms/2000ms.
- **P3/P4** deployed the game; then found and fixed a **total lockout** — a 503 was classified as a refusal, so AuthGate deleted the local account it had just written and Phaser was never constructed. No new player could enter. `isOutage()` splits 5xx/`not-configured` (offline → local play) from 4xx (still refuses).
- **P5** the Journal tab strip is reachable — UP used to wrap to the bottom; three of four footers never named the page key.
- **P6/P7** the name page: one ENTER = one page (was two, so page 4 was never seen); footer names ENTER only; prefill sanitised so a username with a digit no longer causes a silent refusal; non-printable keys now read from the live keymap with ENTER/ESC kept as a floor.
- **P8** the password hash never leaves the device. Proven on real stored rows: before `has_hash=t`, after `account:null`, 1196 → 954 bytes.
- **P9** the deploy tick stops lying — a missing token now FAILS RED; `wrangler deploy` not `pages deploy`.
- **P10** arrows read `< >`; the name hint fits (was 36px off a 320px screen, cutting "to 18").
- **P11** `RESEARCH-multiplayer.md` — 317 lines, 10 sources, VERIFIED/MEASURED/INFERRED tags.
- **AUDIT** (unnumbered, at his request): 17 artifact rows against the live bundle asserting new-shape-present AND old-shape-absent; the arrival proven to play through on production where the film 404s; five faults fixed including unknown `/api` routes answering 503 instead of 404.

## OPEN ISSUES
- **`CF-S120-NOTHING-PUSHES-DURING-PLAY` (HIGH).** `push()` is called from two sites, both in `Restart.ts`. Measured with a control: player walked, local save written, ZERO `/api` calls. **The Neon string alone will NOT deliver cross-device saves** — this is the other half.
- **`CF-S120-DEV-API-LEAKS-A-POSTGRES-PER-RUN`.** Four orphaned containers at close (oldest 25h, from S119). `bye()` is correct but a Windows hard kill delivers no signal, and the container name carries the PID. Fix: reap on STARTUP. Cleaned by hand.
- **`CF-S120-WORLD-INDEX-MTIME-CHURN`** is not the simple fix it looked like — the mtimes are load-bearing for `staleness-check`. Proper fix is content hashes.
- `CF-S120-HUD-NO-PLATE-CONTRAST` (latent, not reproducible in the 2026 pen) · `CF-S117-BOX-FIVE-OPTIONS` · `Input.ts` has 4 unused exported helpers · `CF-S118-BOX-BODY-PAGING` · `CF-S118-SURFACE-GUARD-NAME-COUPLED` · `CF-S117-SKETCHBOOK-VIEWER` · the `~/.claude` trio (SYNC-BRAIN Tier 0/1, each needs its own amendment)
- **STALE, reconciled:** `CF-S117-DEPLOY-HANDLERS` was blocked on "the Vercel project" — no longer true; the game deploys to a Cloudflare Worker. Only its Neon half survives.

## BLOCKED ON
1. **Neon pooled connection string** → `legacy_realm_db.pooled` in the vault, **not into chat**. The project `legacy-of-the-realm` already exists (branch `production`, Frankfurt, PG18).
2. **The $5/mo Workers decision.** Free is 10ms CPU/invocation; PBKDF2 x100,000 measures ~29ms — login FAILS on Free once a DB exists.
3. **`www.` 404** — a zone Redirect Rule, 301 to apex. PROBED: neither vault token can create it (10000 / 7003). NEVER a second Worker route.
4. **`CF-S107-KEY-EXPOSURE`** — HIGH, open since S107.

## NEXT STEPS
**Full plans with files, pre-taken decisions, traps and tests are in
`Game/founding-realm/BACKLOG.md` → "NEXT SESSIONS — PLANNED, WITH IMPLEMENTATION NOTES". Read that, not this list.**
1. **S121-P1** close the push gap (debounce ~5s + flush on `pagehide`; `stale` must SURFACE, never clobber)
2. **S121-P2** Neon + secrets (owner-gated) — verify register 201 → login 200 → save revision → cross-device
3. **S121-P3** the Durable Object spike — must produce TWO NUMBERS, not a feature
4. **S122** presence design PDR, only after 1 and 3
5. Small: world-index content hashes · box 5-option guard · `Input.ts` orphans · the container reaper

## CHANGED FILES (this session, submodule)
`src/core/{ParcelScene,Journal,Creation,Character,RemoteSave}.ts` · `src/main.ts` · `src/server/worker.ts` · `deploy/dev-api.mjs` · `deploy/RESEARCH-multiplayer.md` (new) · `deploy/GOING-LIVE.md` · `tools/{shoot,build-site}.ts/.mjs` · `.github/workflows/deploy-site.yml` · `BACKLOG.md` · `rebuild/.gitignore`
Parent: `boot-snapshot.md` · `.claude/launch.json`

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **11/11 complete** | ~780K/1M (**ORANGE**)
P1 look-at-S119 `d1a8106` · P2 press-tap `9eba952` · P3 deploy `—` · P4 outage-split `9d4ad49` · P5 tab-strip `ad0d7eb` · P6 name-page `26d8d4f` · P7 prefill+keymap `a5b47bd` · P8 credential `38e5bae` · P9 deploy-pipeline `38e5bae` · P10 arrows+hint `38e5bae` · P11 research `62b98a9` · audit `8ea9a79`

## REFLEXION ENTRIES (this session)
29 entries appended to `.claude/reflexion_log.md` (pruned 66 → 40; S117/S118 blocks dropped, preserved in archived handoffs). Highest-signal:
- `#the-instrument-existed-and-nobody-ran-it` — a FALSE explanation for why you cannot look costs more than no explanation
- `#green-is-not-deployed` — a skip that reports success is worse than a failure, because a failure gets fixed
- `#the-error-message-said-safe-while-it-locked-the-door` — test that the CLASSIFIER can reach the fallback, not just that the fallback exists
- `#i-invented-a-vocabulary-and-called-it-verification` — 16 assertions in types the checker does not implement
- `#i-wrote-a-test-that-could-not-fail` — RIGHT then LEFT proves nothing; make the probe asymmetric and add a control
- `#i-reported-an-absence-i-had-not-actually-searched-for` — a negative result is only as wide as the query that produced it
- `#the-cleanup-step-found-the-bug-the-work-never-would`

## CARRY-FORWARD PRIORITIES
None incomplete — 11/11 closed. The four items above under BLOCKED ON are owner actions, not unfinished work.
═══════════════════════════════════════════════════════════
