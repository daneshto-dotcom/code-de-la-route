═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-15
Session: S109 — the site measured against the cadastre, Parts 1 & 2 built, ~20 owner corrections, and the tool that ends the screenshot round-trip
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ **378c540** · submodule `master` @ **3730f58** — BOTH PUSHED, 0 ahead / 0 behind, credentials verified live
- 18 commits each side. Submodule diff: **26 files, +3815/−1373**
- Stack: TypeScript + Phaser 3 + esbuild · Python tile/map generators · SQLite via `node:sqlite`
- Game lives in `Game/founding-realm/rebuild/` (submodule, PRIVATE)

## CURRENT STATE
- **Six guards PASS** · `tsc --noEmit` **0** · evidence self-test **12/12** · **MCV 0 failures, 0 UNBOUND**
- zone-1 **28 × 38** (was 28×32) · **405/405 walkable tiles reachable, 26 landmarks** · atlas **124 tiles** · 94 legend glyphs
- realm **33 features, 11 media** (was 29 / 10) · 3 warps · 2 interactables
- Context at close **810K / 1M (81% ORANGE)**. Model-routing counter absent — cost data unavailable.

## THIS SESSION'S WORK

**P1 — THE SITE SURVEY (analysis only, nothing built).** Two owner videos studied frame by
frame and transcribed. Then the part that mattered: the cadastre (`estate-map-kit/*.geojson`)
projected into a local metre frame and checked against ESRI satellite at 0.206 m/px — the
polygons land on the roofs, so the numbers stand. It produced the error as a NUMBER: BLD-2's
centre is +4.5 m EAST of BLD-1; the map had it 9.0 m WEST — **13.5 m out, sign reversed** —
while the north-south separation was right to 4%, which is why it never looked wrong. Also
found parcel 217's east boundary is ONE straight 50.3 m run at bearing 35.1° — the "45 degree
wall" Daniel had named four times and the map had nowhere. Full working: `docs/S109-SITE-TRUTH.md`,
both videos verbatim in `docs/s109/TRANSCRIPTS.md`.

**P2 — PART 1, THE GROUND.** Map extended 6 rows north. BLD-2 moved to its measured position
and given its true 6×11 metatiles, its long east flank facing the court. Court's east boundary
rebuilt as a 45°/90° stair (COURT_EAST *asserts* every step is 0 or ±1 column). The 553 m²
"field" deleted — it was an artefact of the smithy being in the wrong place. Boundary wall
corrected to its measured 3.0 m. **Y0 applied INSIDE fill/row/scatter/woodland**, so ~100 paint
calls carrying eleven rounds of S108 corrections kept their literals untouched.

**P3 — PART 2, THE FABRIC.** Three blocked openings cut into the smithy's flank. **One rule for
every locked door** — `"Door Locked - Key Needed"` / Knock / Open-Key / Leave, declared ONCE and
shared, with Open-Key drawn dim and the cursor stepping over it (verified at runtime: 0→2→0→2→0).
Two open-air log stores, the stone basin at its measured 1.0 × 1.8 m, the low wall, the bamboo.
Five new realm records so nothing traces to nothing, including the walkthrough video, hashed.

**P4 — ~20 OWNER CORRECTIONS.** House moved 2 tiles east so its door lines up with the little
gate; forge chimney (2×2); north wall behind the smithy turning west and butting the estate arm;
the smithy's east flank REMOVED at his instruction; its court turned to run-down soil and sealed;
cart ruts moved onto the road's centre line; four dirt tiles inside the gate metalled; a stranded
cart track and the dirt under the avenue crowns cleared; the entrance tree taken off the low wall
it was standing on; crowns respaced and now ASSERTED non-overlapping; the north wood's canopy
hole closed; seven bamboo tiles placed off a coordinate grid.

**THE TOOL.** `tools/grid.py` + `npm run grid`. Labels every tile `col,row` over a render, red
where the tile carries an OVER tile. Built because four round-trips were lost to deriving marked
tiles from pixel arithmetic — a method that answered the same question two different ways.

## OPEN ISSUES
- **GLYPH SPACE IS FULL** — every letter, digit and punctuation mark assigned. Next new tile needs
  a two-char grid or the six SHUT-gate glyphs reclaimed. Decide before the next art request.
- **`earth_shade` / `earth_worn` are named BACKWARDS** relative to appearance. Cost two inverted
  instructions. Warning is in all three legend entries; the rename is its own job.
- **Three smithy openings withdrawn, not deleted** — their art is banded for a north-south
  elevation. Need a home now the flank is gone.
- **The low wall at (18,25)-(18,27) was replaced by bamboo** — three of his seven marks landed on
  it. Needs its own column if still wanted.
- The smithy roof is a flat slab with no end treatment; the NE corner of the court is a large
  empty field. BOTH PARKED at his instruction.
- 9 of 11 palettes still have empty `derivedFrom` (`CF-S107-PALETTE-BACKFILL`).
- `.claude/plans/S108-batch-pdr.md` remains untracked (already archived; S109 ran on direct owner
  instruction with no plan file of its own).

## BLOCKED ON
1. **ROTATE** the Anthropic + GCP keys in `Game/founding-realm/.env` (`CF-S107-KEY-EXPOSURE`, open since S107).
2. Still wanted: **tape measure** (6 sessions) · `IMG_8865` · 1783 gate plans · old smithy access.
3. `CF-S107-PUBLIC-PAGES` — parent repo is public and publishes `.claude/` to Pages. **Daniel
   explicitly authorised pushing everything this session with that exposure on the table.**

## NEXT STEPS
1. **PART 3 — THE ARRIVAL**: spawn outside the gate, guard/ticket vendor NPC, buy ticket + class,
   ISEKAI cinematic, gate opens. The only unbuilt part of the plan.
2. **THEN Council on `CF-S109-WORLDBUILD-SYSTEM`** — his sequencing, explicitly.
3. Ask what to correct. Use `npm run grid` on every screenshot.

## CHANGED FILES
Submodule (26 files, +3815/−1373). New: `tools/grid.py`, `docs/S109-SITE-TRUTH.md`, `docs/s109/`
(transcripts + 4 evidence images). Heavily modified: `tools/build-zone1.py` (+921), `build-atlas.py`,
`build-bld1.py`, `build-cellar.py`, `src/core/Interact.ts`, `src/core/ParcelScene.ts`,
`realm/features.json`, `realm/media.json`, all four maps, atlas art, `package.json`.
Parent: `.claude/reflexion_log.md`, `boot-snapshot.md`, submodule pointer.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **4/4 complete** (S109-P1…P4) | 810K/1M (ORANGE)
- S109-P1 Site survey (analysis only) — completed — 160c423
- S109-P2 PART 1, the ground — completed — 4416a17
- S109-P3 PART 2, the fabric — completed — 0d4cf10
- S109-P4 Owner corrections rounds 1–2 — completed — 3730f58

## REFLEXION ENTRIES (this session)
14 appended to `.claude/reflexion_log.md`. The load-bearing ones:
- **S109-6** Pixel-arithmetic on a screenshot is not a method — four round-trips, two different
  answers to one question. Fixed by building the tool, not by trying harder.
- **S109-7** A pass that runs before the thing it must sit on top of — THREE times in one session.
  Six of seven marks landed and the seventh silently did not; partial success reads as success.
- **S109-8** Checking half a set is how a bug survives being fixed.
- **S109-9** When a coordinate frame moves, move the FRAME, not the call sites.
- **S109-10** The guards caught what I would have reported as done.
- **S109-11** A name that contradicts its appearance keeps producing inverted instructions.
- **S109-12** Superseded assertions are amended with a note, never deleted (nine this session).
- **S109-14** The correction rounds ARE the working mode, and they must scale.

## CARRY-FORWARD PRIORITIES
None incomplete. One held decision:
1. **`CF-S109-WORLDBUILD-SYSTEM`** — world-building/admin system so corrections stop costing
   screenshots. `tools/grid.py` is the first piece and is built. Council NEXT SESSION, after Part 3.

═══════════════════════════════════════════════════════════
