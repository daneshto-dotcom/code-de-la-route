═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-22
Session: S117 — THE SESSION THE GAME ITSELF GOT LOOKED AT
(5 of 6 priorities complete, 1 deferred to the owner, 1 split)
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ `0bd65f6` · submodule `master` @ `a8b6398`
- **UNPUSHED: parent 11 ahead of `origin/main`, submodule 15 ahead of `origin/master`.**
  **`origin` was verified REACHABLE** (`ls-remote` exit 0), so these counts are EXACT and the
  credential is healthy. The only reason the work is local is that the push question has been
  asked three times and never answered. One word does it.
- Stack: TypeScript + Phaser 3.90 + esbuild; art generated by Python into a 174-tile atlas
- Game: `Game/founding-realm/rebuild` — GBC Crystal-style rebuild, 320×288, 16px metatiles
- Save schema: **v6, unchanged.** Nothing this session needed a bump.

## CURRENT STATE
- Build: **PASSING** — `npm run build` exit 0
- Guards: **8/8** (boundary, palette, reachability, realm, evidence, **scene-surface (NEW)**, staleness, bundle)
- `tsc --noEmit`: clean
- Tests: **3,051 assertions, 0 failed** — was 2,958 at S116 close, **+93**
  (evidence 12 planted + control · scene-surface 13 · box-layout 25 · account-rules 55 · save-migration 2,958)
- gitleaks: no leaks · `gh issue list`: empty
- **NEW: `.github/workflows/rebuild-ci.yml`** — the rebuild finally has its own CI
- Context at close: **546,630 / 1,000,000 (54.7% YELLOW)**
- MCV `verify-session-claims`: **hard_fail=0 warn=0 → exit 0**, 24 modified files, **0 UNBOUND**, **36 typed assertions**
- CHECK: full Triumvirate. ANALYZE: complete. Rule 22 audit: complete, and it found two real things.

---

## THE HEADLINE: THE FIRST NPC IN THE GAME HAD A MENU THAT RAN OFF THE SCREEN

S116 built a camera and pointed it at the JOURNAL and the pack — the settings screens. This
session pointed it at the **game**, and the very first NPC interaction a new player ever has
carried **three defects in one method**. All three were one mistake: a text budget that was
**counted instead of measured**.

| # | Defect | Measured |
|---|---|---|
| 1 | `"What is this place?"` unreadable | option column hardcoded at `W-78`; the string is **115px in a 78px slot** → right edge **357 on a 320px canvas**, rendering as `"What is this"` |
| 2 | Dael's line collided with his own menu | body wrapped at hardcoded `W-32` → reached x=**256** through a column starting at x=**230** |
| 3 | A 4th option fell outside the box | rows pitched 15px from `y+12` → row 4 at y=**285**, box ends **284**. Reachable: a full bottle over open ground offers Put down / Drink / Pour out / Leave |

**After:** column x=195 (derived), caret 183, widest option ends **exactly** on the right margin
at 310, body wrap 161 ending x=166 — a 17px gap. Four rows pitch to 13, last bottom **282 ≤ 282**.
A hostile 900px option ellipsises to a **visible** `…`. `STILL_OVERFLOWING: []`.

New module **`src/core/BoxLayout.ts`, deliberately Phaser-free** on the `Input.ts` precedent:
measuring needs a font and stays in `Interact.ts`; deciding where a column goes is pure and moved
out — because the arithmetic was the part that was wrong, and `Interact.ts` touches `Phaser.` seven
times so a headless test importing it dies before its first assertion.

## WHAT A PLAYER CAN DO THAT THEY COULD NOT YESTERDAY
1. **Read all three of Dael's options** — the way into 1601 was a question with its question missing.
2. **Read the help and come back** without the diagram staying painted over the keys page, forever.
3. **Understand character creation.** It said `LEFT / A TO CHOOSE`; it now says `LEFT / RIGHT TO CHOOSE`.
4. **See four options at once** without the fourth falling out of the box.

## THE PRIORITIES

**P1 — WHICH PICTURE WAS LYING** (`b6fcf74` / `df6aef9`). Answer: **neither. The game leaked.**
The JOURNAL owns TWO Graphics surfaces — `chrome`@10, `anim`@20 — and `redraw()` cleared only the
first. `anim.clear()` sits in `update()` **below the `return`** on the page-switch path, so HOW TO
PLAY → KEYS left the last diagram painted on top permanently. Shipped in S115. Reproduced in a
composited screenshot, three cards deep. **Guard #8** `scene-surface-check.ts` asserts every surface
a scene owns is cleared by its wholesale redraw; run against `Journal.ts@HEAD` it reports
`LEAKED: anim` and fails the build. **+13 assertions.**

**P2 — THE SECOND LOOK** (`f2e536a`). Eight beats from a virgin save, with an automatic overflow
probe at every one. Three real defects (→ P3); silent on the other seven beats. Interiors, the 2026
arrival, Dael, the cinematic, 1601, the pack all photographed for the first time.

**P3 — the MessageBox trio** (`f2e536a`). Above. **+25 assertions**, one of which proves the OLD
maths overflowed so the test has teeth.

**P4 — DEFERRED, and it is yours.** The three provisional sprites and the man-glyph. §11.0 says
OFFER art, never install it. You were asleep; art is the most subjective surface in the project.
**First item next session.**

**P5 — THE SKETCHBOOK: KEPT, and the question was wrong** (`a7959fa`). Not orphan code —
`addSketch`/`annotate` both cite **§9.2**, which names *"a new sketch in the book"* as one of five
legal responses to an interaction. Four are built; this is the fifth. Deleting removes a sanctioned
outcome and costs a v6→v7 migration for nothing. Half-waking it — recording pages nobody can open —
would **build the violation the law exists to prevent** while looking like progress in a diff.
Waking it needs a viewer, i.e. a third JOURNAL page: `CF-S117-SKETCHBOOK-VIEWER`.

**P6a — THE DEPLOY, GROUNDED** (`a7959fa`). The archaeology overturned the premise — see below.
Shipped: `deploy/schema.sql`, `deploy/account-rules.ts`, `deploy/README.md`, **+55 assertions**.

**CI + Rule 22 fixes** (`fb07b5e`). **CHECK** (`a8b6398`).

---

## THE BLOCKER THAT NEVER EXISTED

Forty sessions of handoffs said *"VPS deploy — blocked on Daniel (Hetzner)"*. All of it was false:

- **No VPS was ever provisioned.** Blocked S78→S83, never landed. `deploy.sh`, `Caddyfile` and
  `docker-compose.prod.yml` were written in anticipation of a box nobody bought — which is why
  `deploy.sh` takes the IP as an argument and no IPv4 appears anywhere in the project.
- **The live site ran off THIS PC** — scheduled task `LegacyOfTheRealm-GameServer` + a Cloudflare
  Tunnel. Both still here, both `Disabled`/`Stopped`.
- **You switched it off on 2026-07-17** (S102, deliberate, elevated command you ran). The 502 is
  that switch. S102 even wrote down that a killed tunnel yields a Cloudflare 502 rather than
  NXDOMAIN *"so 502 is not mistaken for still up"* — **it predicted this exact confusion and I
  walked into it anyway**, because I read the config files and not the session archive.
- **The accounts never persisted.** `gateway.ts:801` picks `JSONPersistence` + `InMemoryAuth` —
  users in a `Map`. The durable branch (`:777`) needed the Postgres that needed the VPS. Every
  registration died on restart. The **29 characters survived** in `realm.json` with 5 backups.

**And none of it is needed, because the rebuild is static — and the database is already paid for.**
From your Tier-0 vault: **Neon, Launch plan, ~$10–15/mo, ALREADY PAYING**; Vercel free tier live;
Cloudflare owning the zone with API + DNS tokens. GitHub Pages also proven (it serves
`spark-online.space`). **New spend to ship this: nothing.**

## OPEN ISSUES
- **`CF-S117-DEPLOY-HANDLERS` (HIGH)** — four endpoints, the login screen, the SaveStore wiring.
  **Deliberately not built:** they cannot be integration-tested from here (no Neon DB, no Vercel
  project, connector unauthorised), and a few hundred lines of untested auth code sitting in the
  repo looking finished is the wrong trade. SaveStore stays unwired: it works today.
- **`CF-S116-FIND-CLAUDE-DIR-SUBMODULE` (HIGH, from S116, still open)** — the real fix in
  `~/.claude/hooks/json_helpers.sh`. Shared by 8 projects → SYNC-BRAIN Tier 0/1, own amendment.
- **`CF-S117-MCV-PARTIAL-STATUS-HIDES-BINDINGS` (MEDIUM, new)** —
  `verify-session-claims.py:289` binds only from priorities whose status is exactly `completed`. A
  `partial` priority contributes **no** bindings and its files read as UNBOUND with no hint why.
  Cost me a confusing `hard_fail=4` at close on a priority holding ten verified assertions.
- **The reflexion prune was destroying what its policy promised it preserved (fixed).** At close I
  appended a duplicate S115 block (the existing one is headed `## S115 —`, not
  `## <date> — Session S115:`), overflowed the 50-entry cap, and the pruner deleted **12 unique
  S111/S112 entries**. Then I checked the policy's justification — "older entries live in archived
  handoff docs" — and **S111's are NOT in `.handoff-archive`**. Restored from a backup taken one
  command earlier; the prune now **MOVES** blocks into `.claude/reflexion-archive.md`. Zero-loss
  verified: 49 entries before, 58 across log+archive after, nothing missing.
- **`CF-S117-TOKEN-TOOL-CWD-SENSITIVE` (LOW)** — `real-context-tokens.py` handed me an *Atelier*
  session mid-way through. Not a regression of the S115 fix: run from the project root it is
  correct. Always `cd "$PROJECT_ROOT"` first.
- **`CF-S117-BOX-FIVE-OPTIONS` (LOW)** — five options cannot fit (63px block, 52px box).
  Unreachable today; the test asserts the ceiling so the next person is told, not surprised.
- **`CF-S107-KEY-EXPOSURE` — rotate the `.env` keys. Open since S107. HIGH. Owner-only.**
- The `man` glyph **reads as a bottle** standing still (measured, not impression). `bottle_empty`,
  `bottle_water`, `key_iron` remain **provisional art**. Both are P4, deferred to you.
- The five HOW-TO-PLAY diagram frames are still **sparse**; the three interiors are **letterboxed**;
  the pack has ~45px of dead space. All composition — your call, not mine.
- The hand-key tap still fires on **release**, not press.

## BLOCKED ON DANIEL
1. **Push?** 11 + 15 commits, credential verified healthy. Asked three times.
2. **P4 art** — pick from options, next session's first item.
3. **The deploy's two owner actions** — the Neon DB + Vercel project (and a **fresh** JWT secret;
   the vault's belongs to CNC), and the Vercel free-tier commercial-use question.
4. **`CF-S107-KEY-EXPOSURE`** — rotate the keys.
5. **The gate's lower half: open bars or solid panel?** One photograph.
6. **A tape measure for STO-1.** **Dael's tutorial role** (since S110). **Where the key lives.**

## NEXT STEPS
1. P4 — the art, offered not installed.
2. `CF-S117-DEPLOY-HANDLERS` once the DB and project exist. `deploy/README.md` is the runbook.
3. Push, or say no.
4. The two `~/.claude` carry-forwards, together, as one SYNC-BRAIN amendment.
5. Answer the composition questions (interiors letterboxed? diagrams sparse?).

## CHANGED FILES (this session)
```
 rebuild/src/core/BoxLayout.ts        | +134 (new, Phaser-free)
 rebuild/src/core/Interact.ts         | +150/-40  (MessageBox measured; close() invariant)
 rebuild/src/core/Journal.ts          |  +25      (the anim leak)
 rebuild/src/core/Creation.ts         |  +11      (LEFT / RIGHT TO CHOOSE)
 rebuild/src/core/Input.ts            |  +19      (primaryLabel)
 rebuild/src/core/SaveStore.ts        |  +24      (the sketchbook decision)
 rebuild/tools/scene-surface-check.ts + .test.ts  | +290 (guard #8 + 13 assertions)
 rebuild/tools/box-layout.test.ts     | +130      (25 assertions)
 rebuild/tools/account-rules.test.ts  | +120      (55 assertions)
 rebuild/tools/shot-inspect.py        |  +18      (legible zoom errors)
 rebuild/deploy/{schema.sql,account-rules.ts,README.md} | +400 (new)
 rebuild/docs/SEEING-THE-GAME.md      |  +95      (the two-worlds rule)
 .github/workflows/rebuild-ci.yml     | +130      (new)
```

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | **5 complete, 1 deferred (P4)** | context 546,630/1M (54.7% YELLOW)
- S117-P1 the JOURNAL diagram leak + guard #8 — `b6fcf74` / `df6aef9` (+13)
- S117-P2 the second look, 8 beats audited — `f2e536a`
- S117-P3 the MessageBox trio + BoxLayout — `f2e536a` (+25)
- S117-P4 **DEFERRED** — art needs the owner's eye (§11.0)
- S117-P5 the sketchbook kept, with the reason — `a7959fa`
- S117-P6a the deploy grounded + rules — `a7959fa` (+55)
- CI + Rule 22 — `fb07b5e` · CHECK — `a8b6398` · bind — `0bd65f6`
- Council: Grok 1 call, Gemini 1 call. **CHECK: 4 claims, 2 refuted by measurement, 1
  reclassified, 1 confirmed and actioned — plus one retraction of my own unmeasured claim.**

## REFLEXION (9 entries, `.claude/reflexion_log.md`)
- **#six-out-of-six-now-an-impression-of-a-ui-defect-has-never-been-right**
- **#the-camera-spent-a-whole-session-pointed-at-the-furniture**
- **#a-blocker-carried-for-forty-sessions-was-never-re-checked**
- **#naming-the-cost-beats-paying-it-badly**
- #the-clean-frame-was-the-unusual-one · #the-environment-changed-under-a-documented-workaround
- #i-wrote-the-anti-test-i-was-reconciling · #two-places-deriving-one-number-is-a-bug-with-a-delay-fuse
- #the-prune-was-destroying-what-the-policy-promised-it-preserved

## PROMOTION CANDIDATES (advisory — never auto-codified)
- **GEMINI `TRACE-NOT-SCORE` — THIRD session of evidence. Codify.** Tonight it produced the only
  external finding that changed the repo, and it was an undocumented invariant, not a bug — a
  category scoring cannot reach. The scored seat's one HIGH inverted a `Math.min`.
- **`RE-VERIFY-INHERITED-BLOCKERS`** — a blocker carried without re-probing is a rumour with a
  ticket number. "VPS blocked on Daniel" survived S78→S117 and was false throughout.
- **`TEST-THE-GATE-BEFORE-SHIPPING-THE-GATE`** — tonight's CI step would have failed every run;
  its own header forbids exactly that. The Spark's silent deploy gate is the same class next door.
- **`PROBE-BEFORE-ADOPT`** — fifth session running, still uncodified.

═══════════════════════════════════════════════════════════
