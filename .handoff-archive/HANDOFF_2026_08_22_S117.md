═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-22
Session: S117 — THE GAME GOT LOOKED AT, AND THEN IT GREW A FRONT DOOR
(7 priorities: 6 complete, 1 deferred to the owner)
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ `8684c83` · submodule `master` @ `8b1654e`
- **EVERYTHING IS PUSHED.** `rev-list --count origin/main..main` = **0**; submodule = **0**.
  S114 → S117 is all on GitHub. This is the first session in four to close at zero.
- Stack: TypeScript + Phaser 3.90 + esbuild; art generated by Python into a 174-tile atlas
- Game: `Game/founding-realm/rebuild` — GBC Crystal-style rebuild, 320×288, 16px metatiles
- **Save schema: v6 → v7** (accounts). Migration covered by +71 assertions.

## CURRENT STATE
- Build **PASSING** · **8/8 guards** (boundary, palette, reachability, realm, evidence, **scene-surface (NEW)**, staleness, bundle)
- `tsc --noEmit` clean · gitleaks clean · `gh issue list` empty
- Tests: **13 + 25 + 55 + 3,029** — evidence self-test, scene-surface, box-layout, account-rules, save-migration
  (was 2,958 at S116 close)
- **NEW: `.github/workflows/rebuild-ci.yml`** — the rebuild finally has its own CI, scoped to `rebuild/**`
- MCV: **hard_fail=0 warn=0 → exit 0**, 30 modified files, **0 UNBOUND**, **61 typed assertions** across six priorities
- Context at close: ~700K / 1M (70% ORANGE)
- CHECK: full Triumvirate. ANALYZE: complete. Rule 22 audit: complete.

---

## THE HEADLINE: THE FIRST NPC IN THE GAME HAD A MENU THAT RAN OFF THE SCREEN

S116 built a camera and aimed it at the JOURNAL and the pack — the settings screens. This
session aimed it at the **game**, and the first NPC a new player ever meets carried **three
defects in one method**. All three were one mistake: a text budget **counted, not measured**.

| # | Defect | Measured |
|---|---|---|
| 1 | `"What is this place?"` unreadable | option column hardcoded at `W-78`; the string is **115px in a 78px slot** → right edge **357 on a 320px canvas** |
| 2 | Dael's line collided with his own menu | body wrapped at `W-32` → reached x=**256** through a column starting at x=**230** |
| 3 | A 4th option fell outside the box | rows pitched 15px from `y+12` → row 4 at y=**285**, box ends **284**. Reachable from a full bottle over open ground |

**After:** column x=195 (derived), widest option ends **exactly** on the right margin at 310,
body wrap 161 → a 17px gap. Four rows pitch to 13, last bottom **282 ≤ 282**. A hostile 900px
option ellipsises to a **visible** `…`. `STILL_OVERFLOWING: []`.

## AND THEN THE GAME GREW A FRONT DOOR

Register/login → hand selection → the 2026 arrival. Daniel asked for it mid-session and then
corrected the approach twice, both times rightly.

- **The card is ADAPTED from the old build** (`public/landing.html:253-265`), not reinvented —
  his call: *"the login window was already built in the past... i dont know if you actually
  need to rebuild everything."* Read and rewritten under §5's read-only-reference rule.
- **It stayed DOM rather than becoming a Phaser scene**, and the deciding argument is mobile: a
  canvas text field does not raise the phone keyboard, and password managers cannot fill it.
- **The skin is the rebuild's**, not the old game's: cream, 11px mono, 2px ink borders, no
  gradient, no radius. Contrast re-measured against the new palette rather than inherited —
  `SHADE on TAN` is 3.98:1, AA-large only, so no body text uses it anywhere.
- **`AccountRules.ts` is shared** by client and future server, so the client refuses exactly
  what the endpoint will. It lives in `src/core/` because tsc rejects a cross-rootDir import
  that esbuild accepts, and a build passing on one of two compilers is a trap.
- **ACCOUNT is a third JOURNAL page**, reachable by ESC from anywhere — his answer, and better
  than both of mine. `SIGN OUT` (keep the world, forget the name) and `RESTART` (erase and
  begin again) are labelled by consequence because they sit one keystroke apart.

## THE PRIORITIES

**P1 — WHICH PICTURE WAS LYING** (`b6fcf74`). **Neither — the game leaked.** The JOURNAL owns
two Graphics surfaces (`chrome`@10, `anim`@20) and `redraw()` cleared only the first;
`anim.clear()` sat in `update()` *below* the page-switch `return`. Shipped in S115.
**Guard #8** now asserts every surface a scene owns is cleared by its wholesale redraw — run
against `Journal.ts@HEAD` it reports `LEAKED: anim` and fails the build. **+13.**

**P2 — THE SECOND LOOK** (`f2e536a`). Eight beats from a virgin save with an automatic overflow
probe at each. Three real defects; silent on the other seven.

**P3 — the MessageBox trio** (`f2e536a`). Above. **+25**, one of which proves the old maths
overflowed so the test has teeth. New `BoxLayout.ts`, Phaser-free.

**P4 — DEFERRED, and it is yours.** The three provisional sprites and the man-glyph. §11.0 says
OFFER art, never install it. **First item next session.**

**P5 — THE SKETCHBOOK: KEPT** (`a7959fa`). Not orphan code — `addSketch`/`annotate` cite **§9.2**,
which names *"a new sketch in the book"* as one of five legal interaction outcomes. Deleting
removes a sanctioned outcome; half-waking it builds the very violation the law forbids.

**P6a — THE DEPLOY, GROUNDED** (`a7959fa`). The archaeology overturned the premise — below.
`deploy/schema.sql`, `AccountRules.ts`, `deploy/README.md`, `apply-schema.mjs`. **+55.**

**P7 — THE ACCOUNT** (`a8acefb`, `41ea786`, `d23a6b1`, `8b1654e`). Save v7, the gate, the
JOURNAL page. **+71** across two migration sections.

**CI + Rule 22** (`fb07b5e`) · **CHECK** (`a8b6398`).

---

## THE BLOCKER THAT NEVER EXISTED

Forty sessions of handoffs said *"VPS deploy — blocked on Daniel (Hetzner)"*. All false:

- **No VPS was ever provisioned.** `deploy.sh`, `Caddyfile` and `docker-compose.prod.yml` were
  written in anticipation of a box nobody bought.
- **The live site ran off this PC** — a scheduled task plus a Cloudflare Tunnel, both still
  present, both Disabled/Stopped.
- **Daniel switched it off on 2026-07-17** (S102). The 502 is that switch. S102 even wrote down
  that a killed tunnel yields a Cloudflare 502 *"so 502 is not mistaken for still up"* — it
  predicted this exact confusion and I walked into it anyway, having read the config and not
  the history.
- **The accounts never persisted.** `gateway.ts:801` picked `InMemoryAuth` — users in a `Map`.
  The 29 characters survived in `realm.json`; the logins never did.

**None of it is needed: the rebuild is static, and the database is already paid for.**
**Daniel created the Neon project this session** — `legacy-of-the-realm`, Frankfurt, PG18,
branch `production`. The vault block `legacy_realm_db` exists but still holds
`PASTE_..._HERE`; paste the connection string and `deploy/apply-schema.mjs` applies the schema
and proves it landed.

## OPEN ISSUES
- **`CF-S117-DEPLOY-HANDLERS` (HIGH)** — four endpoints, the SaveStore network wiring. Blocked
  on the Vercel project + the free-tier commercial-use question. Deliberately not written blind.
- **`CF-S116-FIND-CLAUDE-DIR-SUBMODULE` (HIGH, from S116)** and **`CF-S117-MCV-PARTIAL-STATUS-HIDES-BINDINGS` (MEDIUM)** — both in `~/.claude`, shared by 8 projects → SYNC-BRAIN Tier 0/1, own amendment.
- **`CF-S107-KEY-EXPOSURE` — rotate the `.env` keys. Open since S107. HIGH. Owner-only.**
- **The reflexion prune was destroying what its policy promised it preserved (FIXED).** It
  deleted 12 unique S111/S112 entries; S111's were verified **absent** from `.handoff-archive`.
  Restored from a backup; the prune now **moves** blocks to `.claude/reflexion-archive.md`.
  Zero-loss proven by entry text, not tags — a tag diff had said "nothing lost" and was wrong.
- **`CF-S117-BOX-FIVE-OPTIONS` (LOW)** · **`CF-S117-SKETCHBOOK-VIEWER` (LOW)** · **`CF-S117-TOKEN-TOOL-CWD-SENSITIVE` (LOW)**
- The `man` glyph **reads as a bottle**; three item sprites remain **provisional**. Both are P4.
- Interiors are **letterboxed**, the pack has ~45px dead space, diagram frames are **sparse** — composition, and yours.
- Offline, sign-out shows **REGISTER not LOGIN**: the hash lives with the account, so clearing
  it removes the only thing to check against. Honest for one machine; LOGIN needs the server.

## BLOCKED ON DANIEL
1. **P4 art** — options offered, not installed. First item.
2. **The Neon connection string** into `legacy_realm_db.pooled`, then the **Vercel project** and the **free-tier ruling**.
3. **`CF-S107-KEY-EXPOSURE`** — rotate the keys.
4. **The gate's lower half: open bars or solid panel?** One photograph.
5. **A tape measure for STO-1.** **Dael's tutorial role** (since S110). **Where the key lives.**
6. **Test the login on your phone** — the case that decided the DOM-vs-canvas call, still unverified.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | **6 complete, 1 deferred** | context ~700K/1M (70% ORANGE)
- Council: Grok 1 call, Gemini 1 call. **CHECK: 4 claims — 2 refuted by measurement, 1
  reclassified, 1 confirmed and actioned**, plus one retraction of my own unmeasured claim.
- **27 commits across two days**, all pushed.

## REFLEXION (12 entries, `.claude/reflexion_log.md`)
- **#six-out-of-six…** → now **seven for seven**: an unmeasured read of a frame has never been right.
- **#the-camera-spent-a-whole-session-pointed-at-the-furniture**
- **#a-blocker-carried-for-forty-sessions-was-never-re-checked**
- **#naming-the-cost-beats-paying-it-badly**
- **#i-fixed-fail-quiet-then-reintroduced-it-one-commit-later** — same file, same day.
- **#i-contaminated-the-browser-he-was-about-to-open** — twice.
- **#the-answer-was-his-not-mine** — ACCOUNT belongs in the JOURNAL.
- **#a-path-is-a-moving-target-in-the-same-way-a-version-number-is**
- **#the-prune-was-destroying-what-the-policy-promised-it-preserved**

## PROMOTION CANDIDATES (advisory — never auto-codified)
- **GEMINI `TRACE-NOT-SCORE` — third session of evidence. Codify.** It produced the only
  external finding that changed the repo, and it was an undocumented invariant, not a bug.
- **`RE-VERIFY-INHERITED-BLOCKERS`** — a blocker carried without re-probing is a rumour with a
  ticket number.
- **`TEST-THE-GATE-BEFORE-SHIPPING-THE-GATE`** — tonight's CI step would have failed every run.
- **`PROBE-BEFORE-ADOPT`** — fifth session running, still uncodified.

═══════════════════════════════════════════════════════════
