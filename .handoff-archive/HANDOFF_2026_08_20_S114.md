═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-20
Session: S114 — THE WORLDBUILD SYSTEM, THE HELD ITEM AND THE STACK (3 priorities + 1 owner-found fix batch)
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm`
- Parent `main` @ `ea1938e` · submodule `master` @ `4fb5546` — **both PUSHED, 0 unpushed, remotes verified reachable**
- Stack: TypeScript + Phaser 3.90 + esbuild; art generated by Python into a 174-tile atlas
- Game: `Game/founding-realm/rebuild` — GBC Crystal-style rebuild, 320×288, 16px metatiles
- Save schema: **v5** (was v4)

## CURRENT STATE
- Build: **PASSING** — `npm run build` exit 0 end to end (prebuild → esbuild → check)
- Guards: **7/7 PASS** (boundary, palette, reachability, realm, evidence, **staleness [new]**, bundle)
- `tsc --noEmit`: clean
- Tests: **`npm test` both suites green** — evidence-check 12/12 planted violations refused + control;
  save-migration **2850 assertions, 0 failed**
- MCV `verify-session-claims`: **hard_fail=0 warn=0 → exit 0**, 24 modified files, **0 UNBOUND**
- gitleaks: no leaks across 555 commits
- Dev server: **stopped and port 19111 released** at close (STEP 1.8). Restart with `npm run dev` from
  `Game/founding-realm/rebuild`; a fresh port is assigned. Both admin pages open off disk with no server.
- Context at close: **~592K / 1M (59% YELLOW)**
- ANALYZE: **completed** (GOV-28 correctly flagged that it had been skipped — see the section below)

## SESSION COST
- Model routing data unavailable (`~/.claude/session-model-counts.tmp` absent)
- External API: **Grok 2 calls, Gemini 2 calls** (two Council rounds). ~$0.20 estimated, NOT metered.
- Cumulative log: `~/.claude/usage-log.csv`

## THIS SESSION'S WORK

**P1 — CF-S109 THE WORLDBUILD SYSTEM, HALF ONE** (`2cf873d`)
The oldest open item, Council-sequenced by Daniel after Part 3. Two self-contained generated pages
plus the guard that stops either lying. `docs/explorer.html` — a parcel's render with an SVG grid
over it; click a cell and it reports the address, the tile on every layer, walkability, the map's own
desc, the evidence claims, and anything the ENGINE puts there. `docs/tile-library.html` — all 174
tiles at 3× with frame index, palette quads, every provenance claim + record id, and where each sits
in the world; search reaches the whole card, so `OBS-FORECOURT-SURFACE` finds all 7 tiles standing on
it. `src/art/world-index.json` is the spine: per-cell annotations for all seven placement routes plus
the inverted index `tileName → where placed` — a question nothing could answer before.
**NO IN-GAME INSPECTOR, by Council decision:** an overlay converts a pointer through canvas scale and
camera scroll, which is the same pixel arithmetic whose unreliability caused CF-S109. A render is
exactly `cols*tilePx` by `rows*tilePx` (asserted, not assumed) and the SVG uses the map's own
coordinate system as its viewBox, so a cell reports integers off its own rect.
Also generated the two missing renders (`bld1ground`, `bld1attic`) and fixed a **pre-existing**
`npm run build` ordering flaw — prebuild rewrote the atlas and maps, then check found dist stale, and
esbuild sat AFTER check so it never ran. Disclosed before commit rather than slipped in.

**P2 — PHASE A: the held item shows in your hand** (`c96ce2e`, fix `4fb5546`)
Daniel: *"when i equip it it doesnt show as if it is in my hand while it should - inconsistent."*
My first answer to him was WRONG — I said the 3-colour `player.png` made it impossible; that limit
only forbids drawing the item INTO the sheet. Corrected unprompted.
Position is **derived from `player.x/y`, never synced**, and both Council members were overruled on
evidence: both proposed a Phaser Container, but `this.player` is a plain `add.image` tweened directly
and no Container exists, so that meant refactoring movement/depth/lunge to buy what deriving gets
free. Placed on **POST_UPDATE** after live measurement showed a 1px one-frame lag at the top of
`update()`.
**Then Daniel found the real defect in the shipped build:** it drew only the ACTIVE hand, so the other
hand was invisible and pressing `1` on an empty left hand made a carried item VANISH. That came from
adopting Gemini's one-hand-only advice as a finding when it was a legibility guess. **Both hands now
draw**, mirrored when the back is turned, staggered vertically when side-on.

**P3 — PHASE B: stacks, and a split you hold Enter to open** (`a521c90`)
Daniel: *"i picked up multiple empty bottles ... but i only see one in the inventory. there should be
a simple split mechanic ... like in spark."* **Diagnosed first: not a display bug — there was no
stacking anywhere in the game.** Every slot held exactly one tile name.
**Only the PACK stacks; hands are capped at one BY LAW** — stacking hands collides with Items.ts law 1
(a vessel takes the nature of what fills it), which would have to split a stack mid-FILL. That cap is
why FILL/SPILL/DRINK/THROW/PLACE are untouched by this priority; the compiler found 5 sites, all pack.
**`assign()`'s "always a swap" law is dead** — both Council members reached that independently, the
strongest convergence the protocol produces. New rules: same-tile in pack → merge (no cap, a cap is a
number the record does not support); stack ↔ hand → **draw one / return one** (the case both models
left open); anything else → swap, justification intact. `landingFor()` merges BEFORE claiming the
other hand — the one line that actually answers the complaint.
`full` → tile-aware **`canTake(tile)`**, which also closes S113's last "a take can never be refused"
corner. Split on **hold `Enter`/`F`** (Daniel's choice, and it lands exactly on the throw-sight idiom).
Count drawn as a **numeral** over Gemini's objection — his premise was refuted by probe: the subscreen
already draws Latin `L`/`R` in the same 11px monospace.
**A bug only live testing could find:** ESC cancelled a split and the very next frame re-opened it,
because the key was still down and every open-condition was still true. Fixed with `holdSpent`.
`tools/save-migration.test.ts` — property-based over 120 random v4 carries (per-tile totals invariant,
slot-by-slot ONE-TO-ONE, no zero/negative/fractional/NaN qty), 11 hostile shapes, v3→v5 coverage, and
the stack rules each asserting THE TOTAL NEVER CHANGES.

**Close-out — MCV RECONCILIATION (the most important thing in this handoff)**
The close gate hard-failed with **14 failures**: all three priorities **UNCOVERED** (zero typed
assertions), 9 UNBOUND files, 2 superseded historicals. I had written long, accurate, genuinely-
verified `check_method` PROSE and never authored `verification[]`. That is exactly S113's
`#prose-is-not-verification` lesson, **which I quoted back approvingly earlier in this same session**
and then broke three times. Now **73 typed assertions, 0 UNBOUND, exit 0**. Binding them per file also
exposed that P3 had edited the **v3** migration branch without testing it — now covered.

**Then it happened AGAIN, from my own fix.** Closing the stale `S113-P5` row (correct — S114-P1 had
delivered it) created a *completed priority with zero assertions*, which is the same UNCOVERED class,
minutes after reconciling the first one. Bound to CF-S109's own four wants — click a tile → coords +
name; a browsable library; point at the one I want instead; the index that makes those answerable — so
the row proves the REQUEST was met rather than duplicating P1's 32. Final: **82 assertions, exit 0.**
The lesson generalises past this session: *any* edit that flips a status to completed must carry its
assertions in the same write.

## OPEN ISSUES
- **A drop-from-pack verb is still missing.** With everything full and a container in front, Take draws
  dim. Recoverable (place from a hand), so NOT a soft-lock — but the pack is its obvious home.
- **`bottle_empty` / `bottle_water` / `key_iron` are PROVISIONAL art**, first to redraw.
- **Sketchbook API is dead code** (0 callers) — `SaveStore.addSketch`/`annotate`.
- **Verb-menu ceiling past ~8 entries** recorded, not solved (menus cap at 4).
- **`real-context-tokens.py` reads the wrong project** when several are active — it reported 384,808
  from Chateau Guardian's transcript for a session at 286,164. Every token figure it produces is
  suspect. `CF-S114-TOKEN-INSTRUMENT`.
- **The dev server was left running on 19111** deliberately (Daniel was mid-test). SessionEnd reclaims it.

## BLOCKED ON
- **Daniel: which throw-aiming option** — (a) mouse aims + arrows walk [recommended], (b) arrows aim
  and movement freezes, (c) reticle sticks to a tile. His two requirements collide; nothing can be
  built until he picks. **ASK BEFORE BUILDING.**
- **Daniel: `CF-S107-KEY-EXPOSURE`, rotate the `.env` keys.** Open since S107. HIGH. Owner-only.
- **Daniel: where the key really lives** — *"make it hard to find later or make it into a quest."*
- **Daniel: a tape measure for STO-1** — unblocks a real interior behind a door that already opens.
- **Daniel: the gate's lower half** — open bars or solid panel? One photograph settles it.
- **Daniel: backlog #29, the film** — parked; direction is stills, not video.

## NEXT STEPS (priority order)
**Immediate**
1. **ESC → SETTINGS + THROW REDESIGN** — PDR drafted, awaiting the throw-option answer above. Two
   parts: a settings scene with rebindable keys + a "how to play" page (the real fix for *"i dont know
   how to throw the bottle or to hit with it"*), and the toggled-reticle throw. Rebinding is the big
   piece: keys are hardcoded `kb.addKey(K.ENTER)` and must move into a keymap in the save plus a
   binding layer. Full tier, Council mandatory, est. 50–70K.
2. **A drop-from-pack verb** — closes the last dim-Take corner.

**Short-term**
3. Move the key to wherever Daniel wants it (one line in `build-cellar.py`).
4. Wake the sketchbook on first pickup, or delete it.
5. `CF-S114-TOKEN-INSTRUMENT` — make the token script prefer the CWD-derived project dir.

**Medium-term**
6. STO-1's interior once measured; backlog #29's frame-by-frame film; redraw the provisional item art.

## CHANGED FILES
```
 rebuild/README.md                      |   67 +
 rebuild/docs/explorer.html             |  274 ++
 rebuild/docs/tile-library.html         | 6449 ++++++++++++++++++++++++++++++++
 rebuild/docs/renders/bld1attic*.png    |  Bin (3 new)
 rebuild/docs/renders/bld1ground*.png   |  Bin (3 new)
 rebuild/package.json                   |   18 +-
 rebuild/src/art/world-index.json       |    1 + (158 KB, new)
 rebuild/src/core/ParcelScene.ts        |  143 +-
 rebuild/src/core/SaveStore.ts          |  323 +-
 rebuild/src/core/Subscreen.ts          |  270 +-
 rebuild/tools/explorer.ts              |  404 ++
 rebuild/tools/save-migration.test.ts   |  467 ++
 rebuild/tools/staleness-check.ts       |  158 ++
 rebuild/tools/tile-library.ts          |  392 ++
 rebuild/tools/world-index.ts           |  (13.5 KB, new)
 BACKLOG.md                             |  rows 35-37
 21 files changed, 8926 insertions(+), 45 deletions(-)
```

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **3/3 complete** + 1 fix batch | context 589K/1M (59% YELLOW)
- P1 CF-S109-WORLDBUILD-SYSTEM — completed — `2cf873d` (32 assertions)
- P2 PHASE A the held item — completed — `c96ce2e` + fix `4fb5546` (10 assertions)
- P3 PHASE B stacks + split — completed — `a521c90` (31 assertions)
- Also closed the stale `S113-P5` row: it WAS CF-S109, delivered here as P1. Closed with a pointer
  rather than left pending, because a stale pending row misreports state to every future session.

## REFLEXION ENTRIES (this session)
16 entries appended to `.claude/reflexion_log.md` and verified; log pruned to the 50 cap (S109's block
aged out). Headline six:
- **#i-quoted-the-lesson-and-then-broke-it** — wrote the prose, cited the rule, skipped the assertions.
- **#a-council-opinion-about-looks-is-a-hypothesis** — one-hand-only was a guess; Daniel was the instrument.
- **#the-law-nobody-in-the-room-named** — Items.ts law 1 settled hand-stacking; neither model had read it.
- **#a-cancel-that-instantly-undoes-itself** — ESC cancelled, the next frame re-opened.
- **#assert-the-invariant-not-the-current-value** — third break of the same version-number assertion.
- **#the-instrument-degrades-with-use** — synthetic key dispatch dies part-way through a page's life.

## ANALYZE — COUNCIL RETROSPECTIVE (run at close, GOV-28 was right that it was missing)
Adoption tallied from the two Battle Ledgers, then each model was sent the objective record of what
happened to every one of its positions. **Grok 8/17 adopted (47%)** — strongest on architecture and
safety; the two risks it rated highest both became shipped guards; it caught every DATA risk and missed
every UI/input-state one. **Gemini 8/12 adopted (67%)** — best on UX specification, worst on any claim
about the existing state of the code or screen; **1 of its 8 adopted positions shipped a defect** (the
one-hand-only render the owner found in minutes), and **both** of its rejected positions failed for the
same reason: asserting a checkable fact without checking it.

**Headline finding: the strongest signal this protocol produces was wrong once.** Two independent
models agreeing — Container-parenting the held item — was refuted by reading one file. Two models can
share the same missing file, so convergence raises confidence about REASONING and says nothing about
FACTS. **4 of the 5 wrong adoptions this session were unchecked factual claims, every one refutable in
under a minute.**

Domain weighting worked but is not a substitute for verification: the count-glyph decision went to
Gemini's own quality domain and was still killed by a 10-second probe.

**PROPOSED, NOT ADDED — `PROBE-BEFORE-ADOPT`.** A Council position asserting a FACT about the codebase
must be probed before adoption; one about how something will LOOK must be prototyped. Neither may be
adopted on authority, domain weight, or unanimity. Asked separately what they would change, Grok
proposed a mandatory live smoke test and Gemini proposed "Verify-Then-Assert" — the same conclusion my
own reflexion reached independently. Three paths, one rule. **Adding it to CLAUDE.md or the council
skill is Daniel's call under SELF-MAINTENANCE.**

## CARRY-FORWARD PRIORITIES
1. **NEXT BATCH: ESC→SETTINGS + THROW REDESIGN** — PDR drafted, blocked on one owner answer.
2. **CF-S107-KEY-EXPOSURE** — rotate `.env` keys. Open since S107. HIGH.
3. **CF-S110-FILM-REROLLS** — four weak shots + the gate question.
4. **CF-S114-TOKEN-INSTRUMENT** — the token script reads another project's meter. MEDIUM.
5. **CF-S114-MCV-VERSION-ASSERTION-CLASS** — assert the migration branch, never the version number.
6. **CF-S113-MCV-SUPERSEDED** — historical supersessions, logged not dropped.
7. **Backlog #37** — damage, fire, liquid effects. RECORDED, deliberately not designed, on his instruction.

═══════════════════════════════════════════════════════════
