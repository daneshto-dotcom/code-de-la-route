═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — Legacy of the Realm
Generated: 2026-08-29 | Session S122
Focus: the gates were not gates; multiplayer shipped and proved itself in production
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\Legacy of the Realm
- Branch: main | parent f7d6680 | submodule Game/founding-realm @ 3b86e6a (master)
- Stack: TypeScript + Phaser 3, esbuild, Cloudflare Worker + Durable Objects, Neon Postgres
- Codebase: 74 .ts files, ~25,400 lines across src/ + tools/

## CURRENT STATE
- typecheck 0 errors | npm test 13 suites, 4,090 assertions | npm run check 10/10 guards
- scan:secrets no leaks | MCV hard_fail=0, warn=0, 0 UNBOUND across 78 files
- CI: Rebuild CI, Deploy site, Deploy to GitHub Pages, Submodule Bump Gate — ALL GREEN
- Live: legacyoftherealm.com serving a bundle BYTE-IDENTICAL to a rebuild from committed source
- Multiplayer live and proven in production (two clients over wss://, 521 ms connect)

## SESSION COST
- statusline_dead all session (pidfile stale >8.2 Ms); real-token totals unavailable.
- External API: grok 1 call ($0.01), gemini 1 call ($0.00). Four Claude subagents at close.

## THIS SESSION'S WORK — 8 priorities, all completed and verified
- P1 CF-S121-TESTS-ARE-NEVER-TYPECHECKED closed, and WIDER than filed: tsconfig included
  only src/**, so all of tools/ — 11 suites AND the 9 build guards — was unchecked.
  79 errors, 7 root causes, six of them one bug: hand-maintained shadows of types that
  already existed. Guard #10 (check:typecheck-coverage) stops it regressing.
- P2 CI truth. The deploy gate checked one of the two secrets it needs; the header told
  sessions the working vault token was dead. Added `npm run ci:status` — the gate could
  not see GitHub at all, which is how 9 red deploys survived two sessions.
- P3 Surveyed all nine daneshto-dotcom repos. Two in the owner's screenshot had already
  self-healed. navlog-pro is the same class as ours: a weekly job failing into nobody's
  inbox for five weeks, leaving airspace data stale — a WRONG answer, not an absent one.
- P4 HUD clock, top-centre. The owner doubted the clock existed; he was right in the way
  that mattered — it rendered "THE HOUR IS UNKNOWN FROM HERE" on his machine.
- P5 Presence server half: one hibernating Durable Object per (realm, parcel).
- P6 Presence client half: two players see each other move. Rate limit MEASURED from the
  game (STEP_MS=190 → 5.26 tiles/s), not inherited from the old engine's MOVE:500, which
  would have rejected more than half of real walking.
- P7 SERVER tab + click-for-sheet. Measured the tab strip BEFORE building: SERVER did not
  fit until 'HOW TO PLAY' became 'PLAY'. journal-strip.test.ts fails on a sixth tab.
- P8 Four bug-hunting patrols at the owner's request. Details below.

## OPEN ISSUES (17 new carry-forwards; full detail in session-state.json)
- CF-S122-PRESENCE-IS-UNAUTHENTICATED (MEDIUM-HIGH) — anyone can join a room with no
  account, watch who is where, and inject a fake player. Shipped today; never a decision.
- Presence: hello unrate-limited and O(N); peers past the 64th permanently invisible;
  close handshake incomplete (ghost-player risk); no heartbeat (room flickers on idle drop).
- Guards: bundle-check's INPUTS omits a bundled parcel; dist/comic ships unchecked;
  evidence-check's freshness assertion compares the artifact with itself; scene-surface
  can SKIP-and-pass on an unparsed signature; several new tests assert source TEXT.
- CF-S122-DAYNIGHT-GLSL-CONSTANTS — the shader hardcodes 512/64/63 the TypeScript derives.
  Same class as the bug this file's own header documents.
- CF-S122-DOCS-STILL-STALE — GOING-LIVE.md, deploy/README.md and BACKLOG.md still carry
  retracted claims. The worst four were retracted AT THE ORIGINAL SITE this session.

## BLOCKED ON HIM
- CF-S107-KEY-EXPOSURE (HIGH, open since S107) — rotate the Anthropic + GCP .env keys.
- CF-S115-SUBMODULE-STATE-SHADOW (HIGH) — deletion inside a submodule needs his go.
- www. is 404 — one zone Redirect Rule. Do NOT re-probe; he said "late on" at S121.

## NEXT STEPS
1. Decide on presence authentication (CF-S122-PRESENCE-IS-UNAUTHENTICATED).
2. Presence hardening — the four carries above, each with a named fix.
3. Guard gaps from the close-out audit.
4. MAP tab (CF-S122-JOURNAL-MAP-TAB) — the strip is FULL and the test will fail first.
5. Server-wide roster; then item spawn/despawn contention (owner-deferred).

## THE BUG HUNT (P8) — four patrols, ~40 findings, 9 fixed
TWO HIGH BUGS IN THE DAY'S OWN WORK, both proven rather than argued:
- A `move` EMPTIED A PEER'S HANDS ON THEIR FIRST STEP. The carry-over listed name, level,
  charClass and look, and forgot gear — defeating the entire reason gear was made its own
  message. Found by a patrol that EXECUTED the functions and reported before/after. It
  survived 17 test sections because the expression was INLINE and nothing drove a move
  after a join. Fixed, extracted to applyMove(), and section 18 now fails 3 assertions if
  it returns — verified by reintroducing the bug and removing it again.
- A NEWLY-CREATED CHARACTER NEVER JOINED PRESENCE IN THEIR STARTING PARCEL. Creation ends
  with scene.resume() rather than a restart, so create() never re-runs. Every new player
  would have had multiplayer off for their whole first session. The path nobody tests is
  the one every real user takes first.
Plus: `npm run check` never typechecked (proven, fixed); bundle-check now asserts the
artifact is minified and non-trivial (proven both ways); four incoherences retracted at
source, including a cited test file (`tools/input-equivalence.test.ts`) that has NEVER
existed on any branch — the reasoning was sound and the evidence invented.

## CHANGED FILES (this session, submodule)
 .github/workflows/deploy-site.yml | rebuild/wrangler.toml | rebuild/package.json
 rebuild/tsconfig.json | rebuild/src/server/{Presence,worker,workers-runtime.d}.ts
 rebuild/src/core/{PresenceClient,ParcelScene,Journal,Input,Traffic}.ts
 rebuild/tools/{presence,hud-clock,journal-strip}.test.ts
 rebuild/tools/{ci-status,typecheck-coverage,bundle-check,evidence-check,site-check,
   provenance-report,world-index,save-migration.test,auto-sync.test}.ts
 rebuild/deploy/{pg-test-db.mjs,RESEARCH-multiplayer.md} | .gitignore
 parent: CI_HEALTH_ACROSS_REPOS_S122.md | .claude/{launch,verify-watch-roots}.json

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 8/8 complete | budget GREEN (~0K/1000K reported;
statusline dead all session so the figure is not trustworthy — see SESSION COST)
P1 typecheck hole — completed — fd26de4 | P2 CI truth — completed — 8fb06fc
P3 cross-repo survey — completed — d5c585a | P4 HUD clock — completed — 235e08f
P5 presence server — completed — e58138d | P6 presence client — completed — e2fe20d
P7 social layer — completed — 05e2203 | P8 bug hunt — completed — 3b86e6a

## REFLEXION (31 entries appended to .claude/reflexion_log.md, pruned to 49)
Themes: a gate that never checked what everyone assumed (twice); a patrol that EXECUTED
code finding what reading it missed; the bug and the test gap being the same shape; an
invented citation reading as the strongest sentence in its docblock; corrections appended
downstream of stale claims losing to the header; and a number tuned for a different
engine that would have fought the player.

## CARRY-FORWARD PRIORITIES
None incomplete. All 8 priorities finished. 17 new carry-forwards logged (above), plus
the standing owner-blocked items. Two priorities were SPLIT mid-session (P5→P6, P6→P7)
rather than marked done with work outstanding; that is named in session-state so it does
not become a habit of renaming unfinished work as finished.
═══════════════════════════════════════════════════════════
