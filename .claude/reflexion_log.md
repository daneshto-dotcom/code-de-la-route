# Reflexion Log — Legacy of the Realm

Cross-session learnings for the PDCA pipeline. Newest session at top. Pruned to ~50 entries; older entries live in archived handoff docs.

## 2026-08-12 — Session S105: the 7 videos mined, the realm database built, and the corrections driven into the world

- P1 #pattern-draw-the-box-dont-infer-it — Three palette passes produced confident nonsense — dark-green 'painted iron', blue 'roof tile', dark-green 'sunlit grass' — because I inferred crop coordinates from a downscaled overview and was ~5% off vertically. Reading the numbers taught me nothing; the swatch always looked like *a* colour. The fix was to DRAW the sample rectangle onto the source frame and look at it. The offset was obvious in one glance. S104 learned 'verify the crop, not just the cluster'; this is the same lesson one level deeper — verify the crop's POSITION, visually, on the image, not by re-deriving the arithmetic.

- P1 #pattern-dominant-returns-the-background-for-thin-things — k-means 'dominant cluster' is wrong by construction for thin elements. Between the bars of an iron railing, most pixels are the dark foliage BEHIND the railing, so the dominant colour of a correctly-placed crop was still not the railing. Painted iron came out #253021, a dark green. Materials need a per-material pick strategy — lightest cluster for thin bright elements, warmest for brick against mortar, darkest for beams against plaster — not one global rule.

- P1 #pattern-backlight-is-not-colour-evidence — I read the gate's ironwork as black, then as white, then finally as pale grey-green. The first two came from the backlit clip where the gate is pure silhouette. A silhouette carries PROFILE information at high confidence and COLOUR information at none, and I was treating one clip as authoritative for both. Front-lit media is the arbiter for colour; the record now names which clip settles which property.

- P2 #pattern-a-guard-that-never-failed-is-not-a-guard — The realm validator passed on first run over 110 hand-authored records. That is exactly when a guard is least trustworthy — passing proves nothing about blocking. I injected 7 deliberate violations (missing geometry, dangling ref, upward-pointing source, bad material ref, duplicate id, measured-without-frame, inference-without-basis) and confirmed 10 errors and exit 1. Static parse is not runtime validation; the test that matters is the failing one.

- P2 #pattern-my-own-tooling-corrupted-the-data-i-was-protecting — To test the validator I round-tripped the JSON through python — and json.load(open(p)) on Windows reads as cp1252, so every em-dash became mojibake in the files I had just written as the project's source of truth. The guard did not catch it because encoding was not one of its rules. Two lessons: always pass encoding='utf-8' explicitly on Windows, and the thing most likely to corrupt a canonical store is the maintenance script, not the author.

- P2 #pattern-resolve-council-splits-on-a-checkable-fact — Council genuinely disagreed: Grok said the brick dentil cornice is post-1601 and should be stripped; Gemini said decorative brique-et-pierre is a hallmark of the style that followed the 1593 rebuild and is a crucial DATING feature for 1601. The tempting move is to average or to defer. Instead I resolved it on a date that can be checked — that fashion belongs to Henri IV's reign, 1589–1610, which contains 1601 — and recorded the dissent with the condition that would flip it. Averaging two architectural claims produces a building that existed in no century.

- SESSION #pattern-refuse-to-duplicate-even-when-it-is-the-obvious-move — Having measured 25 colours, the obvious next step was to add them to palette.ts. I did not. The S104 reflexion records that duplicated palettes drifted within minutes, and the structural fix was to give colour one home. Measurements now live in the database with provenance; converting them into 4-colour GBC palettes is a separate art step that costs a real trade (parcel-001 is already at 8/8 background palettes). Restraint here was the higher-quality choice, and it is only visible as a non-action.

- SESSION #s105-meta — Daniel redirected the session twice mid-turn — dropping the building numbers for his own scheme, then asking for the database and clarifying what he meant by the 2026 AI stack. Both landed while tools were still running. The right response was to record the ruling immediately and let it reshape the deliverables, not to finish the original plan first. His clarification also asked for judgement, not compliance: he listed twenty stack items and said some are not needed, which is an invitation to say NO to specific ones. Naming fine-tuning, distillation and synthetic data as actively wrong for this project was more useful than adopting them.

- P3 #pattern-a-data-only-session-is-a-failed-session — I shipped two priorities of survey and database and called it done. Daniel: 'if we have done work. produced new pallets. and did so much of other things,. why didnt we implement it in the game? i want small improvements EVERY session!' He was right and the correction is now constitutional (ACTIVE_PLAN 13.1). Data work that never reaches the screen is invisible to the person paying for it. Plan the visible change FIRST, not as the leftover.

- P3 #pattern-the-owner-found-the-bug-i-rendered-twice — Daniel spotted the inverted hipped roof — 'the triangles of the roof are pointing th wrong way' — in a screenshot I had generated and looked at twice without seeing it. I had even DESCRIBED the roof as a pyramid while reading the real photos. The fix was one boolean swap. I was reading renders for 'does it look like a game' rather than 'is this geometry physically possible'. Ask of every render: could this object stand up in the real world?

- P3 #pattern-a-measured-colour-still-needs-the-right-ramp-step — I put the measured render #86867A on the BASE step and the wall came out grey concrete. The measurement was the dominant of a region that included shade, so on a sunlit wall it belongs as the SHADE step with lighter values above it. A measurement is a fact about a REGION UNDER PARTICULAR LIGHT, not a fact about the material. Placing it in the ramp is a second, separate judgement — and getting it wrong looks exactly like getting the measurement wrong.

- P3 #pattern-my-own-comment-broke-my-own-assertion — I added a file_lacks assertion binding the roof fix, then wrote an explanatory comment that quoted the buggy line verbatim — so the assertion failed on my own documentation. MCV caught it. Worth keeping because the instinct to document the old behaviour is correct; quoting it verbatim in a file under a lacks-assertion is not. Describe the old behaviour, do not paste it.
## 2026-08-12 — Session S104: first game code — parcel-001 built from Daniel's photographs, then reshaped twice by his corrections (3 priorities, all shipped)

- P1 #pattern-verify-the-crop-not-just-the-cluster: k-means over a named photo region returns a confident colour even when the crop landed on sky or shadow instead of the material. Six of 26 regions were wrong on the first pass and would have shipped into palette.ts unnoticed. Rendering each crop NEXT TO its extracted swatch and reading that sheet caught every one. Extract-then-verify-visually, never extract-then-trust.

- P1 #pattern-a-clean-test-on-the-wrong-question-refutes-nothing: to find which parcel-217 edge was the public road I tested which edges had no neighbouring parcel. It ran clean and gave a confident answer — but the kit holds ONLY the 23 estate parcels, so every estate-boundary edge looks "free". The test was well-formed and measured the wrong thing. Nearly baked a 46-49 degree skew into the tile grid. Ask what the data set EXCLUDES before trusting an absence. Recurred later the same session: a least-squares circle fit to the "forecourt curve" scored 8.6x better than a straight line and was still fitted to the wrong edge (the backs of the houses) — refuted only by drawing it on an orthophoto. A good fit to the wrong feature is the most convincing kind of wrong.

- P2 #pattern-the-guard-caught-what-discipline-would-not-have: palettes were duplicated between palette.ts and build-atlas.py. I retuned STONE in one and not the other within minutes of writing both. The build-time guard caught it by decoding actual pixels. Fix was structural, not vigilance: the atlas builder now PARSES palette.ts. A guard that checks the artefact beats a rule that asks humans to keep two copies honest.

- P3 #pattern-hardware-limits-produce-better-art-than-taste-does: the 8-background-palette GBC limit failed parcel-001 at 9. The fix — drawing the cellar void in BRICK's darkest instead of adding IRON for one tile — lost nothing visible and removed a whole palette. The constraint made the decision I would have argued about.

- P3 #pattern-input-nuance-is-a-real-bug-not-a-feel-issue: turn-in-place was implemented as "any direction press needs a 60ms hold", which made every tap a no-op and looked like broken keyboard input. Crystal only turns without moving when the direction CHANGES; pressing the way you already face walks immediately. Cost a full debugging detour into Phaser focus and key mapping before the logic itself was the culprit.

- P3 #pattern-repeated-tiles-amplify-per-tile-noise: three separate art defects this session were one lesson — a texture baked into a tile becomes a PATTERN once the tile repeats. Two rut lines per road tile read as a ploughed field; a wandering crack per render tile read as regular white slashes down the facade; alternating tree/hedge tiles read as a checkerboard. At 16px, per-tile detail must be either invariant or map-placed, never "random" inside a tile that tiles.

- SESSION #pattern-owner-correction-beat-my-inference-every-time: four times the owner's direct knowledge beat careful derivation — the forecourt is a half-circle (I had a box); the road is its edge and off-limits (I had made it walkable); the gate is 1783 and unchanged so there is NO 1601 divergence (I had reasoned my way to oak leaves); and his Google Maps traces confirmed the smithy I identified. Derivation is for what the owner cannot see — occluded geometry, measurements, cross-checks. For intent and for history, ASK FIRST.

- SESSION #s104-meta: owner delivered the blocking asset mid-turn and pre-approved the batch. Right move was to finish the A.0 probes already in flight, then let the photographs re-scope everything. The cadastre INDEPENDENTLY confirmed the plan's building claims to ~1% and the photo walk order matched the measured bearings — two unrelated sources agreeing is far stronger evidence than either alone, and it is why the unresolvable #13/#5 building numbers did not block the build. Session ended with Daniel taking direct design authority (§11.0 THE WORKING AGREEMENT): he directs every step, Claude may OFFER archived assets, never adds without a go.

## 2026-08-11 — Session S103: the plan finished — scale locked, mechanics authored, all repo alarms silenced (3 priorities, all shipped)

- P1 #pattern-the-alarm-was-ours-probe-the-repo-before-blaming-the-vendor: Owner reported "github or cloudflare is mailing me every day that the site is down". Both named suspects were third parties; the actual sender was our own S97 `uptime-monitor.yml` — a `*/15` cron, a deliberate `exit 1`, plus a "Still down" comment per fire on a dedup issue open since 2026-07-09 (~96 runs/day for a month). A 60-second `gh run list` + `gh issue list` probe found it; guessing at Cloudflare settings would have burned the session and fixed nothing. LESSON: when a user blames an external service for notification spam, grep your OWN scheduled workflows first — from the inbox, a watchdog aimed at a door you deliberately closed is indistinguishable from a vendor problem.

- P1 #pattern-adversarial-check-is-worth-it-even-when-most-findings-die: GROK-ANALYST raised 8 findings on a 13-line YAML comment-out. Four were falsifiable and I falsified three in one command (`git ls-remote --heads` killed "other branches keep the cron armed"; a 403 from the dependabot API killed "Dependabot mail remains"; reading lines 57-68 killed "closing the issue breaks dedup"). The survivors were the valuable part: CI failing on push against a FROZEN dep tree, and the re-arm step being documentation-only rather than a machine gate. LESSON: a low adoption rate is not evidence the CHECK was wasted — verify each claim mechanically instead of accepting OR dismissing wholesale, and report the kill list alongside the survivors so calibration data stays honest.

- P2 #pattern-grep-your-own-draft-for-facts-the-owner-never-said: In the P2 PDR I wrote "the three entrances and their real roles — main gate at 217, the north entrance, and 189 as the forest keeper's service gate for livestock and wood" as if summarising Daniel. He had never mentioned a north entrance, parcel 189, or a forest keeper; I synthesised it from a cadastre label and narrative momentum. It never reached the plan file — but only because CHECK ran a mechanical fabrication hunt (grep the artifact for every claim drafted but not owner-confirmed). LESSON: the dangerous fabrication is not the wild one, it is the plausible one phrased as a summary of what the user just told you. Grep the deliverable for owner-attributed claims and trace each to a real message or a data file.

- P2 #pattern-keep-the-conclusion-name-the-fabricated-evidence: GROK-ANALYST's FAIL verdict rested on an "11 days per asset approval cadence (historical session data)" and a "2025 marketing window closes in <90 days". Neither exists — there is no cadence dataset, and the festival date is an explicitly OPEN owner decision in the same plan. Both lazy responses are wrong: accepting the finding with its numbers, or dismissing the finding because the numbers are fake. Gemini reached the same conclusion independently with no numbers at all. I recorded the conclusion AND named the fabrications inside the document so no future session cites them. LESSON: separate an external model's conclusion from its evidence, verify each independently, and when you keep a conclusion whose evidence was bad, say so in the artifact.

- P2 #pattern-a-completed-priority-with-no-assertions-looks-exactly-like-a-fabricated-one: I closed P2 with a 2,000-character `check_method` essay and ZERO mechanical assertions. MCV hard-failed it as UNCOVERED, and it was right to: from the verifier's position, an eloquent prose claim with nothing to grep is the same shape as an invented one. The artifact was real and pushed — that is not the point. I had written the narrative of verification without the act of it. Reconciled with 9 verbatim needles, each `grep -F` confirmed on disk BEFORE being written into the array rather than after. LESSON: prose describing a check is not a check. Any priority marked completed needs at least one thing a script can fail on.

- P3 #pattern-silence-the-failing-alarm-not-the-passing-one: Closing out the noise, the tempting move was to park every workflow in sight. I audited both repos instead: the submodule's three were failing forever against a frozen dependency tree and got parked; the parent's three (deploy, propagation-check, submodule-bump-gate) auto-fire on push and were verified GREEN on this very bump, so they email nothing and stayed armed. Parking a passing workflow is superstition dressed as hygiene — it removes a real guard to cure a symptom it was not causing. LESSON: when silencing alerts, the unit of decision is "is THIS one firing falsely", proven per-workflow with run history, not "alerts are noisy".

- SESSION #s103-meta: The owner narrowed my proposals three times — "correct me as we go" instead of a full-estate map audit, "600 square meters" instead of my creeping two-screen parcel, "just finish the plan" instead of building — and was right every time. My drift was always toward MORE: more concept boards, more map, more parcels. That drift is precisely what killed the previous build, and the person enforcing the parcel doctrine was him, not me. PATTERN: when a user repeatedly narrows what you propose, the narrowing is the signal, not friction to route around.

## 2026-07-18 — Session S102: REBUILD PIVOT — master plan + full archive + site offline (3 priorities, all shipped)

- P1 #pattern-scope-the-owner-decisions-before-the-council-not-after: A total-pivot directive ("rebuild everything from the gate") hides a small set of owner-only decisions that reshape the whole deliberation space. Four targeted AskUserQuestion items (live-site fate, foundation reuse, style fidelity, stale side-request) took one exchange and pre-resolved what would've been the Council's biggest fights — Grok argued hard for a teaser page (D7) Daniel had ALREADY rejected, so the ledger just recorded "overruled by owner, dissent logged once" instead of burning a round. Corollary: when a Council position collides with an explicit fresh owner decision, resolve OVERRULED-BY-OWNER + one-line surface-back — never silent compliance (hides risk), never re-litigate (disrespects the decision). Also: plan-doc-as-priority (P1 = write the multi-session bible, THEN execute against it) let a 100-session codebase pivot cleanly; the doc's cold-boot test ("could S103 run from this file alone?") is the right acceptance bar for any multi-session plan artifact.

- P2 #pattern-mechanical-coverage-gate-survives-agent-death-by-spend-limit: A 12-then-11 agent extraction fan-out was truncated TWICE by spend limits (Fable monthly, then org-wide Anthropic monthly) — agents_done went 0, then 3. What saved the priority: (1) a DETERMINISTIC ground-truth manifest (inventory.mjs) + a MECHANICAL coverage checker (coverage.mjs, manifest-vs-covers-globs, exit 1 on any gap) made "done" provable by a script, not agent self-reports; (2) agents that "failed" on the final structured-return had ALREADY written their spec files first — the failure notification UNDERCOUNTED real progress (always inspect disk, not just the workflow result array — categories reported null had complete .md on disk); (3) committing WIP the moment the limit hit made partials durable; (4) the remaining 8 categories finished in the MAIN LOOP (org limit blocks subagents, not the main loop) with grouped reference-tier entries — more token-efficient than agents. Lesson: for any interruptible fan-out, make completion MECHANICALLY VERIFIABLE from disk (a script that exits non-zero on any gap), commit partials eagerly, keep a main-loop fallback — never let "the workflow errored" mean "the work is lost."

- P2 #pattern-mcv-schema-not-fabrication: An MCV HARD FAIL ("claim did NOT verify against disk, S149 fabrication class") fired twice and BOTH were false alarms — first a stale S98 parent-orphan session-state the gate kept reading, then a verification[] SCHEMA mismatch (I wrote {file, assert}; the verifier requires {type:"file_contains", path, needle, why} — the linter had converted P1 to the right form, so P1 passed and P2's 5 read as "unknown assertion type: None"). The archive was real (584/584 coverage, citations spot-verified). Lesson: when MCV fails, READ THE REPORT — "unknown assertion type" is a schema bug, not fabrication; match the verifier's exact schema, use grep-confirmed needles (the strongest binding is a computed-result string like coverage-report.json's "uncoveredCount": 0, not mere existence); always re-run verify-session-claims.py to exit 0 BEFORE claiming reconciled. Also caught: editing a JSON reflexion array by hand introduced a missing-comma/dup-key corruption — validate JSON parses after any hand-edit of session-state.

- P3 #pattern-scheduled-task-stop-orphans-the-process-and-the-tunnel-hides-a-second-instance: Taking a Windows-served site "fully dark" is NOT one action. Stop+Disable-ScheduledTask disabled the respawn but LEFT THE SPAWNED node PID listening on :3000 (stopping a task ends the task instance, not the detached child tree). AND Stop-Service Cloudflared left a STANDALONE cloudflared.exe process (separate from the service) still routing — public stayed 200 with the service Stopped+Disabled. Only killing both orphans by verified PID (non-elevated, user-owned) darkened it (local :3000 → connection-refused, public → 502 origin-unreachable). Lessons: after any "stop the server" step VERIFY the port is closed + ingress process gone, never trust task/service state alone; "service stopped" ≠ "process gone" on Windows (check Get-Process, not just Get-Service); killed-tunnel yields a Cloudflare 502 (edge keeps the CNAME), not NXDOMAIN — name that distinction so "502" isn't read as "still up"; run the A.0 empirical probe DURING execution, not just before — it caught both orphans.

- SESSION #s102-meta: Daniel ordered a ground-up rebuild (GBC Pokémon-Crystal, tiny parcels from the domaine gate, LOVE-gate per parcel, old build archived not scrapped). 3/3 priorities shipped: P1 ACTIVE_PLAN_realm_rebuild.md (Founder DNA 8f4153f), P2 the archive parts-bin (submodule 19d00a6 — 584/584 mechanical coverage, 50 specs + 39 JSON extracts, freeze tag pre-rebuild-final @ 4dc92e8, Triumvirate CHECK SHIP), P3 site offline (dark, verified). Parent 0276b96. Session survived TWO hard spend-limit interruptions (Fable then org-wide) mid-fan-out — recovered by mechanical-coverage gate + eager commits + main-loop fallback. MCV reconciled to exit 0 (was a schema false-positive, not fabrication). Next: S103 = rebuild begins at the gate (parcel 0 = Imagen concept boards reviewed WITH Daniel, then parcel-001 gate scene). Carry-forwards: breadth-module deep-entry-at-pull-time (Grok), gameplay-video capture while old build restartable (Gemini).

## 2026-07-14 — Session S101: B-05 NPC chains end-to-end + P5 trigger plumbing (2 priorities, shipped + DEPLOYED live; QR perks → S102)

- P1 #pattern-live-smoke-catches-what-unit-tests-structurally-cannot: Three separate defects survived a 25/25 unit suite and were caught ONLY by the live WS boot smoke: (1) the ACCEPT_QUEST validator rejected every chain id BEFORE the handler branch could run — unit tests called the module functions directly, below the validator layer; (2) dotenv override:true silently repointed the "isolated" test server at the LIVE save dir — env propagation worked (PORT bound correctly) but .env clobbered SAVE_DIR inside the process, so the empty scratch dir was the only symptom (retro-flags S100's smokes, same mechanism; live save + all 5 backups verified clean); (3) the distinct-zone walk regression. Lessons: a smoke must FAIL-FAST on isolation violations (assert the server's own resolved persistence path == scratch BEFORE any mutation — now structural in chain-boot-smoke.ts); at least one test must enter through the real protocol because layer-crossing bugs live BETWEEN validator/handler/engine; on Windows, shell:true child trees orphan on /T kill — kill by PORT, not pid tree; and when a save file is missing, read the server's OWN boot log line for the resolved dir before theorizing — one log line settled what three hypotheses could not.

- P1 #pattern-adversarial-check-earns-its-cost-when-findings-flip-the-design: The Triumvirate CHECK on B-05 produced one verdict flip and four real fixes. Grok's FAIL included a plausible CRITICAL (reputation-listener re-entrancy corrupting state mid-finalize) REFUTED by trace + a dedicated re-entrancy unit test (completedAt set before applyReward; reward-sourced reputation legitimately feeds other EARN_REPUTATION quests) — #empirical-refutes-plausible-criticals again. But the CHECK also surfaced genuinely-missed defects: first-match-only starvation (latent until content grows an (actionType,filter) overlap), cold-accept of a chain's final quest (EPOCH-reward skip — the unit test itself was exploiting the hole), quest text promising "N different X" while the counter counted repeats (fixed with a generic distinct/uniqueKeys objective mechanism), and a claimed-logged-but-absent carry-forward (buff rewards). Treat reviewer findings as hypotheses — refute with evidence or fix, never argue; and when a fix invalidates an existing test's premise, that test was documenting the bug.

- P2 #pattern-map-vs-emit-vs-listener-audit-before-reviving-dead-events: A dead event system fails in FOUR distinct places, each needing its own probe: defined (typed map), EMITTED (grep bus.emit), MAPPED (trigger-engine eventMappings), LISTENED (bus.on consumers). P5 had every combination. Reviving an emit without re-verifying TEMPLATE SEMANTICS at the consumer end is dangerous — the combat:resolved mapping evaluated BOTH duelists and the lone victory-framed template's ACTION_COUNT condition would have handed the LOSER a victory choice; the DO-time template trace caught it (fix: victor-only mapping, proven by spawns-for-victor/not-for-loser test). When a reviewer demands a new service module to "clean up" plumbing (WorldStateService), check whether it removes plumbing sites or just relocates one — the ctx still had to carry the fields through 4 of 5 sites, so a per-tick memo in the existing callback bought the same O(1) without new architecture. Balance-threshold reachability is provable from sim constants (decay rates × time) — cheaper and MORE rigorous than building force-spawn tooling.

- SESSION #s101-meta: 2 priorities shipped (P1 B-05 4de117c, P2 P5-plumbing f224892) AND deployed live (S100+S101 together per Daniel's standing choice; scheduled-task restart via PowerShell Start-ScheduledTask after Git-Bash path-mangling broke schtasks flags; deploy verified by an S100-only endpoint returning 401 where S99 would 404). The A.0 probe (5-agent workflow) proved the S100 carry-forward map materially stale in 8 ways BEFORE planning — accept was already wired, completeChainQuest already existed, the real crux was single-entry-per-chain history destruction. Full-tier Council flipped my two-store D1 recommendation to single-store re-key (Grok right). Session totals: 56 new unit tests, 2 live smokes + restart drills, 4 Grok + 4 Gemini calls (~$0.50), context peaked 597K/1M YELLOW pre-resume. QR perks (Full-tier) deliberately deferred to S102 rather than started at YELLOW.

## 2026-07-11 — Session S100: T1.3 legend sheet + D2/D6 admin auth + T1.4 durability + B-04/R1 quests (4 priorities; B-05 + P5 → S101)

- P1 #pattern-reachability-reveals-latent-render-bugs: Extracting the summons canvas into shared artifact-canvas.js and re-verifying the SUMMONS render (not just the new legend sheet) exposed a latent overlap — royal-summons drew date/footer at fixed H-150/95/62 while the name block flowed from wrapped body text, so a long name+title collided them. Invisible since S98 because the summons was UNREACHABLE until the S99 reveal fix made the cutscene attainable. Lesson: when a feature becomes reachable for the first time, its untested render/edge paths ship for the first time too — re-run the FULL visual regression on the now-reachable surface, not just the diff. Canvas-artifact fix pattern: flow the bottom block RELATIVELY from the last wrapped line with a floor (keep the classic bottom-anchored look for short content) AND a ceiling clamp (footer stays in-frame), plus a degrade rule that DROPS optional flavour text rather than overlapping. Proved by rendering both typical and worst-case (max name+title) personas via preview_eval canvas→jpeg.

- P2 #pattern-credential-bound-token-version-no-store: Admin JWT revocation-on-rotation without any per-token store: bind the token to the CURRENT credential via an sv claim = HMAC(JWT_SECRET, ADMIN_PASS) stamped at mint and enforced at the single verifyToken choke point. Rotating ADMIN_PASS + restart recomputes the version so every old admin token fails; works with stateless InMemoryAuth, survives restart, leaves player tokens untouched, ZERO router changes. Two load-bearing details: (1) KEY the HMAC by JWT_SECRET, not a bare hash — JWT payloads are world-readable so a bare hash is an offline-cracking oracle; (2) fail CLOSED (version-unset/missing-sv/mismatch all reject) so legacy no-sv tokens die on deploy. For SSE-token-in-URL: a single-use short-TTL ticket via authenticated POST beats a cookie (no CSRF/SameSite, no new infra) and a fetch-streaming polyfill (keeps native EventSource) — but native auto-reconnect replays the spent ticket, so the client MUST close-on-error and re-handshake. Unit tests prove the logic; only a live boot smoke proves the gateway seed-block wiring + router mount actually run.

- P3 #pattern-persist-then-self-heal-with-one-time-grandfather: A memory-only Map populated at creation and never persisted silently loses all data on restart (earned achievements, wiped every nightly restart, unnoticed because the sidecar never existed). Fix in two parts: (1) add the persistence sidecar so all FUTURE saves are durable; (2) a ONE-TIME boot self-heal for records predating the sidecar. Two subtleties: (a) re-derivation only recovers CONDITION-based state (44/45) — a value awarded IMPERATIVELY (founding_player, a flag-condition string never written to achievementFlags) is unrecoverable, so it needs an explicit grandfather; (b) gate the grandfather on isMigration (no sidecar present) NOT on "missing the achievement", or it re-grants every boot to players who legitimately never earned it. Only a boot smoke booting from a COPY of the REAL live save (no sidecar) proves the migration end-to-end (29 real chars migrated) — a synthetic-state unit test proves the serialize round-trip but not that the gateway self-heal fires on production data.

- P4 #pattern-make-snapshot-only-data-real-then-reuse-the-validated-pipeline: When synthesized-only-in-the-snapshot data is unusable (class quests: ACCEPT rejected them since the validator checks gameState.quests, and the reward re-rolled every sync from Math.random()), make it REAL state that flows through the EXISTING validated pipeline — don't special-case the validator. Class quests became real gameState.quests rows (idempotent ensure by day-stamped STABLE id + FNV-deterministic reward = zero churn without a store) and the whole accept/complete path worked unchanged. For R1 actionId fidelity: the client already HAD the specific id in the snapshot but collapsed it to a coarse type on dispatch; threading it back is client→protocol→emitter, but the load-bearing piece is a NEW server guard — client-proposed identifiers that drive triggers MUST be validated against server-authoritative eligibility (getAvailableActions) at the point of use, or they're a spoof vector (there was NO existing class-eligibility check). The empirical A.0 probe before coding paid off: it revealed the quest system has NO objective mechanism anywhere (global quests already insta-complete), so a bespoke class-only objective would be inconsistent — resolved by gating only the mechanically-trackable types.

- SESSION #s100-meta: 4 priorities shipped (P1-P4), each with full test coverage + a LIVE boot smoke that proved runtime wiring TSC/unit tests could not (P2 8/8 seed-block sv + ticket handshake, P3 11/11 self-heal on a real-save copy, P4 7/7 WS real-player class-quest delivery). The A.0 empirical probe before P4 repeatedly refuted plausible-but-wrong premises (#empirical-refutes-plausible-criticals held again): no objective mechanism exists; no server-side class-eligibility check exists. External Council (Grok+Gemini) refuted well — Grok's P3-Redis-REJECT and questId-migration were both empirically wrong. Model ran Fable 5 then Opus 4.8 (weekly Fable limit) — no quality loss. Ended YELLOW (553K/1M); deferred P5 (only-if-GREEN stretch, carries the balance-sensitive R7) + B-05 (own sidecar+restart-drill) to S101 rather than rush them.

## 2026-07-10 — Session S99: durability off-OneDrive + SQLite adapter + reveal reachability (P0–P4, shipped + deployed live)

- P0 #pattern-read-the-env-before-naming-the-landmine: The Council was briefed on a HIGH "store is JSON only by accident of a broken PG password" landmine — but reading .env showed PERSISTENCE=json is explicitly set (dotenv override:true), so usePostgres is ALREADY false and PG is never contacted; the 28P01 the runtime-verifier saw was its OWN probe, not the server path. Landmine DOWNGRADED active-data-loss → config-fragility (determinism hangs on one .env line while unused full PG creds + AUTO_MIGRATE=true sit nearby). Read the config that SELECTS behavior before assigning severity; a verifier's own side-effect can masquerade as the system's. Fix still shipped (PG explicit opt-in + fail-fast) but as Micro hardening, not a P0 emergency.

- P1 #pattern-one-anchor-for-a-path-default-not-per-file-dirname: Consolidating a save path resolved 3 ways (server.ts __dirname-relative, gateway cwd-relative './save', routes process.cwd()+save), the trap is re-deriving __dirname in the NEW shared module — save-paths.ts at src/persistence means a copied-from-server.ts "../save" resolves to a non-existent src/save and SILENTLY seeds a fresh world (compiles clean, passes suites that inject explicit paths, fails only at runtime on the live 112KB save). Fix: ONE app-root anchor + a unit test PINNING the unset-SAVE_DIR default === the historical absolute save path. Keep the festival field-initializer sourced from the pure resolver (NOT relocated to start()) — the live catch-branch never reassigns it, so relocation splits the save dir.

- P2 #pattern-lazy-require-native-addons-so-the-fallback-catch-can-catch: A top-level `import Database from 'better-sqlite3'` runs at MODULE LOAD (when the gateway imports the adapter), BEFORE initPersistence's try/catch, so a native-ABI failure crashes boot instead of falling back to JSON. Move the native require() INSIDE the constructor (mirror the PG branch). Cutover corollary: the first-boot bootstrap must reuse the FULL recovery chain (quarantine + N backups), not a bare read — a torn primary at cutover would seed fresh and UPSERT it into the new store = permanent loss. Prove with a real boot smoke (PERSISTENCE=sqlite on a scratch port, bootstrapped from a copy of the live save), not just adapter unit tests.

- P3 #pattern-restore-drill-must-fingerprint-not-just-liveness: A restore drill asserting "HTTP 200 + characters>0" FALSE-PASSES on the exact failure it exists to catch — a failed restore seeds a fresh 1601/tick-0/12-char world that satisfies characters>0. Fingerprint the SPECIFIC backup: assert currentYear>=backup.year AND tickCount>=backup.tick (a fresh seed can never reach them; >= is monotonic-safe vs the sim advancing ticks during boot). Bonus: once SAVE_DIR is env-driven (DUR-06), the drill isolates via env (SAVE_DIR+EVENT_LOG_PATH+cwd=scratch-with-no-.env) with NO source-tree copy — the pre-DUR-06 copy-tree complexity evaporated.

- P4 #pattern-derive-the-gate-from-persisted-state-not-an-ephemeral-map: The reveal funnel died on every restart because the chain step gate read an in-memory chainProgress Map that was never persisted/rebuilt, and `continue`d BEFORE the restart-safe QUEST_COMPLETED conditions ran. Derive the gate from PERSISTED state (count completedTemplateIds in the chain). ENUMERATE before choosing the fix: drop-the-gate looked simplest but C02's B15 has NO QUEST_COMPLETED condition and relied solely on the gate — derive-from-persisted fixes ALL chains. A real-engine test (initQuestEngine + event bus) reproducing the post-restart state (empty chainProgress + populated completedTemplateIds) is what proves it; a mock would not.

- SESSION #s99-meta: 5/5 code priorities + live deploy. A 10-agent state-discovery/adversarial-verify workflow + 3-way Grok/Gemini Council caught 5 load-bearing defects BEFORE code; two runtime smokes (SQLite boot + restore drill) plus the live migration validated what static parse could not. A mid-session PC reboot lost nothing (all committed; server auto-restarted via the scheduled task). Migrated the live save OFF OneDrive → C:\ProgramData\LegacyOfTheRealm\save and deployed S99 live. Gotcha: the game-server scheduled task is un-disableable from a non-elevated shell (schtasks /Change → Access denied; /Run works) — safe-ordering (SAVE_DIR set only after ProgramData is populated + originals kept) covered the un-disableable restart race.

## 2026-08-13 — Session S106: ZONE-1 enlarged and opened, the cellar built, and the layout corrected four times by the owner

- P1 #pattern-guard-playability-not-just-well-formedness - Every guard this repo had checked that the world was WELL-FORMED (schema, palettes, layer agreement, referential integrity). None checked it was PLAYABLE. At 360 cells you can eyeball that; at 896 cells across three layers you cannot. reach-check.ts flood-fills from the start tile using the engine's exact collision rule and asserts landmarks the GENERATOR declares - so intent and test move in the same edit and cannot drift apart. It found three defects on its first three runs, every one invisible in the render: a path tile with four unwalkable neighbours, bramble sealing the garden's only entrance, and 15 tiles of grass stranded behind a wall. Well-formed and playable are different properties and only one of them was being defended.

- P1 #pattern-a-tile-has-no-orientation-but-a-wall-does - The low garden wall rendered as the rungs of a ladder. The tile was fine: it carries its bright mortar cap across the TOP, which is right for an east-west run and nonsense stacked down a north-south one, where the cap repeats every 16 px. The same bug was waiting in the 2.1 m boundary wall. The fix is not a cleverer tile, it is admitting that a run in each direction is a different tile. Generalises: any tile with a directional highlight needs one variant per axis it will actually be laid along.

- P1 #pattern-a-forest-is-a-mass-with-an-edge-not-a-field-of-circles - The eastern forest came out as a quilt of identical polka dots. I first tried jittering the placement, which did nothing, because the CIRCLE was the problem: the crown tiles have transparent corners, and tiled adjacent those cut corners line up into a lattice no offset can break. The fix was a structurally different tile - a seamless canopy whose lobes deliberately cross all four edges - with round crowns kept only for the forest's visible EDGE, where a silhouette is exactly what you want. Randomising placement cannot fix a problem that lives in the tile's silhouette.

- P1 #pattern-the-owner-corrected-the-layout-and-the-database-had-already-said-so - Daniel moved the smithy from east to west-behind-the-house. Two records I had read straight past said precisely that in plain words: OBS-GRASS-FIELD, 'the field between BLD-1 and THE SMITHY WALL', and OBS-BOUNDARY-WALL, 'runs between the grass field and the smithy'. I had weighted hand-traced estate_local_m coordinates - from a 360-degree pan, explicitly flagged APPROXIMATE in their own note - above the prose of the observations they were attached to. Numeric-looking evidence is not automatically stronger evidence. Read its stated confidence before letting it outrank a sentence.

- P1 #pattern-probe-the-harness-before-diagnosing-the-subject - Scripted keypresses moved the player zero tiles and I was one step from reporting movement broken. document.hidden was true, requestAnimationFrame was parked, and Phaser's tween manager never advanced; the player HAD been moving between tool calls, on wall-clock. The environment was broken, not the code - and I had changed no movement code at all this session. Two lessons: measure the harness (frames-per-500ms) before diagnosing the subject, and when the interactive route is fighting you, switch to the STRONGER test - a headless flood-fill proves the whole zone is connected, which no scripted walk down one corridor ever could.

- P1 #pattern-satisfy-both-readings-when-it-is-free - Daniel said the garden gate 'opens onto the forecourt'; the database said its low wall 'separates the garden from the inner road'. Those are two different places. The tempting moves are to pick one, or to average. Instead the garden got BOTH gates - the little one off the forecourt in his words, the iron one off the drive in the record's - because the cost was one tile. Only spend a decision where the readings actually conflict on the ground.

- P1 #pattern-council-earns-its-cost-when-it-deletes-something - The single most valuable Council output was GROK-ANALYST refusing my invented five-bar field-gate at the north edge. I was about to record a level-design convenience as evidence_class:inferred in a database whose entire purpose is that facts trace to frames. The zero-invention answer was already in the evidence: the field is 'fully tree-occluded from above', so the zone is ringed by trees and the drive simply runs on under closing canopy. Deliberation that only adds is decoration; deliberation that removes something you are attached to is the thing actually working.

- SESSION #pattern-the-owner-reviewed-a-png-and-found-what-six-guards-could-not - Daniel looked at one map render and said the smithy was on the wrong side of the property. No guard in this repo can catch that, because every one of them checks internal consistency and none of them knows what the real place looks like. That is the third session running where his direct knowledge beat careful derivation, and it is why 13.1's LOVE-IT gate is worth its cost: showing him a picture mid-priority is not a courtesy, it is the only test available for the class of error that matters most here.

- SESSION #pattern-read-the-checker-not-just-the-cure-it-suggests — MCV told me to run a script that does not exist, and I logged a carry-forward saying the gap was uncurable. That was wrong. The tool DOING the checking (verify-session-claims.py) was present the whole time and documents its own assertion schema in its header comment. I had searched for the remedy the error message named and stopped when it was missing, instead of reading the thing that produced the error. When a cure is missing, go read the diagnostic.

- SESSION #pattern-i-repeated-the-exact-mistake-the-last-session-recorded — S105's reflexion says, in plain words: added a file_lacks assertion, then wrote an explanatory comment quoting the buggy line verbatim, so the assertion failed on its own documentation — 'describe the old behaviour, do not paste it'. I then did precisely that in palette-check.ts, quoting the removed hard-coded parcel list inside the comment explaining its removal, and MCV failed on it. Reading a lesson is not the same as having it. The durable fix is not vigilance but the assertion itself, which caught me inside one session rather than three.

- SESSION #pattern-the-verifier-caught-two-claims-i-had-not-checked — Of the three assertions that failed, two were mine being simply WRONG about a file I had written minutes earlier: I asserted the survey document contained phrases it did not. Nothing was fabricated in the work, but the CLAIMS about the work were unverified guesses. That is the whole value of machine-checkable assertions over prose check_method: prose cannot be wrong out loud.

