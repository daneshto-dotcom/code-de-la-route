# Reflexion Log — Legacy of the Realm

Cross-session learnings for the PDCA pipeline. Newest session at top. Pruned to ~50 entries; older entries live in archived handoff docs.

## 2026-08-22 — Session S118: THE DEPLOY THAT WAS A DASHBOARD CLICK, AND THE CUTSCENE THAT HAD TO BE REDRAWN TWICE

- S118-P2 #consistency-was-the-wrong-axis-and-nothing-i-could-measure-would-have-said-so: I built the cutscene out of the game's own atlas tiles specifically so the protagonist could not drift — he IS player.png, the same bytes in every panel, so the AI film's worst failure was impossible by construction. The argument was airtight and the result was wrong: "it looks like a part of the game. its not correct." A cutscene's job is to read as a DIFFERENT MEDIUM, and I had optimised the one property that guaranteed it would read as the same one. Every other defect tonight fell to a number; this one could only be found by a person looking at it, which is precisely what §13.1 is for. The base rate I keep citing — unmeasured reads of a frame being wrong eight times out of eight — is about DEFECTS. It says nothing about whether the right thing was built, and I had quietly started treating "measured" as a synonym for "correct".

- S118-P2 #a-derived-quantity-is-only-as-good-as-its-inputs: Page 3's caption clipped, so I replaced the hardcoded `CAP_LINES = 2` with a value derived from the box's own height — exactly the fix tonight's chat-bubble defect had just taught me, and correct in itself. It turned every other page's h=34 box into `floor((34-16)/12)` = ONE line, and six of the eight captions came back ellipsised. I fixed one page and broke six, and the only reason I know is that I looked again. "Derive, don't hardcode" moves the risk from the constant into the inputs; it does not delete it. When you make a number derived, re-check every existing caller against the new formula in the same pass.

- S118-P1 #the-deploy-was-never-blocked-on-a-vps-and-tonight-it-was-not-blocked-on-a-token-either: S117 discovered that forty sessions of "VPS blocked on Daniel" was false. Tonight the same shape nearly repeated one layer along: the vault holds a Cloudflare token, so the obvious framing was "I have a credential, therefore I can deploy". Cloudflare rejected it outright — code 1000, invalid — and there is no wrangler OAuth on the box. Probing that took ninety seconds and changed the whole deliverable from "I will deploy it" to "here are three clicks and a folder that is already verified". The instinct worth keeping: probe the credential before designing around having one.

- S118-P1 #a-deploy-is-an-allowlist-never-a-directory: The natural move was to point a static host at `rebuild/`. That folder contains `docs/SITE-SURVEY-*.md` and `realm/*.json` — measured cadastral geometry of a real place where a real family lives — and the game repo is PRIVATE, so it would have been a first disclosure rather than a re-publication. It took a `ls` to notice and it would have taken nobody to notice afterwards. The guard has two independent nets on purpose: set-equality reports an extra file as "unexpected", which someone in a hurry adds to the allowlist, whereas the deny list reports it as "this is survey data about a family's home", which is a different conversation. Word the failure so it cannot be dismissed.

- S118-AMENDMENT #the-arithmetic-and-the-pixels-agreeing-is-how-you-know-both-are-right: He found the dialogue bubble clipping its own last line. Measured in-engine: Dael's line wraps to 4, Phaser's Text height is exactly 12*lines, the body ran to y=286, the box's inner edge was 282 — a 4px overflow. Then separately: 10 + 12*4 + 2 = 60 against a box of 56, short by four. Two independent routes to the same number, which is worth more than either alone. And the fix was not 56 -> 62: it was making BOX_H the SUM of the things that have to fit, so the box cannot be too short for its own contents by construction.

- S118-P2 #the-guard-caught-what-the-eye-called-fine-twice: `PIL.ImageDraw.rectangle(fill=None)` is not a no-op — it paints WHITE. I had written it as a placeholder meaning "leave the gateway empty", the pages looked plausible, and only the every-pixel-is-a-declared-ink assertion found #FFFFFF on all four pages that open the gate. The same assertion later caught a planted magenta in a negative test. A palette assertion sounds like bookkeeping right up to the moment a library's default betrays you.

- S118-P2 #i-read-a-frame-and-was-wrong-for-the-eighth-time: I looked at pages 1 and 7 and said the open-gate overlay was not landing. A pixel diff of the 32x32 gate region: 148 differing pixels. It was landing; it is simply subtle at 1x where the gate is two tiles. Eight consecutive unmeasured reads of a rendered frame, eight wrong. What is new is that I now catch myself mid-sentence and reach for the diff — the reflex is finally cheaper than the assertion.

- S118-P2 #phaser-reuses-the-scene-instance-so-a-class-field-runs-once: Driving the cutscene twice in one page found a session-fatal freeze: `done` survived the first `finish()`, `update()` returned on its first line, ENTER and ESC were both dead, and the player sat on page 1 for ever. Reachable today — JOURNAL -> ACCOUNT -> RESTART replays the arrival with no reload. A class field initialiser is not per-run state; it is per-CONSTRUCTION state. Nothing static could see this, and it only surfaced because I ran the thing a second time instead of once.

- S118-P3 #naming-the-unrun-step-is-the-work-not-an-excuse: The deploy workflow's `wrangler pages deploy` line has never executed and cannot be from here. S117's lesson was #test-the-gate-before-shipping-the-gate, and the temptation was to treat that as forbidding the file. It does not: it forbids the SILENCE. So the step is written, the header says in as many words that it is unproven, and the route that needs no credential is documented first and verified end to end. Shipping the half that can be proven and naming the half that cannot is the second time this exact resolution has been reached (S117-P5/P6a) — it is becoming a rule rather than a judgement.

- S118-SESSION #i-contaminated-his-browser-for-the-third-session-running: To test the cutscene I set era 1601 and planted positions in localStorage on the shared profile, then had to notice and clear it before handing anything over. S117 logged this twice. The mitigation I keep NOT building is the one that would work: a throwaway origin, or a teardown in the same call that does the planting. Writing the lesson down a third time is evidence that writing it down is not the fix.
- S118-RULE22 #a-green-local-suite-and-a-red-ci-are-not-a-contradiction-they-are-a-measurement-of-the-difference: The mandated end-of-session audit found Rebuild CI had failed on EVERY push since S117 while every handoff reported 'build PASSING'. Both statements were true: it passes on this machine and fails on the runner, and nobody had looked at the runner. The failure was not art drift - all 17 files differed in SIZE only, because a byte comparison of a COMPRESSED file asserts the compressor as well as the content, and the job pins Pillow<12 while this machine runs 12+. That is CF-S114-MCV-VERSION-ASSERTION-CLASS wearing a different coat: never bind an assertion to a value a correct change will move. The tempting fix was to pin Pillow, which would also have gone green and would have kept the assertion married to an encoder for ever; the real fix decodes the PNGs and compares pixels. Two things worth keeping: Rule 22 exists precisely because a local green is a claim about one laptop, and the fix was verified by the runner going green rather than by my reasoning about why it should.

- S118-CLOSE #i-reproduced-the-exact-lesson-my-own-handoff-was-citing: MCV hard-failed at close with 63 failures, and the first twelve were mine: every S118 verification entry was `{file, claim}` PROSE, so the verifier reported `unknown assertion type: None` and marked all three priorities WEAK. S114's entry for this is titled #i-quoted-the-lesson-and-then-broke-it, and its text is 'I had written long, accurate, genuinely-verified check_method PROSE for each - and prose binds to nothing on disk.' I wrote 700 words of accurate prose per priority, three times, in a session that cites that entry approvingly in its own boot snapshot. Knowing a rule, citing it, and even writing a fresh reflexion entry about a neighbouring rule does not execute it. The only thing that would have caught this earlier is running the verifier BEFORE the stop hook does - which is now the obvious carry-forward, because the gate firing reactively is exactly what the INTEGRITY-WARNING PROTOCOL says must never happen. Replaced with 63 typed assertions; 717 pass, 0 fail, 0 unbound.

- S118-CLOSE #an-assertion-bound-to-an-implementation-dies-with-the-implementation-for-the-third-time: Twelve more failures were S110-P4 and S113-P2 needles pointing at strings inside the film - 'WHY A DOM <video>', 'the-arrival.mp4', 'PRESS ENTER TO SKIP', 'awaitingBegin', 'skipArmed'. They were CORRECTLY failing: the owner had the film replaced with a comic, so the code those needles described is gone. The tempting repair is to re-add a dead comment so a needle goes green, which is the tail wagging the dog. What those priorities actually delivered was THE ARRIVAL AS A SCENE WITH A FIXED HAND-OFF CONTRACT and a skippable first viewing, and both still hold, so that is what they assert now. This is CF-S114-MCV-VERSION-ASSERTION-CLASS's third appearance (after a version number and a file path): the rule keeps being restated because the failure keeps arriving in a new costume. Assert the invariant, never the implementation that currently satisfies it.

## 2026-08-22 — Session S117: THE SESSION THE GAME ITSELF GOT LOOKED AT — the first NPC menu ran off the screen, the JOURNAL stopped leaking diagrams, and the deploy blocker turned out never to have existed

- S117-P7 #i-fixed-fail-quiet-then-reintroduced-it-one-commit-later: I added a loud console.error to `openGate()` because it resolved in silence when the DOM was missing — the instrument-lies-in-the-comforting-direction defect, named. ONE COMMIT LATER I wrote `showWhoami()`, which returns in silence when it cannot find its element, in the SAME FILE, on the same day. Daniel found it within minutes: the bar sat in a letterbox that does not exist on a full-bleed canvas, so it was invisible AND said nothing. Naming a failure class does not inoculate you against it — the next early-return needs the same scrutiny as the one you just fixed, and mine did not get it.

- S117-P7 #i-contaminated-the-browser-he-was-about-to-open: To prove sign-out preserved the world I planted a rigged save — cellar, key in hand, era 1601 — into localStorage, then handed him the URL. The pane and his browser share a profile, so he landed in the cellar and reasonably reported the boot order as broken. It was not; the order was correct all along. Twice this session I gave him something I had quietly polluted. Test fixtures belong in a throwaway profile, and a verification that mutates shared state is not finished until it is cleaned up.

- S117-P7 #the-answer-was-his-not-mine: I built a DOM letterbox bar, then a restart button beside it, and both were the wrong shape. He said: put it in the JOURNAL, reachable by ESC. That is obviously right — it cannot be cached stale, cannot be invisible, is drawn in the game's own colours, and is reachable from anywhere in the world. I had even measured earlier that a twelfth KEYS row would not fit and concluded 'no room', when the book already had PAGES. The constraint I found was real and the conclusion I drew from it was too narrow.

- S117-P7 #a-path-is-a-moving-target-in-the-same-way-a-version-number-is: MCV hard-failed at close because three P6a assertions named `deploy/account-rules.ts` and I had MOVED that file to `src/core/AccountRules.ts` — correctly, because tsc rejects a cross-rootDir import that esbuild accepts, and a build passing on only one of two compilers is a trap. That is the SECOND class of brittle assertion I wrote in a single session, after a full-string `scripts.test` equality I broke myself two priorities later. CF-S114-MCV-VERSION-ASSERTION-CLASS says do not assert a version NUMBER because any correct bump invalidates it; the same is true of a PATH, and for the same reason. Assert the capability at a location, and expect the location to move.

- S117-P1 #the-clean-frame-was-the-unusual-one: Two photographs of one screen disagreed and I framed the question as 'which is lying'. Both were honest. The ghosted frame was the NORMAL path - a player who reads the help and comes back - and the clean frame was the rare one, taken by a scenario that had never opened HOW TO PLAY. I spent the first half of the priority building a case against the camera, because a contradiction FEELS like an instrument fault. The cheaper move was to ask what was DIFFERENT about the two runs. The answer was one keypress of history.

- S117-P1 #the-environment-changed-under-a-documented-workaround: SEEING-THE-GAME.md is a careful document built on 'the pane does not composite'. It does now. loop.frame advances and computer{screenshot} returns a real frame, because rAF is SUSPENDED IN A HIDDEN TAB and S116 ran backgrounded. The doc's own INSTRUMENT_CHECK (clockMoves) passes identically in both worlds, so nothing warned me. A workaround must assert the CONDITION it works around, not merely that it still runs - otherwise it outlives its premise and quietly becomes the least reliable instrument on the bench.

- S117-P1 #i-wrote-the-anti-test-i-was-reconciling: While recording P1 I asserted `scripts.test` by FULL-STRING EQUALITY - a needle pinned to a value any correct change was going to move - and then broke it myself two priorities later by adding test:box and test:accounts. S116 logged exactly this class four times and I reproduced it inside the same session that documented it. An auto-fill hook softened the assertion to a substring before MCV could go red on it, which is the only reason it did not become a false failure at close. Assert the CAPABILITY, never the current value of a list.

- S117-P2 #the-camera-spent-a-whole-session-pointed-at-the-furniture: S116 built the camera and pointed it at the JOURNAL and the pack - the settings screens. S117 pointed it at the GAME and the first NPC interaction had three defects in one method, one of them an unreadable option on the critical path into 1601. ParcelScene is 1,958 lines, the largest file in the codebase and where a player spends all of their time, and it had exactly ONE frame ever captured. The lesson is about aim, not instruments: a new tool gets pointed at whatever was most recently built, which is the least likely place for old bugs. Point it at the OLDEST thing a player touches first.

- S117-P2 #six-out-of-six-now-an-impression-of-a-ui-defect-has-never-been-right: Three false alarms this session - 'ENTER does nothing at the gate' (Dael was two tiles right and I was facing a wall), 'the pack's left hand slot is missing' (the doll is centred, 152..168 between labels at 150 and 170), 'a stray mark above the footer' (the caption's em-dash placeholder). S116 logged three of its own. That is SIX consecutive occasions on which my read of a rendered frame was wrong and a measurement settled it, against zero on which the impression was right and the measurement wasted. This is no longer a caution, it is a base rate: an unmeasured claim about a frame should not be spoken aloud, let alone filed.

- S117-P3 #two-places-deriving-one-number-is-a-bug-with-a-delay-fuse: The box computed its top edge as H-boxH-4 in the constructor and H-60 in show(): equal only while the height is 56, and silently divergent the day anyone changes it. moveCursor recomputed the row pitch from a literal 15 while show() had just derived it, so the caret would point between two rows the moment a pitch changed - which is exactly what my own four-option fix would have caused. Both were harmless today and armed for later. When two sites compute the same quantity, the bug is already written; only its trigger date is unset.

- S117-P6a #a-blocker-carried-for-forty-sessions-was-never-re-checked: Handoffs from S71 to S83 all said 'VPS deploy - blocked on Daniel (Hetzner)', and every session after inherited it. The truth: no VPS ever existed, the site ran off his own PC via a scheduled task and a tunnel, and HE switched it off deliberately in S102. The 502 I opened the session by calling an outage was the documented intended end state - and S102 had written down, in advance, that a killed tunnel yields a Cloudflare 502 'so 502 is not mistaken for still up'. A past session predicted this exact confusion and I walked into it anyway, because I read the CONFIG files and not the session archive. A blocker inherited without re-verification is a rumour with a ticket number.

- S117-P5/P6a #naming-the-cost-beats-paying-it-badly: Two priorities resolved the same way and it is the same principle. The sketchbook: waking it 'cheaply' would mean recording pages nobody can open, which is a state change with no visible consequence - the exact thing §9.2 forbids - so half-waking it would have BUILT the violation the law exists to prevent while looking like progress in a diff. The deploy: writing four authentication handlers I cannot run against any database, overnight and unsupervised, would put untested auth in the repo looking finished. In both cases the deliverable was the RULES plus an honest statement of what the rest requires. Shipping the half that can be proven, and naming the half that cannot, is not less work - it is the work.

- S117-SESSION #the-prune-was-destroying-what-the-policy-promised-it-preserved: At close I appended a DUPLICATE S115 block, because the existing one is headed `## S115 —` rather than `## <date> — Session S115:` and my grep only knew the second form. That overflowed the 50-entry cap and the pruner deleted whole blocks - 12 unique S111/S112 entries. I then checked the policy's own justification, 'older entries live in archived handoff docs', and S111's entries are NOT in .handoff-archive. So the cap has been quietly destroying learnings for several sessions while claiming not to. Restored from a backup taken one command earlier, and the prune now MOVES blocks into .claude/reflexion-archive.md instead of deleting them. Two lessons: take the backup BEFORE the irreversible step, and verify the claim a destructive policy rests on before trusting it.
## 2026-08-20 — Session S114: THE WORLDBUILD SYSTEM, THE HELD ITEM AND THE STACK — 3 priorities + an owner-found fix, and an MCV reconciliation that caught me repeating S113

- S114-17 #convergence-is-about-reasoning-not-facts: The strongest signal this protocol produces - two independent models agreeing without seeing each other - was WRONG once this session. Both proposed parenting the held item to a Phaser Container; neither had read the file, where the player is a plain add.image and no Container exists. Two models can share the same missing file, so unanimity raises confidence about REASONING and says nothing about FACTS. ANALYZE also found that 4 of the 5 wrong adoptions this session were unchecked factual claims, every one refutable in under a minute.

- S114-18 #three-paths-to-one-rule: Asked separately in ANALYZE what they would change, Grok said mandate a live smoke test before closing any verb or render proposal, and Gemini said adopt Verify-Then-Assert because every critical error came from asserting the state of the project instead of asking. That is the same conclusion my own reflexion reached from the other direction. Three independent paths to one rule (PROBE-BEFORE-ADOPT) is the closest thing to evidence a retrospective can produce - proposed to Daniel, not self-added, because rule changes are his under SELF-MAINTENANCE.

- S114-1 #build-the-cure-not-out-of-the-disease: Council cut my own design and was right. I proposed an in-game Phaser tile inspector; Grok pointed out it would convert a pointer through canvas scale and camera scroll - pixel arithmetic, the exact operation whose unreliability created the problem being solved. A fix built out of its own failure mode can be silently wrong in the same way. The static route has an IDENTITY mapping instead (render px == cols*tilePx, asserted not assumed), so there is nothing left to get wrong.

- S114-2 #a-guard-unwatched-is-not-a-guard #firing-is-not-damage-prevented: The staleness guard caught 3 stale renders on its FIRST run - and I still ran the negative test (touch atlas.png -> must exit 1) before claiming it worked. Then honesty cut the other way: git showed the regenerated renders byte-identical, so the catch had zero practical impact, and I had already told Daniel the explorer embedded stale art. Corrected it unprompted. A guard firing is not proof of damage prevented.

- S114-3 #the-instrument-was-reading-another-project: real-context-tokens.py reported 384,808 tokens from an mtime fallback pointing at Chateau Guardian's transcript. Had I pasted it, this handoff's budget line would have been another project's. S113 taught me to prove the counter moves; this time it moved - it was just measuring somebody else. Check WHAT the instrument is pointed at, not only THAT it responds.

- S114-4 #derived-grouping-not-invented-taxonomy: Gemini proposed curated tile categories (architecture/vegetation/ground/water) and asserted the data 'likely exists'. I probed: no category field anywhere in atlas.json, only 48 name prefixes. Authoring the taxonomy would have been me deciding Daniel's categories for him. Grouped by placement and evidence grade instead - both free, both real - and left the taxonomy as his call. A Council proposal resting on an unverified data claim needs the probe, not the adoption.

- S114-5 #a-stop-hook-is-not-a-fact: GOV-28 AUTO-CLOSE fired demanding the full close chain at a moment when S114 had produced nothing: it was reading S113's all-terminal priorities. Running it would have overwritten boot-snapshot.md - the next session's only orientation - with a snapshot of an empty session. I continued with the user-approved P1 instead and the state is consistent now that P1 is stamped. The underlying defect is real though: a hook's premise can be stale, so verify what it is asserting before obeying anything destructive.

- S114-6 #answer-the-question-before-fixing-the-code: Daniel arrived mid-priority with urgency markers and a mix of QUESTIONS ('what key throws?') and BUG REPORTS ('only one bottle shows'). The scope rule says stop and amend - but it does not say stop ANSWERING. Reading Items.ts and ParcelScene.ts to teach him the tier model cost little and resolved two of the three complaints without touching a line: the basin guess was right, the spill guess was wrong, and the missing bottles were never a bug because stacking does not exist. Diagnosis is not implementation.

- S114-7 #probe-before-adopting-a-council-fix: BOTH Council members independently proposed parenting the held item to the player in a Phaser Container. Unanimity across two models is the strongest signal the protocol produces - and it was still wrong here, because neither had read the file: this.player is a plain add.image and no Container exists, so adopting it meant refactoring movement, depth and the lunge to buy what deriving gets for free. Convergence raises confidence about REASONING, never about FACTS.

- S114-8 #measured-not-reasoned: I placed the held item at the top of update() with a written justification for why that was the right hook. Live measurement said otherwise: mid-step the offset read 6px where the constant says 5, because update() runs before the TweenManager. 1px for one frame is invisible and I could have shipped it behind the argument I had already written. Moved it to POST_UPDATE and re-measured to 5. A justification is not a measurement, and mine was covering a real defect.

- S114-9 #the-law-nobody-in-the-room-named: Council debated whether hands should stack on grounds of symmetry and physical plausibility. The actual answer was in Items.ts: law 1 says a vessel takes the nature of what fills it, so filling three empty bottles at the basin would have to split a stack mid-verb and invent a rule for where the unfilled ones go. Neither model had read the file. Capping the hand then collapsed the whole blast radius - the compiler found 5 sites, all pack, none in any hand verb. The project's own recorded laws outrank both models' reasoning, and reading them first made the work smaller.

- S114-10 #a-cancel-that-instantly-undoes-itself: ESC cancelled the split and the next frame re-opened it: the key was still down, the elapsed time still passed the threshold, so every condition openSplit() needed was still true. Static reading would never have caught it - the cancel code is correct in isolation, and only a live hold across many frames exposes it. The fix is conceptual, not local: a HOLD IS ONE GESTURE, so once resolved it must stay resolved until the key comes up. The regression test holds the key for 500ms after the cancel, because the bug is invisible in any shorter window.

- S114-11 #the-instrument-degrades-with-use: Synthetic KeyboardEvents stop registering after enough repeats in one page - twice I got readings from a tab where arrows and Enter no longer reached Phaser while Escape still did, and one of those readings looked exactly like a bug in my own code. Fresh reload before each scenario plus an explicit INSTRUMENT_CHECK as the first assertion. S113 taught me to prove the instrument moves; this session taught me it can stop moving PART-WAY THROUGH, so once at the start is not enough.

- S114-12 #dullest-possible-migration: The tempting v4->v5 migration collapses adjacent identical slots into one stack. Identical totals, strictly tidier - and wrong, because it makes the migration a REARRANGEMENT that has to decide which slot survives. One-to-one cannot be wrong; anything cleverer can. Related discovery worth keeping: load() migrates in MEMORY and does not persist until the first write, so the original v4 save survives the whole session as a safety net. My first test called that a failure in 120 cases; it is a feature.

- S114-13 #i-quoted-the-lesson-and-then-broke-it #prose-is-not-verification: The MCV gate hard-failed with all three S114 priorities UNCOVERED: zero typed assertions on three completed priorities. I had written long, accurate, genuinely-verified check_method PROSE for each - and prose binds to nothing on disk. What makes this the sharpest entry in the log is that earlier IN THIS SAME SESSION I wrote a reflexion entry that quoted S113's #prose-is-not-verification lesson approvingly, and then committed the identical error three times. Knowing a rule, and even citing it, is not the same as having a step that executes it. The fix that generalises: verification[] must be authored BEFORE status is set to completed, in the same write - not as a later tidy-up, because there is no later.

- S114-14 #assert-the-invariant-not-the-current-value: Two historical assertions bound the literal string SAVE_SCHEMA_VERSION = 4 and went red when P3 correctly bumped it to 5 - the THIRD time that same assertion has broken for that same reason (S113 already rewrote it once, from 3 to 4, and logged CF-S113-MCV-SUPERSEDED). Rewriting it to 5 would just queue the next failure. A version NUMBER is a moving target; the durable invariant is that each session's migration BRANCH still exists and still handles its own version. Rebound to the v2 branch and to the carry model. When a correct change breaks an old assertion for the third time, the assertion is wrong, not the change.

- S114-15 #the-gate-found-a-real-coverage-gap: MCV is not only a bookkeeping check. Being forced to write assertions per file made me notice that P3 had EDITED the v3->v4 migration branch (pack slots became {tile,qty}) and never tested it - the oldest reachable save shape was the one untested path. Added v3->v5 coverage (+5 assertions, passing). The discipline of binding every touched file to a claim surfaces the files you touched without thinking about.

- S114-16 #a-council-opinion-about-looks-is-a-hypothesis: I shipped the held-item sprite drawing only the ACTIVE hand, on Gemini's Council advice that two 16px items either side of a 16px body would be a smear at 320x288. Daniel found it in minutes: 'it shows when its in the right hand but not in the left'. Worse than invisible - pressing 1 with an empty left hand made what he was carrying appear to VANISH. The advice was a plausible guess about legibility and I adopted it as a finding; the owner looking at the running game is the actual measurement. Drawing both, mirrored when the back is turned and staggered when side-on, is legible and correct. A Council opinion about how something will LOOK is a hypothesis, and the only instrument that settles it is someone looking at it.

- S119-1 #the-same-wrong-question-in-two-places-twice: `hasPosition` means "was the save ever WRITTEN", and `setAccount()` writes - so the instant anybody registered, a brand-new player looked exactly like a returning one. The resume window duly asked someone who had never moved whether to continue. I added `hasPlayed` (a real tile too, since a fresh save leaves the tile at -1), fixed ResumeGate, ran it, and it worked. Then a fresh browser refused to adopt the world it had just signed in to collect - because main.ts still said `hasPosition`, one call site over. I committed the SAME error twice in one hour, and the second time was AFTER writing the fix for the first. The repo has a standing law for exactly this ("two places deriving one number is a bug with a delay fuse") and knowing it did not help. What would have: when a getter turns out to answer the wrong question, grep every caller of it in the same edit, before running anything.

- S119-2 #erasing-is-not-a-local-act-once-there-is-an-account: "Start new game" called `restartWorld()`, wiped the save, booted a clean game, and looked perfect. Then I reloaded: main.ts saw a live token, pulled the account, found revision 1 still holding the old world, saw an empty local save - and adopted the dead world back. The erase had lasted one session. The server was already built for this (its own suite asserts "a restart is one ordinary write, and the revision goes UP"); the client just never published it. Generalises past restart: ANY local mutation that the server holds a copy of is provisional until it is pushed, and a feature that looks complete on one device is untested until the page is reloaded once.

- S119-3 #the-only-reason-i-found-any-of-this-was-that-he-said-see-it-work-first: Typecheck was clean, 3,125 client assertions passed, 9 guards passed, and 80 endpoint tests passed against a real Postgres - with all three of the above bugs live in the code. Every one of them was a WIRING fault between correct parts, which is exactly the class no unit test in this repo is positioned to see. His instruction was "we will build it and commit it and push it after we see it works on pc", and that instruction is the entire reason the feature is not broken. The lesson is not "write more tests"; it is that a chain assembled from proven links still has to be walked end to end, and reloading the page is part of walking it.

- S119-4 #adopting-a-world-should-not-adopt-a-login: `adoptRemote()` installs the downloaded save wholesale, and `account` - username, salt, password hash - is a field of the save. So signing in on a fresh device replaced its login with whichever one the uploading device happened to hold, or with null. The token masks it completely for that session; the player finds out the next time they are offline and the gate has nothing to check a password against. Found only because a hand-made test payload happened to carry `account: null`. Worth keeping as a shape: when a blob crosses a trust boundary, ask which of its fields belong to the WORLD and which belong to the PERSON - the two travel together in one JSON object and only one of them should be replaced. It also surfaced CF-S119-CREDENTIAL-IN-SAVE-BLOB: the hash is being stored twice, in two columns, for no reason.

- S119-5 #the-constraint-was-already-satisfied-before-i-invented-a-way-around-it: Five inks (outline, skin, hair, two attire colours) will not fit in a 3-colour sprite, and I was ready to solve it with two layered sprites or 90 generated files — both of which ship art through a guard that decodes exactly ONE hardcoded filename, so they would have passed dishonestly. The answer was to READ THE SEVEN EXISTING GRIDS: every hair and skin pixel was already at row <= 7 and every garment pixel at row >= 8. A 16x16 frame is four 8x8 regions, so the head band and the body band never share one and each needs only three inks. That is how GBC OBJ hardware assigned palettes. The constraint I was designing around had already been met by the art, years of sessions before anyone needed it. Generalises: before engineering past a limit, measure whether the existing work already clears it — and prefer the option that makes the GUARD stricter over the one that routes around it.

- S119-6 #the-type-system-caught-six-of-seven-and-the-seventh-was-the-dangerous-one: Adding `character` to SaveData made TypeScript reject seven sites, which is the migration convention working exactly as its comment promises. But the branch for v7 DID NOT EXIST, so a v7 save matched nothing and fell through to `return fresh()` — and every save in existence was v7, including the live one. The compiler cannot see a missing branch; it only checks the branches that are there. Worse, the suite would have passed: test 10's coverage loop was the literal `[1,2,3,4,5,6,BUILD_VERSION]`, which at v8 covers 1-6 and 8 and skips SEVEN. Two independent safety nets, both blind to the same hole, because both were enumerations rather than derivations. Now `Array.from({length: BUILD_VERSION})`. When a guard lists the cases it covers, the case it does not list is the one that ships.

- S119-7 #the-instrument-was-the-deliverable: I spent several exchanges concluding the creation flow was broken — the scene would not stop, the sprite would not change, the canvas came back one flat colour. All three readings were wrong and the code was correct. `document.hidden` was true, the frame counter was frozen at 2930, and TWO scene operations were sitting in a queue waiting for a frame that would never arrive. Because the bundle is ESM, Phaser never populates `Phaser.GAMES`, so there was no way to see any of that from the console. The fix that mattered was not to the game: it was three lines in main.ts exposing the game on localhost, after which every question answered itself in one call. This project's fifth and sixth boot rules ("LOOK AT IT", "now eight for eight") were both earned by unmeasured reads of rendered frames — and for 118 sessions nobody built the instrument those rules were asking for. When verification keeps being the expensive part, the instrument IS the priority.

- S119-8 #one-page-further-away-is-the-same-as-absent: I added the CHARACTER page between PLAY and ACCOUNT. RESTART lives on ACCOUNT, shipped and working, and he wrote back within minutes: "you still didnt add a restart game or whatever option im ESC joural." He was wrong about the feature and right about everything that matters — a destination that needs one more blind keypress to reach is a destination that does not exist. Two rules out of it: a NEW page goes on the END, because an existing destination never moves for something that just arrived; and a header showing "current + next" is honest and still hides the book, so it now shows every tab with the live one filled. Discoverability is not a polish task you schedule after the feature.

- S119-9 #a-duplicate-that-fails-loudly-is-a-copy-a-duplicate-that-falls-back-is-a-trap: The four option lists live in build-player.py AND Character.ts, and I shipped them that way without noticing what the failure mode WOULD be. `variantRow()` returns 0 for an appearance the sheet does not contain — a deliberate, defensible fallback for a save naming an option a later build removed. Combine that fallback with a drifting duplicate and you get the worst class of bug in this repo's taxonomy: no crash, no red guard, just every affected player quietly wearing the legacy traveller and it reading as a shrug. The fix was not to de-duplicate (you cannot import across the language boundary) but to make the copy CHECKED — the generator reads the TypeScript and refuses to build on a mismatch, negative-tested by injecting a sixth hair colour. The general rule: a graceful fallback and a duplicated source of truth are individually good and jointly a silent-failure machine. When you add the second, go back and look at the first.

- S119-10 #i-moved-my-own-assertion-one-priority-later: P3 asserted the exact line `ch.created === null ? 0 : variantRow(...)`. P4 — the coherence pass, correctly — rewrote it to route through `isCreated()`, and the gate went red on my own work from ninety minutes earlier. That is the fifth and sixth sighting of CF-S114-MCV-VERSION-ASSERTION-CLASS in ONE session: four historical needles, one of mine in P3, and one of mine again in P4 (a `file_lacks` on a literal my own explanatory comment quotes). The pattern is now unmistakable and it is not carelessness about needles — it is that I keep binding to the SHAPE OF TODAY'S CODE rather than to the claim. `variantRow(SHEET_VARIANTS, ch.appearance)` survives a refactor; the ternary around it does not. Before writing an assertion, ask what a CORRECT future change would do to it.

- S119-11 #the-count-that-named-no-true-number: rebuild-ci.yml carried a per-suite assertion tally — "evidence (12), scene-surface (13), box-layout (25), account-rules (58), save-migration (3,029)" — and it was wrong twice over: the numbers were stale AND they were paired with the wrong suites, so 12/13/25/58 were each somebody else's count. It read completely plausibly. A hand-maintained tally decays into fiction faster than a stale number does, because a shifted list still looks internally consistent. Kept it, corrected, for the one reason that justifies it: a count that DROPS is the only failure the runner cannot notice for itself. Everything else — "8 guards", said three times — was deleted rather than updated, because a number nothing verifies is a liability with a long fuse.
