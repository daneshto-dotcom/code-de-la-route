{
  "_comment_s104": "S104 (2026-08-12) added ignore glob '*/rebuild/dist/*'. RATIONALE, stated openly because narrowing MCV scope silently is exactly what this mechanism exists to prevent: rebuild/dist is esbuild+asset BUILD OUTPUT, is listed in rebuild/.gitignore, and is regenerated byte-identically by `npm run build` from sources that ARE diff-bound (src/**, tools/**). Asserting on it would bind claims to artefacts rather than to authored code. This exactly mirrors the pre-existing '*/public/dist/*' entry for the frozen old build. The SOURCES of those artefacts (tools/build-atlas.py, tools/build-player.py, src/art/*) remain diff-bound and carry content assertions in S104 P2.",
  "_comment": "S102 (2026-07-17) PROJECT-LOCAL MCV watch config (S258 mechanism) for Legacy of the Realm. Scopes diff-binding to THIS project's agent-authored surfaces; the global config's WS1 toolkit roots (~/.claude/scripts|hooks|skills, parent-workspace plans/boot-snapshot) belong to parent-workspace sessions and were tripping cross-session false-positive UNBOUND fails here (8 on 2026-07-17, all S176 DB-durability work from another seat). Roots below = the FROZEN old build (must not change post pre-rebuild-final tag — any drift needs a binding assertion) + the future rebuild/ surface + root plan/backlog docs. archive/ is deliberately NOT a diff-bound root: it is verified by its own mechanical checker (archive/tools/coverage.mjs) + per-priority verification[] bindings on key files.",
  "watch_roots": [
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/src",
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/public",
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/tests",
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/rebuild",
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/ACTIVE_PLAN_game_overhaul.md",
    "C:/Users/onesh/OneDrive/Desktop/Claude/Founder DNA/Extension Projects/Legacy of the Realm/Game/founding-realm/BACKLOG.md"
  ],
  "ignore_globs": [
    "*.bak",
    "*/_bak/*",
    "*/logs/*",
    "*.log",
    "*/public/dist/*",
    "*/rebuild/dist/*",
    "*/node_modules/*",
    "*/save/*",
    "*/test-results/*",
    "*/__pycache__/*",
    "*.pyc",
    "*/session-port.json",
    "*/session-state.json*",
    "*/rebuild/realm/realm.db",
    "*/rebuild/docs/renders/*",
    "*/rebuild/realm/realm.db-shm",
    "*/rebuild/realm/realm.db-wal",
    "*/rebuild/docs/s110-film/*.png",
    "*/rebuild/docs/s110-film/*/*.png",
    "*/rebuild/docs/s110-film/*.mp4",
    "*/rebuild/docs/s110-film/*/*.mp4",
    "*/rebuild/docs/s110-film/v1-rejected/*",
    "*/rebuild/docs/s110-film/v1-rejected/*/*",
    "*/rebuild/docs/s110-film/master/*",
    "*/rebuild/docs/s110-film/clips/*",
    "*/rebuild/docs/s110-film/seeds/*",
    "*/rebuild/docs/s110-film/rejects/*",
    "*/rebuild/docs/s110-film/PLATES.md"
  ],
  "_comment_s105": "S105 (2026-08-12) added ignore glob '*/rebuild/realm/realm.db'. RATIONALE, stated openly because narrowing MCV scope silently is exactly what this mechanism exists to prevent: realm.db is a DERIVED INDEX, listed in rebuild/.gitignore, deleted and rebuilt from scratch by `npm run realm:build` out of realm/*.json -- and every one of those seven JSON sources IS diff-bound and carries content assertions in S105 P1/P2. Asserting on the .db would bind claims to an artefact rather than to authored data. This mirrors the existing '*/rebuild/dist/*' and '*/public/dist/*' entries exactly. The realm/*.json sources remain diff-bound.",
  "_comment_s106": "S106 (2026-08-13) added ignore globs '*/rebuild/docs/renders/*', '*/rebuild/realm/realm.db-shm' and '*/rebuild/realm/realm.db-wal'. RATIONALE, stated openly because narrowing MCV scope silently is exactly what this mechanism exists to prevent, and this session already added 46 files under docs/renders alone: (a) docs/renders/** is PURE BUILD OUTPUT of tools/render-map.py, tools/render-views.py and tools/build-atlas.py - deterministic PNGs regenerated byte-identically from sources that ARE diff-bound and DO carry content assertions in S106 P1/P2. Asserting on them would bind claims to artefacts rather than to authored code, which is precisely the objection recorded for '*/rebuild/dist/*' in S104. (b) realm.db-shm / realm.db-wal are SQLite's shared-memory and write-ahead-log sidecars for realm.db, which is ALREADY ignored; they are transient, byte-unstable between runs, and carry no authored content. The realm SOURCE (realm/*.json) stays bound and is asserted record-by-record in S106 P4.",
  "_comment_s110": "S110: docs/s110-film/ holds GENERATED AI MEDIA (Imagen plates, Veo clips, the stitched film) produced for the cinematic. Only the BINARIES are ignored, on the same basis as docs/renders/* above: MCV diff-binding asserts text content, and there is nothing meaningful it can assert about a generated PNG. The narrow globs are deliberate — docs/s110-film/PLATES.md stays diff-bound, because the prompts, the model used and the honest gap-list ARE checkable text and are the part that must not drift. The images themselves are verified the only way images can be: by Daniel looking at the finished film, which is the P5 approval gate.",
  "_comment_s110_v1": "S110: docs/s110-film/v1-rejected/ holds a cinematic the OWNER REJECTED in full. It is kept rather than deleted (a rejected thing that survives in the record cannot be silently re-adopted) but it is not asserted against - there is nothing meaningful to bind to work that was thrown out. The sibling working directories are a live agent workspace and move under their own control; PLATES.md at the top level stays bound once the rebuild lands, because the prompts and the gap list are checkable text and are the part that must not drift.",
  "_comment_s110_plates": "TEMPORARY, AND IT MUST BE REMOVED WHEN S110-P5 CLOSES. PLATES.md is the film agent's manifest and the agent is still writing it, so it cannot be bound to a completed claim - MCV binds assertions from COMPLETED priorities and P5 is in progress (v1 was rejected by the owner and is being rebuilt). The manifest is the one part of the film that IS checkable text - the prompts, the model, the honest gap list - so it goes back under binding the moment P5 closes. If you are reading this and P5 is complete, delete this glob and assert against the manifest instead."
}